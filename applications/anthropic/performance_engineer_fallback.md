# Performance Engineer Fallback Notes

Keep this only as a fallback variant if post-run evidence materially strengthens the pure low-level performance story.

## Safe Angle
- strongest low-level evidence today is the backend dispatcher, fallback semantics, and low-bit runtime contract work
- strongest debugging evidence today is multi-backend behavior made explicit and testable
- do not default to this variant while `Science of Scaling` is the cleaner role fit
