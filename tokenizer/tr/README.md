# Turkish Tokenizer Cache (Opt-in)

This folder is reserved for the Turkish tokenizer cache. Populate it by running:

```bash
python scripts/download_tr_tokenizer.py
```

Default behavior remains the teacher tokenizer unless `use_tr_tokenizer=true`.
