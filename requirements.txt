# ==============================================================================
# MERTFORMER TITAN - REQUIREMENTS
# ==============================================================================
# Version: v27.0 - Production Ready
# Platform: CUDA / MPS / CPU
# ==============================================================================

# Core ML Framework
torch>=2.0
numpy>=1.24

# Hugging Face Ecosystem
transformers>=4.30
datasets>=2.12
accelerate>=0.20
sentencepiece>=0.1.99
protobuf>=3.20
peft>=0.4.0  # LoRA/Adapter Support (Critical for MacBook Fine-Tuning)

# Training Utilities
tqdm>=4.65
zstandard>=0.21  # For streaming datasets
pyyaml>=6.0      # YAML config overlays

# Testing
pytest>=7.3
pytest-cov>=4.1  # Optional: coverage reports

# System Monitoring
psutil>=5.9

# AGI Orchestrator (Optional)
# Uncomment if using full orchestrator features:
# duckduckgo-search>=3.8  # Web search
# beautifulsoup4>=4.12    # HTML parsing
# requests>=2.31          # HTTP client
# SpeechRecognition>=3.10 # Voice input
# Pillow>=9.5             # Image processing
# CUDA-specific (install via run.sh on Linux)
# bitsandbytes>=0.41.0
# flash-attn>=2.5.0

huggingface_hub>=0.20
scipy>=1.10
onnx>=1.14
onnxscript>=0.1.0
onnx-simplifier>=0.4.33  # Mobile Optimization (Critical)
wandb>=0.15.0            # Experiment Tracking
galore-torch>=0.0.1      # GaLore Optimizer (VRAM Saver)
