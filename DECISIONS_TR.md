# Mimari Kararlar

- **Config disiplini**: `config.py` çalışma zamanı varsayılanı; isteğe bağlı YAML overlay desteği.
- **Swarm mimarisi**: Hedef mimari olarak dokümante; core training'e zorunlu değil.
- **Multimodal**: Metin çekirdeği kanıtlanana kadar ertelendi.
- **Kanonik KD hattı**: Dış hat adı `offline_clean` korunur; 45K closure koşusu için katı precompute-KD yolu olarak ele alınır.
- **Öğretmen yüzeyi**: Teacher kanonik yolda `meta-llama/Llama-3.3-70B-Instruct`'e pinlenir; sessizce takas edilmez.
- **Fallback sınırı**: Kanonik `offline_clean` eğitim yolundan teacher'sız fallback kaldırıldı. Logit'ler eksikse, sessizce düşürmek yerine tam blocker ile fail edilir.
- **Prompt yüzeyi**: `prompts/system_v1.txt` post-training davranış kanıtı genişletmeyi haklı çıkarana kadar tek kanonik sistem-prompt yüzeyi kalır.
- **Artefakt stratejisi**: Mevcut release/repo zip'leri korunur; gerçek koşu erişimi için ayrı bir training-outputs bundle zip eklenir (`artifacts/mertformer_training_outputs_bundle.zip` + SHA256 + manifestler).
- **`use_bitnet` bağlanması (2026-09-02)**: `layers/ffn.py`/`mla.py`/`moe.py`/`liquid.py` artık `BitLinear` vs düz `nn.Linear`'ı önceki koşulsuz hardcoded `BitLinear(...)` yerine `layers/bitlinear.py::make_linear(cfg.use_bitnet, ...)` üzerinden kuruyor — flag bundan önce yapısal olarak hiçbir işe yaramıyordu (bkz. `BACKLOG.md`). `layers/liquid.py`'nin `packed_pair`/`packed_pair_compile` eğitim-implementasyonu internalleri bilerek yeni geçitten geçirilmedi, ham quant fonksiyonlarını doğrudan çağırmaya devam ediyor — ayrıca ölçülmüşler (2026-07-12 duvar-saati bulguları) ve `use_liquid=False` iken hiç çalışmıyorlar, `scripts/run_bitlinear_ablation.py` de BitNet'i izole etmek için bunu iki kolda da zorluyor. Yalnızca gelecekte bir pass `use_bitnet=False`'un Liquid yoluna da etki etmesini özellikle isterse yeniden ele alınmalı.
- **Denetlenmiş kararlar (2026-06-12 sıfır-çelişki kapanışı) — belgelenir, "yeniden düzeltilmez"**: Çok-ajanlı denetim bunları canlı kodla doğruladı; kasıtlıdır, bug değildir. P5/P8 README perf tablosu (~1.2s/step, ~107 tok/s, 11.000 tok/s toplam) açıkça "Projected/Est./Not Measured/architecture simulation" etiketli — claim-disiplinine uyumlu projeksiyonlar. P13 `utils/safety.py kill_if_non_finite` `utils/__init__.py __all__` ile export edilen public utility (orphan değil). P18 `config.auto_configure_batch_size` nvidia-smi: naif LOCAL_RANK guard'ı eklenmez (rank-0-only guard per-rank micro-batch'i desync ederdi); Method-A `torch.cuda.get_device_properties` gerçek GPU node'larında nvidia-smi'den zaten kaçınır, print'ler env-gated (`TITAN_CONFIG_VERBOSE`). P11 gated Meta `Llama-3.3-70B-Instruct` teacher yüklemesinde `trust_remote_code=True` kasıtlı (güvenilir birinci-taraf repo); production dataset yüklemeleri bunu etkinleştirmez. **Ölçülen param sayısı `3,672,982,022` (~3.67B)** kanonik ölçülen runtime toplamıdır; explicit `moe_intermediate=8192` (config/config.py) bunu tutar; `economics/flops_estimator.py DEFAULT_PARAMS=2.64e9` ayrı design-target olarak korunur (bkz. reports/FACTS.json). D3 Stage-1 kod kaynağı `bigcode/the-stack-v2`→`bigcode/the-stack-dedup` (v2 satırlarında `content` boş, `blob_id`/`src_encoding` taşır → ~%28 kod corpus'unu sessizce açlığa düşürüyordu; the-stack-dedup gerçek `content` sunar; preflight smoke-probe artık boş-alan kaynakta `SOURCE_FIELD_EMPTY` ile gürültülü fail eder; `datasets/hashes.json` revision/sha256 eğitim makinesinde re-pin edilmeli). Llama isimlendirme: tam uyum EXTERNAL-PENDING legal (bkz. NOTICE + reports/teacher_output_license_assessment.md). Tam ayrıntı: `DECISIONS.md`.
- **Mac'te yapılabilir backlog tasfiyeleri (2026-06-13, Pass 7) — gerekçeli "şimdi-değiştirilmez" olarak kapatıldı**: 33 topk bf16 reddedildi (bf16 top-k routing hassasiyetini düşürür + `scripts/cfc_moe_tolerance_check.py` gate'ini kırar; layers/moe.py fp32 cast kasıtlı). 14 Liquid impl default `TITAN_LIQUID_TRAIN_IMPL` ile zaten seçilebilir; DEFAULT'u çevirmek #1 MFU kaldıracı, pilot timing verisine bağlı. 17 MoE capacity-loop vektörizasyon yalnız GPU-perf (eğitim kutusunda). 20 teacher pad-mask GPU-perf (teacher-precompute makinesinde). 36 legacy logit-realign yolu kasıtlı env-gated escape hatch (`TITAN_ALLOW_LEGACY_LOGIT_REALIGN`, default off). 48 sharded checkpoint (accelerator.save_state) gerçek multi-GPU ister (eğitim kutusu). **23 MLA→GQA rename DONE (2026-06-16)** kanonik yolda; importer/test/grep/doc senklandı; kasıtlı korunan: `layers/mla.py` dosya adı + `TITAN_MLA_KV_PACK` env knob + 5 vendored onefile delivery kopyasındaki iç `MLA` sembolü (frozen bundle'lar). 34 train.py split / 68 whole-repo mypy kalıcı bırakıldı (yüksek blast radius, ~sıfır fonksiyonel kazanç). 40 drift telemetry deferred (`lifelong_safety.safety_metrics() last_drift` zaten var, katman default off; salt-additive). 24/25/31/38 test backlog (RoPE position, block-order schema, SIGTERM handler, packing resume-seam) additive pure-CPU coverage, risksiz deferred. Tam ayrıntı: `DECISIONS.md`.
- **MLA → GQA yeniden adlandırma (2026-06-16, DONE)**: Attention sınıfı `layers/mla.py` içinde artık `GQA` (her zaman grouped-query'di; latent-MLA bottleneck değildi). Tam ayrıntı için `DECISIONS.md`.
- **Doğrulanmış bulgular (2026-06-16) — belgelendi, davranış değişikliği 45K sonrasına ertelendi**: z-loss efektif ağırlığı `2e-6` (çift çarpım: `1e-4 * 0.02`), Liquid `dt=1.0` sabit (gated RNN), `epoch_mode` varsayılan `False`, sequential MoE dispatch + `.item()` (yalnız GPU-perf), `mark_weights_updated()` uncalled (cache-invalidation kancası — silinmez). Hiçbiri bu turda değiştirilmedi (45K koşusunu confound etmemek için). Mekanizma + post-45K aksiyon: `DECISIONS.md`.
- **Repo küratörlüğü — ticari/strateji katmanı (2026-06-16)**: Ana repo teknik/kanıt yüzeyidir (PRIVATE kalır; tek public yüzey README türevli Gist). İş materyali reviewer-facing ağaçtan çıkarıldı, **hiçbir dosya kalıcı silinmedi** (hepsi `/private/` altına taşındı ya da untrack edilip yerelde yeniden üretiliyor). Taşınanlar: anthropic iç-strateji notları → `private/anthropic_internal/`; `PITCH(±TR)` + el yazımı `sales_funnel_90d(±TR)` / `ip_licensing_split(±TR)` / `founders_hub_application(±TR)` → `private/commercial/` (README/README_TR index link'leri kaldırıldı). Untrack: `reports/investor_deck.pptx(±TR)` (`build_investor_deck.py` yerelde üretir). Yerinde bırakılan (closure-ladder tarafından **üretilen**, sıkı bağlı — belgelenir, rewire EDİLMEZ): `gtm_master_plan.md`, `commercial_handover_pack.md`, `legal_ip_pack.md`, `reports/commercial_handover/` — bunları çıkarmak yeşil ladder builder'larını rewire etmek (davranış değişikliği) gerektirir; safe-fix/belgele politikası gereği internal closure-governance artefaktı olarak kalır. Aktif ticari işin kanonik yeri ayrı private **dealroom** reposudur. **⚠ DÜZELTME (2026-07-27):** `reports/commercial_handover/`'ın "yerinde kalır" kararı bir gün sonra (2026-06-17, commit `90bf7c0`, aşağıdaki satır) fiilen tersine döndü — klasör `private/commercial/commercial_handover/`'a taşındı; bu, `build_closure_governance_pack.py`'nin kendi scorecard kanıt kontrolünü + 2 doc referansını ~1 ay boyunca kırık bıraktı (2026-07-27 X-ray denetiminde bulundu, kanıt path'leri gerçek konuma repoint edildi — klasör geri taşınmadı). Tam ayrıntı: `DECISIONS.md`.
- **Repo küratörlüğü — ikinci geçiş (2026-06-17): tek-persona + kanonik scaffold**: Kök kanonik scaffold eklendi (`STATUS/TRUTH_MATRIX/BACKLOG/GOVERNANCE/REPRODUCE.md` + `_TR`) → reviewer için tek giriş. `archive/` kuruldu; `docs/tr/` bilinçli kurulmadı (EN↔TR parite gereği TR ikizleri yan yana kalır). README 178KB→~4KB kesildi (tam hâli `archive/`'a; `sync_manifest.py` artık tam ağaç yerine `docs/PROJECT_STRUCTURE.md`'ye pointer basıyor). Anthropic başvuru paketi → `private/anthropic_internal/`; kalan tüm ticaret/GTM/outreach → `private/commercial/` (el yazımı) ya da gitignore (generated). Closure cluster: redundant/superseded generated raporlar gitignore (yerelde üretilir), el-yazımı superseded olanlar → `archive/closure/`; kalan generated closure detayı kök scaffold'un işaret ettiği **ikincil generated detay** olarak korundu (tümünü gitignore'lamak yeşil ladder'ı churn ederdi). Boş şablonlar → `archive/templates/`; ASCII-Türkçe `codex_deep_audit_DE` → `archive/audits/`. Çelişkiler kaynakta/generator'da düzeltildi (test-sayısı 370, 3.70B→3.67B, the-stack-dedup, closure_57 satır 53 out-of-scope, `/Users/` path sızıntıları + guard kör-noktası). Tam ayrıntı: `DECISIONS.md`.
- **Eğitim ölçeği — ara koşu yok, doğrudan 45K (2026-06-17)**: Kanonik koşudan önce ya da sonra **ara-ölçek model koşusu (250M / 500M vb.) YOK** — ne TÜBİTAK ne başka bir milestone için. Sıradaki ve tek eğitim işi **kanonik 3.67B 45K koşusudur (K1)**. **Neden:** daha küçük bir *model* kendi eğitim dinamiklerine sahiptir (MoE 8-top2 ve Liquid/CfC mixer küçük ölçekte farklı davranır — toy ablation Liquid etkisinin gürültüde kaybolduğunu zaten gösterdi), 45K davranışını öngörmez, "kanıt" yüzeyi olarak compute/zaman değmez. **Bu karara DAHİL DEĞİL (korunur):** 45K öncesi ücretsiz smoke bir alt-ölçek model DEĞİLDİR — `TITAN_MAX_STEPS=2` ile *kanonik 3.67B mimarinin kendisinin* birkaç adım koşulması (davranış-param değişikliği yok) + K4 checkpoint zinciri (save→restore→resume). Bu yalnız "kod import olup adım atıyor mu + artefaktlar geri dönüyor mu" doğrulamasıdır — pahalı 45K compute'unu kırık bir import/checkpoint zincirine yakmamak için ucuz sigorta (son H200 koşusu artifact retrieval'da ölmüştü). "250M deneme" değil. Tam ayrıntı: `DECISIONS.md`.
- **45K-öncesi de-risk pass'i — operasyonel sertleştirme uygulandı, eğitim-davranışı yine ertelendi (2026-06-17)**: İki bağımsız kıdemli review (doküman + 6-turlu 45K kod denetimi) canlı doğrulandı; kod denetiminin hükmü: kanonik tek-geçiş yolunda garantili çökme yok, asıl de-risk 2-GPU smoke + koşunun kendisi. **Uygulandı (eğitim matematiğini değiştirmez → confound etmez):** grad_clip tek-yönlü ratchet kaldırıldı (transient; latent bug), atomik checkpoint (`os.replace`), resume `weights_only=False`, MoE runtime buffer'ları `persistent=False`, `MoE.forward` içi `all_reduce` kaldırıldı (DDP/checkpoint-recompute NCCL-deadlock riski + per-step host sync), telemetri throttle (log_interval 1→10 + host snapshot `TITAN_TELEMETRY_INTERVAL=100`). **Token bütçesi:** 23.6B kanonik + 1024 explicit opt-in (~188B, 8×; LR/curriculum step-based, ölçeklenmez) + `TITAN_STRICT_TOKEN_BUDGET` hard-fail guard. **Yine document-only (kod değişmedi):** z-loss 2e-6, dt=1.0, dropout 0.1 stack (undertrained rejim), early-stop patience=5 — koşu-config kararı. **Doküman (frontier-lab cila):** `TECHNICAL_REPORT(±TR)` klinikleştirildi (synaptic/wisdom/fluid-intelligence/Onyx-Storm/Emotional-Weighting register düştü; §10 "spekülatif, uygulanmadı, kapsam-dışı"), `ARCHITECTURE` Liquid 12-seed verdict'e senklandı, inert `orchestrator/` AGI runtime + flag-off cognitive katmanlar out-of-scope belgelendi (silinmedi), MoE-ara-boyut 8192 düzeltildi, closure_57 item 20 "kv-head sharing" olarak dedup. **Öğretmensiz smoke knob'u (davranış-nötr):** `config.py` `distill_alpha` artık env-override edilebilir (`TITAN_DISTILL_ALPHA`, default 0.8 — varsayılan davranış değişmedi); `TITAN_DISTILL_ALPHA=0` kanonik eğitim girişini 70B teacher'ı yüklemeden/indirmeden smoke koşmaya izin verir (train.py teacher'ı yalnız `distill_alpha>0` iken yükler; loss saf cross-entropy'ye düşer). REPRODUCE(.md/_TR)'de tek-komut öğretmensiz pre-45K smoke. **Lisans çelişkisi → Proprietary çözüldü (2026-06-17):** README/README_TR footer'ı `Apache-2.0` diyordu ama otoriter `LICENSE` dosyası **Proprietary & Confidential, tüm haklar saklı** (CONTRIBUTING dış katkıyı kapatıyor, CITATION.cff zaten `Proprietary`, pyproject `license={file="LICENSE"}`, repo private + ticari dealroom). README footer'ları tek hataydı → "Proprietary & Confidential — tüm haklar saklı; bkz. LICENSE" olarak düzeltildi. Bilinçle proprietary yönüne çekildi (diğer tüm metadata yüzeyiyle uyumlu + güvenli yön; başıboş `Apache-2.0` kazara açık-kaynak grant'i = geri-alınamaz). Llama yükümlülükleri değişmedi, `NOTICE`+`MODEL_LICENSE.md` ile açık. Hukuki tavsiye değil; gerçekten açık-kaynak yapma kararı ayrı, bilinçli bir karardır. **Dış review pass — 5 madde, canlı doğrulandı (2026-06-17):** ✅ DÜZELTİLDİ `pyproject.toml`'a eksik `mertformer_sdk.kernels.cpp` eklendi (`cpp/__init__.py` var). ✅ DÜZELTİLDİ (claim) `TECHNICAL_REPORT(±TR)` §3.1 "12x" → Hedef/tahmin, §7 SHA256 → "tasarım" (henüz 45K zinciri yok). ✅ Self-doc: `apply_github_policy.sh` artık rapora `note` yazıyor → `return_code:1` beklenen best-effort 403 (private/non-Pro, §0.8). ❌ BUG DEĞİL: `ci.yml` `checkout@v6`/`setup-python@v6` — bunlar VAR (2026 ortası released) ve reponun kendi CI'ı bugün yeşil `success`; v4/v5'e düşürmek çalışan pipeline'ı regress eder → dokunulmadı. ✅ Zaten yapıldı: README Apache→Proprietary önceki pass'te. **Llama naming (EXTERNAL-PENDING legal):** Llama-3.3 yalnız KD teacher + tokenizer olarak kullanılıyor (Llama weights yok); öğrenci adının "Llama" taşıması gerekip gerekmediği hukuki-yargı sorusu, net rename değil — NOTICE/MODEL_LICENSE'da açık, public model release öncesi IP/legal review'a işaretli. **Bulgu-listesi temizliği + whitepaper fast-forward senk (2026-06-17, Pass 4.5):** app/web Claude WHITE_PAPER'a (EN+TR) arXiv Section 8 ekleyip push etti (`eed0a4d`/`0e8f040`); local temiz fast-forward geride kaldığı için `git merge --ff-only` ile senklendi (divergence/çakışma yok). Ayrı "nihai bulgular listesi" canlı doğrulandı, **yalnız gerçek+güvenli** olanlar düzeltildi: **Doc (EN+TR)** WHITE_PAPER_TR §2 "Statik"→"Durumsuz"; CHANGELOG 2026-06-17 bölümü; IMPLEMENTATION_PLAN Founders-Hub (2026-05-31 yapıldı); TASK private/commercial repoint; README_CHECKLIST tazelendi; ablations/results+no_liquid 12-seed verdict'e senk (+0.50 pilot superseded); AGENTS/START_HERE gitignored `reports/final_*` annotasyonu. **Config/security** ruff→v0.15.5, base.yaml log_interval 1→10, Dockerfile constraints.txt, registry→Build30 V2, secret_scan+policy gho_/ghu_/ghs_/ghr_+PAT, pip_freeze chess. **BUG DEĞİL (bırakıldı):** moe_dispatch parallel/sequential çelişki yok; secret_scan↔policy regex aynıydı (tek gap gho_); CI @v6 yeşil. **Intentional (bırakıldı):** env.lock/cuda.lock template, warmup_steps self-documented dead field, ADR-0001 historical (immutable), dataset TBD disabled. **Pre-45K freeze:** post-45K backlog kod maddeleri + mimari gözlemler dokunulmadı. **Kullanıcı aksiyonu:** `gho_` token'ını GitHub'dan revoke et. **NİHAİ EKSİKLİKLER temizliği — doğrulanmış alt-küme (2026-06-18, Pass 4.6):** Paralel app-Claude'un tüm-gün derlediği ~207 maddelik "nihai eksiklik listesi" 3 Explore ajanıyla canlı doğrulandı; **yalnız gerçek+güvenli** olanlar düzeltildi (training-math'a dokunulmadı, pytest 370 değişmedi). ✅ Stale `BUILD 27`/`V27.0` prefix'leri 14 yorum/print/docstring'den kaldırıldı (kod-logic'i değişmedi). ✅ Reviewer-facing doc-truth: `CITATION.cff` (false `date-released` kaldırıldı, "Onyx Storm" düştü, affiliation eklendi); `LICENSE`(+`_TR`) "MertFormer AI Team"→"Mert Yünlü" + partner/investor overclaim yumuşatıldı + çelişen "confidential Swarm/LiquidRouter" isimlendirmesi çıkarıldı; `SECURITY.md`(+`_TR`) internal jargon temizlendi + gerçek vuln-reporting (GitHub Security Advisories); `CONTRIBUTING.md`(+`_TR`) `codex/`→`feature/`/`fix/` + dürüst AI-asistan disclosure (git-history `Co-Authored-By: Claude` endişesini history-rewrite olmadan kapatır); `requirements.txt` header → Build 30 V2. ✅ Observability: `distillation_manager.py` resume-state `except: pass` artık log basıyor (fallback aynı). ✅ Parite: TROUBLESHOOTING(±TR) NCCL/corruption/tokenizer senaryoları + 2 tiny TR-orphan'a EN twin. ❌ **BUG DEĞİL/FALSE:** "git tag push edilmedi" YANLIŞ (13 tag remote'da); moe_dispatch/CI@v6/secret↔policy önceden teyitli. **LEFT (quarantine/freeze/external):** inert orchestrator (core/self_audit), tüm pre-45K freeze maddeleri, post-45K/infra, deprecated/peripheral TR-orphan, EN-only kasıtlı docs. **Skip:** README badge (PRIVATE repo → canlı badge 404 + gist churn). Tam ayrıntı: `DECISIONS.md`.
- **Repo-geneli nihai denetim kapanışı (2026-06-27, Pass 5) — belgelenir, "yeniden düzeltilmez"**: Devasa bir çok-ajanlı denetim her tracked dosyayı okudu, bulguları yüzeye çıkardı, gerçek olanları canlı kodla doğruladı ve iki cerrahi, davranış-koruyan dalgada düzeltti (Wave A = T1/T2 yapısal+label+bug, 263 uygulandı / 131 dosya; Wave B = T0 mekanik). pytest boyunca 370 passed / 4 skipped; verify_all yeşil. Kararlar kasıtlıdır, geri-alınmaz: **`layers/mla.py` korundu (rename YOK), yalnız-label fix** — sınıf grouped-query attention uygular (GQA: `num_kv_heads`=8 < `num_heads`=16), latent-MLA değil; label/docstring'ler "GQA"ya düzeltildi (`scripts/sync_manifest.py` role-override → "grouped-query attention (GQA) implementation"); dosya adı/modül/state-dict key'leri kasıtlı rename EDİLMEDİ (rename `release_manifest.json` / `immutable_evidence_register.json` / onefile hardcoded SHA referansları + mirror-parite yüzeyini path+SHA sürekliliğinde kırardı; dosya adı bir identifier'dır, claim değil — düzeltilmesi gereken tek şey yanlış "latent attention" claim'iydi). **Liquid/CfC 45K yolunda kalır** — 12-seed ablation inconclusive (Δ küçük, p=0.305); kaldırmak da eşit-kanıtsız, mühürlü kararla korundu; ayrıca `layers/moe.py`'deki `Liquid`/`Fluid Dynamics` label'ları netleştirildi (causal depthwise Conv1d + BitLinear gate, continuous-time CfC cell DEĞİL; gerçek CfC `liquid.py`'de); isim/param path'leri mühürlü checkpoint/state_dict kontratına bağlı olduğu için korundu. **`orchestrator/` + spekülatif katmanlar (`qinn.py`, `world_model_head.py`) = inert / kapsam-dışı** — 45K yolunda feature-flag OFF (hiçbir training loss'a bağlı değil) olarak dürüstçe işaretlendi; içlerindeki gerçek bug'lar düzeltildi ama promote/remove EDİLMEDİ; `qinn.py.__version__` artık `mertformer_sdk.__version__`'dan beslenir (tek doğru kaynak); `world_model_head *_logits` alanları tanh-sıkıştırılmış state olarak belgelendi (logit DEĞİL), isimler test/mirror-parite geriye-uyum için korundu. **`mertformer_sdk/kernels/{cpp,metal,vulkan,npu}` = dürüst stub'lar** — `bitnet_cpu.cpp` düz `torch::matmul` olarak (ternary kernel değil), `metal/vulkan/npu/engine.py` `F.linear` fallback stub olarak (henüz custom shader yok, roadmap) etiketlendi; korundu, silinmedi — kanonik low-bit yollar `bitlinear.py` (PyTorch STE, training) ve `triton_fused_bitlinear.py` (GPU). **Generated raporlar generator'da düzeltildi, elle değil** — `scripts/sync_manifest.py matrix_payload` artık GERÇEK karşılaştırma (`missing_in_structure`/`missing_in_manifest` set-diff; `ok` hesaplanır), hardcoded-green değil; `docs/PROJECT_STRUCTURE.md` ve tüm `reports/*` kaynaktan regenerate edilir, elle edit EDİLMEZ. **2.64B design-target vs 3.67B ölçülen param çelişki DEĞİLDİR** (biri design-target, diğeri ölçülen instantiate sayısı; ikisi de measured/target disiplini ile raporlanır; hangi boyutun gerçekten eğitileceği gerçek bir pre-45K görevi, belgelenir, gizlenmez). **`tokenizer/tokenizer.json` `interfaces/tokenizer_spec.json`'un frozen byte-mirror'ı** (`check_tokenizer_sync.py` ile zorlanır); Wave-A `note` honesty edit'i geri alındı (frozen mirror byte-aynı kalmalı). **S25 ternary CPU-kernel microbenchmark** kapsam-sınırlı ölçülen kanıt olarak eklendi (`reports/benchmarks/ternary_kernel_microbench_s25/`: README + results.json + .cpp); tek-op CPU/NEON microbench (FMA tier bit-exact 3.01×; SDOT turbo ~8.3×, approximate int8); açıkça tam-model t/s DEĞİL, NPU DEĞİL, kanonik kernel dispatcher'a entegre DEĞİL — yalnız ilgili kernel kanıtı. **`config/` vs `configs/` ve çoklu entrypoint (`run.sh`, `zero_touch_start.sh`, `launch_*.command`) olduğu gibi korundu** (yeniden yapılandırma çok sayıda path referansını etkiler, frozen koşuyu kozmetik düzen için kırma riski); kanonik training girişi `run.sh`, runtime default yüzeyi `config/config.py`. **Sonsuz-cila döngüsü yok** — bu son kozmetik/hijyen/truth-sync pass'i; kalan gerçek gate sahip-compute → 45K koşusu → checkpoint → checkpoint-bağlı eval; daha fazla "cila" açıkça kapsam-dışı. Tam ayrıntı: `DECISIONS.md`.
- **Wave C eki (2026-06-27) — versiyon-string tekdüzeliği (yeniden BÖLME)**: Modül `__version__` 35 modül dosyasının tamamında tekdüze `"1.0-BUILD30-V2"` (kanonik proje versiyonu; `CITATION.cff v1.0-TITAN-BUILD30-V2`, `registry/` ve release-zip `...B30...` adlandırmasıyla uyumlu); önceki bir dalga 8 tanesini `"1.0"`a indirip 3-yönlü split yaratmıştı, bu tekdüzelik için geri alındı (bu sabitler dead/unreferenced; hiçbir test/manifest/schema okumaz). `-BUILD30-V2` suffix'i yeniden soyulMAZ (güncel build kimliği, stale fossil değil; yorumlardaki `V21.0 FIX` / `V23.0:` gibi gerçek fossiller kaldırıldı). `__author__` tekdüze `"Mert Yünlü"` (birkaç dosyada çıplak `"Mert"` ile split'ti). `sync_manifest.py` mla role-override, legacy `mla` dosya adının SHA/manifest sürekliliği için korunduğunu ama implementasyonun GQA olduğunu açıkça not eder. Tam ayrıntı: `DECISIONS.md`.
- **Wave D eki (2026-06-28) — post-audit doğruluk/stabilite düzeltmeleri (geri ALMA)**: Tüm `RMSNorm` variance'ı FP32'ye upcast eder — `layers/mertformer_block.py RMSNorm.forward` (block norm1/norm2 VE final_norm tarafından kullanılır) artık `x.float().pow(2).mean(...).rsqrt()` hesaplayıp geri cast eder (`layers/mla.py _QKRMSNorm`'u aynalar); hız için bf16-variance'a geri DÖNME (precision/consistency kazancı 3.67B ölçeğinde önemli; değişiklikten sonra pytest 370/4 değişmedi, hiçbir exact-value test bf16 variance'a bağlı değildi). `config.weight_quantization` etiketi `"absmax_per_tensor"` → `"rms_per_channel"` olarak gerçek koda uyduruldu (`bitlinear.py weight_quant` = per-row RMS); yalnız-label, alan hiçbir yerde okunmaz (doğrulandı) → truth-fix, davranış değişikliği değil. `bitlinear.py weight_quant`'a quant parite notu eklendi: `liquid.py jit_quant` ile lockstep kalmalı (ikisi de per-row RMS); birini absmean'e çevirmek diğerini çevirmeden train(weight_quant)/eval(jit_quant) desync ederdi. Tam ayrıntı: `DECISIONS.md`.

### Laptop preflight koşu geri-bildirimi (2026-07-02) — 45K öncesi kararlılık sinyali (belgelendi, frozen yola UYGULANMADI)
Tek-laptop preflight (`scripts/preflight_run.py`, RTX 5070, commit `5fc5068`; kanıt `evidence/2026-07-02-laptop-preflight/`) `LR=1.5e-3`'te **ıraksadı** (grad_norm → inf; loss rastgelenin üstüne çıktı). Karar: bu gerçek koşu-geri-bildirimidir ve [BACKLOG.md](BACKLOG.md)'deki 45K öncesi kararlılık kalemlerini açar, ama **frozen eğitim yolu `main`'de değişmez** — her fix (LR rejimi, göreli Liquid eşiği, `generate()` Liquid-state parite, held-out ppl harness) kendi compute-koşusunda uygulanıp yeniden doğrulanmadan main'e girmez; böylece kanonik 45K doğrulanmamış bir training-math düzenlemesiyle confound edilmez. Iraksayan koşu checkpoint'leri yalnız altyapı kanıtıdır (SHA-referanslı; ağırlıklar hariç, `.pt` gitignored).
- **45K öncesi kararlılık pass'i (2026-07-08) — aday düzeltmeler eğitim yoluna UYGULANDI, gerçek koşuda yeniden doğrulanmayı bekliyor**: Kaynak brief `CLAUDE CODE — AUTONOMOUS EXECUTION BRIEF · Pre-45K Stabilization + Repo Final-Sweep Pass` (rev v2), macOS'ta yerel çalıştırıldı (CUDA yok — RTX 5070 ayrı makine). Yönetici karar: `DECISIONS.md`'in kendi duran politikası "eğitim-matematiği değişiklikleri **belgeli aday düzeltmeler** olarak iner ve nihai sayılmadan önce kendi compute-koşusunda yeniden doğrulanır" der; bu pass tam olarak o maddeyi işletir — **sıradaki GPU koşusu bu düzeltmelerin doğrulamasıdır**, ıraksama "çözüldü" DEĞİLDİR. **Uygulananlar:** LR rejimi (`1.5e-3`→`3e-4` *tarama başlangıcı*, router ×1.5 farkı kaldırıldı, warmup 0.10→0.15, üçü de `TITAN_*` env ile taranır); ölü config alanları (`warmup_steps`/`min_lr_ratio`) gerçekten okunur hale getirildi (davranış korunarak — `min_lr_ratio` varsayılanı 0.01'e çekildi); Liquid spike eşiği mutlak→EMA-göreli (mutlak 5.0 yalnız ilk 100 gözlemde soğuk-başlangıç tabanı; 2026-07-02'de ~10.4 loss ölçeğinde mutlak eşik her adım vurduğu için Liquid katmanları **hiç eğitilmemişti**); `generate()` Liquid-state paritesi (gerçek bug: her decode adımı CfC yinelemesini `h=0`'dan yeniden başlatıyordu → üretimde Liquid katmanları durumsuz no-op'tu; teacher-forced eğitim bu bug'ı hiç tetiklemiyordu); yeni `eval/held_out_ppl.py` (checkpoint-zorunlu, sabit korpus+seed, deterministik packer; rastgele-başlangıçta `mean_nll 11.7649` ↔ `ln(128000)=11.7599` ile doğrulandı); yeni `config/model/mertformer_pilot_stabilization.yaml` (**ölçülen** 171,617,923 param, tahmin değil; yalnız-kararlılık, kabiliyet/ölçekleme iddiası YOK → mühürlü "ara-ölçek koşu yok" kararıyla çelişmez, STATUS'a uzlaştırma cümlesi eklendi); WSD cosine clamp (resume'da `TITAN_MAX_STEPS` değişirse LR tabanda kalmak yerine tepeye geri tırmanıyordu). **YENİ ve ön-onaylı DEĞİL (bilinçle işaretlendi, gizlenmedi):** genel loss-ıraksama devre kesici (`utils/divergence_guard.py`, `safety_brake_reason="loss_divergence_relative"`) — NaN freni yalnız sonsuz/NaN loss'u, Liquid guard yalnız Liquid parametrelerini yakalıyordu; "sonlu ama tırmanan" loss'u (10.4→15.0, hiç NaN olmadan) hiçbiri durdurmuyordu. Inert `failure_budget.py`'ın slope mantığından port edildi ama duvar-saati yerine loss EMA'sına bağlandı (deterministik + test-edilebilir); orijinal dosya yerinde, kullanılmadan, artık doğru etiketle duruyor (inert kod promote EDİLMEZ). `loss_ema` rank-yerel olduğu için fren kararı **all-reduce** edilir (H1 NaN-fren presedansı) → DDP desync/NCCL hang yok. Varsayılan AÇIK; **Mert bilinçli olarak tutmalı ya da düşürmeli.** **Tasarım sapması (bilinçli):** brief `MertFormer.forward()`'ın liquid state'leri *döndürmesini* istedi; bunun yerine `_present_liquid_states` üzerinde yayınlanıp `get_last_liquid_states()` ile okunuyor — çünkü ~15 çağrı yeri (`train/`, `eval/`, `tests/`, `mertformer_sdk/` ve **`trainer_core.py`'daki ONNX export wrapper'ı**) `forward()`'ı katı 3-tuple açıyor; 4. çıktı traced export kontratını değiştirirdi. Seçilen desen aynı sınıfın `_last_world_model_outputs` deseni. `reset_router_state()` **genişletildi, rename EDİLMEDİ** (mühürlü `mla.py` presedansı). **Ayrıca:** `AGIPaths.DATA_DIR` eksikti (`orchestrator/core.py:186` okuyordu → anında `AttributeError`) — inert kodda gerçek bug, düzeltildi, promote edilmedi; `utils/logger.py` `"source_sha256_status": "pending"` kalıcı bir yalandı → `"not_applicable_live_stream"`; logger↔logbook_build sır-redaksiyon listeleri yeniden senkronlandı (40-hex yakalayıcı canlı logger'da yoktu); `config/base.yaml` INERT işaretlendi; `preflight_run.py` artık 45K'nın gerçek optimizer'ıyla (GaLore+8bit) koşar ve `last_loss > first_loss` olduğunda bloke etmeyen bir ıraksama uyarısı basar. **Sınırlı sweep (Track 2), 4 gerçek bug canlı doğrulanarak düzeltildi:** `cli.py` kpi-report CWD'ye çözüyordu (sessizce "tüm kapılar fail" raporu + exit 0); `kpi.py` göreli interpreter yolu (`FileNotFoundError`); `overfit_gate.py` off-by-one (sınırda **sıfır** dizi); `data_pipeline.py` bağlanmadan önce stage JSONL'ini siliyordu (ağ kesintisi = veri kaybı, üstelik `.exists()` kontrolü stage'i "var" sanıyordu). **pytest 370→388 passed / 4 skipped.** Teacher/tokenizer/dataset/prompt politikası, MoE 8e2, BitNet çekirdeği, Liquid matematiği, readiness semantiği ve iddia sınırı DEĞİŞMEDİ. Yasak iddia eklenmedi. **Kalan tek kapı aynı: sahip olunan compute → 45K → checkpoint → checkpoint-bağlı eval.** Tam ayrıntı: `DECISIONS.md`.


### 45K öncesi güvenli pass (2026-07-09) — eksik testler + operasyonel sağlamlaştırma (eğitim-matematiği değişmedi)
GPU'suz takip pass'i; açık "yapabildiğin her şeyi yap, commit/push, protokole %100 uy" mandate'iyle koşuldu. **Eğitim MATEMATİĞİ değişmedi**; `train.py`'daki iki dokunuş operasyonel sağlamlıktır, dinamik değil.
- **Şema anomalisi (iz için kayıt):** `interfaces/run_manifest_v1.schema.json` çalışma ağacında boşaltılmış bulundu (111 satır → `{}`), commit edilmemiş, bu oturumun eseri değil (muhtemelen paralel bir ajan/araç). Yedeklendi, sonra Mert kasıtsız olduğunu onaylayınca `git checkout bdee57a -- …` ile sealed haline döndürüldü. Gizemli değişiklik commit'e karışmadı.
- **Eksik testler eklendi (ertelenen birim-test yeniden-temellendirme pass'i):** `test_decoupled_rope`, `test_top_p`, `test_moe_capacity`, `test_quant_parity`, `test_held_out_ppl`, `test_repo_hygiene_guard`. `top_p` ve `moe_capacity` inline algoritmaların **karakterizasyon** testleridir (importlanabilir helper yok; tam-MoE forward testi global `cfg` singleton'ı yüzünden sıra-bağımlı çıktı → flaky test göndermek yerine düşürüldü).
- **`check_disk_space` fail-open → fail-closed:** probe hatasında artık `False` + sebep basar (tek çağıran yalnız yumuşak uyarı basıyor, asla abort/silme yok).
- **SIGTERM → nazik checkpoint:** `train()` artık SIGTERM'i `KeyboardInterrupt`'a çeviren korumalı bir handler kaydeder → mevcut "🛑 Durduruldu. Kaydediliyor…" kaydet-ve-bitir yolunu kullanır (cloud / Kaggle preemption SIGTERM yollar).
- **`TITAN_OCEAN_45K_LAUNCH` no-op kaldırıldı** (`scripts/launch_ocean_45k.sh`; hiçbir `.py` okumuyordu).
- **`datasets/validation_provenance.json` + `scripts/gen_validation_provenance.py`:** fingerprint-dışlama kanıtı (dosya SHA256, satır sayısı, 1500 satır-fingerprint).
- **Bilinçle ERTELENEN:** ADR-0005 tek-isimlendirme assert'i; `p100_safe max_steps` (demo-profil yargısı); D5/D7 (frozen-path → önce RTX-5070 re-run `bdee57a`'yı doğrulasın); `report_builder` baseline (zaten dürüstçe etiketli, bug değil).
  > ⚠ **YERİNİ ALDI (2026-07-25):** `BACKLOG.md`'nin "Pre-45K safe pass — DONE (2026-07-09)" bölümündeki eşleşen düzeltmeye bakın — bu 4 maddeden 3'ü (ADR-0005, `p100_safe max_steps`, D5) commit `8e8978f`'te (2026-07-11) gerçekten düzeltildi, D7 ise aynı pass'te bağımsız olarak denetlendi ve zaten doğru olduğu teyit edildi. Bu madde, düzeltme girdikten iki hafta sonra bile düzenlenmemiş kaldı; 2026-07-25'teki canlı yeniden-kontrole kadar fark edilmedi.
- **Test sayısı:** `388 → 412 passed, 4 skipped` (+24). Güncel-gerçek yüzeyler güncellendi; tarihli kayıtlar bırakıldı. **Kalan tek kapı aynı: sahip olunan compute → 45K → checkpoint → checkpoint-bağlı eval.** Tam ayrıntı: `DECISIONS.md`.


### Dış review triyajı (2026-07-09) — Fable-5 kod incelemesi, 3 bulgu doğrulandı & bölündü
Dış derin inceleme kodda bağımsız yeniden-doğrulandı (bulgu başına bir adversarial doğrulayıcı — hem onayla hem çürüt). Correctness bug'ı / iddia şişmesi / mühürlü-liste sorunu yok; üçü de en yeni kodun (güvenlik guard'ları + config loader) koşu-zamanı sağlamlık incelikleri. Compute'suz doğrulanabilirliğe göre bölündü:
- **Şimdi uygulandı (F2):** config-overlay loader fail-open → fail-closed (`config/config.py`; +`tests/test_config_overlay_strict.py`, `412 → 421 passed, 4 skipped`). Eksik/typo'lu/bozuk overlay veya bilinmeyen key artık sessiz kanonik ~3.67B yerine RAISE eder. Kanonik 45K (TITAN_* env) ve pilot (`preflight_run.py` boyutları hardcode) yolunda uykuda → bağımsız hijyen (Track 2 sessiz-yanlış-sonuç ve `check_disk_space` fail-closed sınıfı), eğitim-matematiği değişikliği değil. Doğrulandı: tüm shipped overlay'ler yalnız gerçek field kullanıyor (test-korumalı); test/CI overlay env-var'ı set etmiyor.
- **CANDIDATE tutuldu (F1, F3), RTX-5070 re-run için:** F1 = divergence guard'ı harmanlı yerine CE-only EMA'ya bağla (α-programı CE ağırlığını 0.2→0.85 çıkarırken harmanı şişiriyor); F3 = altı guard/spike state field'ını checkpoint'e persist et → resume, divergence referansını bozuk seviyeye soğuk-başlatmasın/yeniden-demirlemesin (`TITAN_AUTO_RESUME=1` ile etkileşir). İkisi de eğitim döngüsünün canlı brake/resume KONTROL davranışını değiştiriyor, yalnız koşuda uçtan-uca doğrulanır — yani `bdee57a` LR fix'leri gibi, apply-ready patch'ler BACKLOG'a yazıldı ve aynı re-run'da iner+doğrulanır, main'e **kör-commit yok**. Bağımsız hükümler: F1 gerçek-ama-şişirilmiş (garantili false-brake değil — warmup-sonu referansı gerçek pay verir), F3(ii) daha keskin operasyonel risk (unattended yeniden-demirleme israfı — guard'ın önlemek istediğinin tam tersi), F2 onaylandı. F1/F3'ü şimdi uygulamama gerekçesi: doğrulanmamış brake/resume kontrol mantığını main'e indirmek, tam da reponun "her şey ölçülü/doğrulanmış" iddiasını dürüst tutan disiplin; ikisi de ancak koşu yanarken ısırır ve korudukları koşuda doğrulanır.

### Masa-başı sağlamlaştırma pass'i (2026-07-09) — CI gate'leri + A2 izolasyon kanıtı (eğitim-matematiği değişmedi)
Review triyajı önerisinin güvenli, compute'suz alt-kümesi; her gate bağlanmadan önce zero-false-positive doğrulandı (mevcut ağaçta temiz).
- **A2 (I.2.5):** MoE router'ın per-sample batch izolasyonu kodu değiştirmeden `tests/test_moe_router_batch_isolation.py` ile kanıtlandı — `reset_router_state()` + per-batch-row buffer zaten garanti ediyor; yol haritasının boşluğu eksik testti. `layers/moe.py` değişmedi.
- **CI sertleştirme (I.7.4):** HIGH-severity/HIGH-confidence `bandit` güvenlik gate'i (tek bulgu — `scripts/build_validation_set._fingerprint`'teki dedup SHA1 — `usedforsecurity=False` işaretlendi); eager `orchestrator` paketi üzerinde circular-import subprocess testi; MoE capacity formülü için `hypothesis` property testleri; `interrogate` docstring-kapsama tabanı 15'te (mevcut ~%16.7). `hypothesis`/`bandit`/`interrogate` dev-deps + CI adımlarına eklendi; her biri bağlanmadan önce yerelde temiz doğrulandı.
- **Test sayısı:** `421 → 428 passed, 4 skipped` (+7). Hiçbiri eğitim-matematiğine, readiness'e ya da iddia sınırlarına dokunmuyor. **Tek kalan kapı aynı: sahip olunan compute → 45K → checkpoint → checkpoint-bağlı eval.**

### Nihai kapanış pass'i (2026-07-12) — 91 maddelik pre/post-45K backlog taraması + 42 dosyalık versiyon migration (eğitim-matematiği değişmedi)
Plan-onaylı devasa bir kapanış pass'i (`/Users/mertyunlu/.claude/plans/curried-drifting-catmull.md`), pre-45K/post-45K planlama konuşmasında gündeme gelen her maddeyi kapsar. 5 commit (`e0f5d75`..`f11e81a`), pushlandı. Eğitim-matematiği, readiness, iddia sınırları dokunulmadı.
- **Versiyon-string tek-kaynak migration:** `__version__ = "1.0-BUILD30-V2"`'yi hardcode eden 42 dosya (38 değil, gerçek sayı) `from config.build_label import BUILD_LABEL as __version__`'e taşındı (yeni sıfır-bağımlılıklı `config/build_label.py`). `layers/qinn.py`/`scripts/chat.py` bilinçli olarak ayrı `mertformer_sdk.__version__` SDK-semver şemasında bırakıldı (önceden var olan, kasıtlı ayrım, dokunulmadı).
- **Pilot yaml düzeltmesi (önceki pass'ten bekleyen):** `config/model/mertformer_pilot_stabilization.yaml`'ın `liquid_layers_idx`'i MoE'nin katman setiyle çakışıyordu (`[2,5]` vs `[2,5,8]`), config hiç yüklenemiyordu; `[1,4,7]`'ye düzeltildi, yeniden ölçülen param sayısı `171,617,923` → `172,668,035` (2 değil 3 Liquid katmanı). `scripts/preflight_run_pilot171m.py` (zip-only idi) repoya eklendi.
- **4 yeni kapanış kapısı + lokal bandit aynası + circular-import taraması, hepsi `verify_all.sh`'a bağlı:** `check_dead_attribute_disease.py`, `check_overlay_validity.py` (4 overlay'i taze subprocess'lerde yükler — pilot yaml çakışmasını manuel inceleme olmadan yakalayacak otomatik kontrol tam olarak budur), `check_facts_drift.py` (mevcut string-only `check_facts_consistency.py`'den farklı, canlı yeniden-hesaplama), `check_circular_imports.py` (mevcut `tests/test_no_circular_imports.py`'ı — ki o yalnız `orchestrator`'ın eager-import zincirini kapsıyor — tamamlayıcı), `check_bandit_security_scan.py` (bandit ZATEN `.github/workflows/ci.yml`'de `-lll -iii` ile bağlıydı — asıl boşluk lokal push-öncesi sinyalin olmamasıydı; bu, mevcut CI politikasını/exclude listesini birebir aynalar, yeni bir politika değil).
- **6 yeni checkpoint-bağlı eval probe'u** (`eval/calibration_ece.py`, `adversarial_prompt_robustness.py`, `bias_fairness_probe.py`, `toxicity_probe.py`, `hallucination_rate_probe.py`, `membership_inference_probe.py`), `eval/_probe_common.py`'ın dürüst desenini paylaşır: gerçek checkpoint öncesi `SKIPPED`/`NO_CHECKPOINT`, `--allow-random-weights` altında `random_init_smoke` (asla `measured` değil). `scripts/check_benchmark_regression_gate.py` bunları saklı bir taban çizgisine karşı karşılaştırır (şu an `SKIPPED`, henüz taban yok).
- **Yukarıdakini inşa ederken bulunan+düzeltilen 3 gerçek bug (bu pass'in kendi amacı değil):** (1) `scripts/build_scoped_external_intake_matrix.py`, okunamayan bir dış dosyada (`sha256_file()`'de yakalanmamış `PermissionError`) TÜM `verify_all.sh` merdivenini çökertiyordu — `safe_sha256_file()` sarmalayıcısıyla düzeltildi. (2) `eval/`'in `__init__.py`'ı yoktu (PEP 420 namespace package), `scripts/eval.py` (ilgisiz bir benchmark CLI'ı) tarafından gölgeleniyordu — `eval/__init__.py` + regresyon testiyle düzeltildi. (3) `check_bandit_security_scan.py`'ın kendisini inşa ederken 3 zincirleme bug bulundu (bandit `-x` `./`-relative path ister; `sys.executable` güvenilir değil; başarısız subprocess eski raporu taze gibi sessizce yeniden okutuyordu) — üçü de yalnız gerçek uçtan-uca testle yakalandı.
- **Aynı pass'te commit-sonrası bulunan+düzeltilen ev-idaresi (kullanıcının kendi incelemesiyle yakalandı):** `docs/PROJECT_STRUCTURE.md` ve `reports/{release_manifest,file_sync_matrix,policy_sync_report,project_structure_sync_report}` bayattı — `scripts/sync_manifest.py` ağacını `git ls-files`'tan kurar, ve son `verify_all.sh` koşusu (bunları regenerate eden) yeni dosyalar commit'lenmeden ÖNCE oldu, o yüzden o anda regen'e görünmediler. Commit'ler indikten sonra `sync_manifest.py` yeniden çalıştırılarak düzeltildi.
- **Master Protokol belgesi** (`~/Documents/MertFormer_Kapanış_Release_TruthSync_Master_Protokolu_2026-05-12.md`, Documents-dışı, repo commit'i yok): 9 boşluk düzeltildi, kendi iki tarihi addenda'sına (bu repodaki `1fdd9d6`/`64dd837` gibi) `⚠ DÜZELTME` blockquote'ları eklendi.
- **Lokal git ev-idaresi notu (bu pass'in kendi incelemesiyle bulundu, bu pass'in eseri değil):** lokal klonda kullanılmayan bir dal `codex/pre-sync-20260309_014748` (Mart 2026'daki ortak bir ata'dan bir commit ileride, hiç merge edilmemiş) ve Mart-2026 tarihli bir `stash@{0}` duruyor — ikisi de zararsız, yalnız-lokal, bu oturumdan önce var, `origin/main`'i etkilemiyor. Ayrıca reflog üzerinden doğrulandı: `1fdd9d6`, `45fdb15`'in `git commit --amend`'i ile oluşturulmuş (yalnız mesaj değişikliği, boş tree diff — hiçbir dosya içeriği kaybolmadı), bu oturumun kendi amend-yasağı disiplininden ÖNCEKİ bir pass tarafından; amend, o tek commit için `origin/main`'in geçmişinde görünen force-push'u gerektirdi.
- **Test sayısı:** `452 → 503 passed, 5 skipped` (+51, bu pass'in tüm Part'ları boyunca kümülatif). `bash scripts/verify_all.sh`: uçtan uca yeşil (33 kapı). **Kalan tek kapı aynı: sahip olunan compute → 45K → checkpoint → checkpoint-bağlı eval.**

### Uçtan uca pre/post-45K kalan-madde denetimi + 4 gerçek compute-gerektirmeyen düzeltme (2026-07-12)
Kullanıcı, kalan her pre-45K ve post-45K maddesinin uçtan uca yeniden-doğrulanmış bir dökümünü ve gerçek compute gerektirmeden ya da bir "Mert'in kararı" maddesini ezmeden kapatılabilecek HER ŞEYİN şimdi kapatılmasını talep etti. Canlı-dosya yeniden-doğrulaması (ajan özetine güvenmeden) önceki pass'in pre-45K listesinin doğru olduğunu teyit etti: `reports/launch_time_decisions_checklist.md` hâlâ 9/9 `_pending_`; bu dosyanın kendi kapanış cümlesi değişmedi; repoda hiçbir yerde `pilot171m_run_output.zip`/checkpoint yok (RTX-5070 pilotu gerçekten henüz koşulmadı); `BACKLOG.md`'nin F1/F3'ü hâlâ kelimesi kelimesine `CANDIDATE`; 3 karar-brief'i hâlâ kelimesi kelimesine "not a decision — Mert's call" ile bitiyor. Bunların hiçbiri, ne de pre-45K'da donmuş eğitim-matematiği maddeleri (z-loss, Liquid `dt`, ONNX opset, GPU-perf) dokunulmadı — gerçek GPU re-run'ı olmadan ya da Mert'in açık kararı olmadan bunları kapatmak, `1fdd9d6`/`64dd837`/`8e8978f`'in bir kere zaten düzeltmek zorunda kaldığı tam o sahte-"resolved" hatası olurdu (yukarıdaki `⚠ DÜZELTME` blockquote'larına bakın).
- **DÜZELTİLDİ + bisect edildi, gerçek bug:** "ladder-ezme bug'ı" (2026-07-09'dan beri bilinen, önceden "henüz bisect edilmedi") — `interfaces/run_manifest_v1.schema.json`'u aralıklarla `{}`'ye boşaltıyordu. Kök neden: `tests/test_final_orchestrator_cli.py`'nin `test_final_orchestrator_check_only_releases_lock_and_stays_fast`'ı, `build_contract_outputs`'u `root / "interfaces" / "run_manifest_v1.schema.json"`'a `"{}"` yazan bir sahte fonksiyonla monkeypatch'liyordu ve `sys.argv`'sinde hiç `--project-root` geçmiyordu — yani `root`, orchestrator'ın kendi `ROOT` sabitine, yani **gerçek repo ağacına** düşüyordu. Her `pytest -q` koşusu (dolayısıyla her `verify_all.sh`) tracked dosyayı sessizce bozuyordu. `--project-root <tmp_path>`'i bu teste ve kardeşi `test_final_orchestrator_plan_only_writes_contracts`'a ekleyerek düzeltildi. Bu, `scripts/final_orchestrator.py`'nin `run_post_plan()`'ında ilişkili ikinci bir gizli bug'ı ortaya çıkardı: `run_command()` `cwd=root` ile koşuyor, yani hardcoded göreli `"scripts/post_train_autorun.py"` yalnız `root` orchestrator'ın kendi dizinine denk geldiğinde çözülüyordu — `--project-root`'un (flag'in belirtilen amacı) gerçek bir prodüksiyon kullanımı aynı `FileNotFoundError`'a çarpardı. Sibling script'e mutlak yol (`ROOT / "scripts" / "post_train_autorun.py"`) referansıyla + `--project-root`'u ona da ileterek düzeltildi. Doğrulandı: `interfaces/run_manifest_v1.schema.json` byte-sayısı (1907) düzeltme sonrası tekrarlanan tam-suite koşularında değişmedi; 15/15 orchestrator + post-train-autorun testi geçiyor.
- **DÜZELTİLDİ, gerçek bug:** `scripts/repo_hygiene_guard.py`'deki "staged-vs-tracked" sınıf bug'ı (şema bug'ıyla aynı kök-neden sınıfı — `git ls-files` tabanlı bir kapı yalnız commit'lenmiş/sahnelenmiş dosyaları görür, yani aynı pass'te daha önce oluşturulmuş yepyeni bir dosya bir bare-`except`/build-fossil regresyonunu commit-öncesi kapıdan geçirip ancak commit-sonrası CI'da yakalanabilirdi). `tracked_files()`'ı çıplak `git ls-files`'tan `git ls-files --cached --others --exclude-standard`'a çevirerek düzeltildi, ki bu untracked ama gitignore'lanmamış dosyaları da ek olarak kapsıyor. Mevcut ağaçta temiz doğrulandı (`bare_except=0, build_fossils=0`); guard'ın kendi 4 testi etkilenmedi.
- **DÜZELTİLDİ, güvenli parite eklemesi:** `config/config.py`'nin `liquid_warmup_steps`'i artık `TITAN_LIQUID_WARMUP_STEPS`'i okuyor (mevcut `TITAN_ROUTER_LR_MULT`/`TITAN_WARMUP_RATIO` desenini aynalıyor); varsayılan `10000`'de değişmedi — doğrudan instantiation ile öncesi/sonrası doğrulandı.
- **İncelendi, bilerek çağrılmadan bırakıldı (bir düzeltme değil, çözülmüş bir bilinmeyen):** `layers/liquid.py`'nin `mark_weights_updated()`'i. `_ensure_qcache()`'in gerçek cache-geçersizleştirme mantığı izlendi: zaten her forward'da her param'ın PyTorch `._version` sayacını yeniden hesaplıyor ve herhangi bir uyuşmazlıkta geçersiz kılıyor, yani sıradan in-place optimizer adımları manuel çağrıya ihtiyaç duymuyor. Bu manuel kancanın hâlâ önemli olabileceği tek senaryo — `._version`'ı 0'a sıfırlayan ve bayat bir cache sürümüyle tesadüfen çakışabilecek tam bir `.data = new_tensor` rebind'i — bir GaLore/checkpoint-yükleme-yolu testi olmadan elenemedi, o yüzden metot çağrılmadan ve silinmeden bırakıldı, bu gerekçe artık çağrı noktasında (`layers/liquid.py`) belgeli, açık bir soru olarak kalmak yerine.
- **Bayat bir işaretçi düzeltildi, yeni bir karar değil:** `reports/publication_readiness_kit.md`'nin HF model-card iskeleti hâlâ `license: [PENDING — confirm final license...]` diyordu, ama lisans sorusu zaten kararlaştırılmıştı (Proprietary & Confidential, 2026-06-17 pass'i, bkz. `LICENSE`). İşaretçi bu kararlaştırılmış gerçeği yansıtacak şekilde güncellendi (`license: other` + bir not), gerçekten çözülmemiş daha dar detayı (fiili yükleme anındaki tam HF lisans-etiketi mekaniği) pending olarak işaretlemeye devam ederek — sahte bir yayın-hazırlığı iddiası uydurmadan.
- **`V2_BACKLOG_SEED.md` yeniden denetlendi** (son dokunulduğu `0ae9e6d`, 2026-06-16): kullanıcının elindekiyle byte-byte aynı; hâlâ doğru. Bir ekleme ortaya çıktı: Track F'deki "ONNX export opset 14→17/18" maddesi bu pass'in post-45K listesinde ayrı madde olarak yok, ama yukarıdaki iki ayrı DECISIONS.md girdisinde ("opset" ara) diğer pre-45K-donmuş maddelerle bundle edilmiş şekilde doğru izleniyor ve kodda değişmedi (`scripts/mobile_export.py:215`, hâlâ `opset_version=14`). Track A-E'ye dokunulmadı — bilerek closure-takip BACKLOG.md kapsamının dışında, bir boşluk değil.
- **Test sayısı:** `503 passed, 5 skipped` (değişmedi — yukarıdaki düzeltmelerin hiçbiri eğitim-matematiğine dokunmuyor ya da test yüzeyi eklemiyor/çıkarmıyor; yalnız bir test-izolasyon düzeltmesi, bir dosya-tarama düzeltmesi, bir env-override eklemesi, bir doc-yorum eklemesi, ve bir bayat-işaretçi düzeltmesi). `bash scripts/verify_all.sh`: uçtan uca yeşil. **Kalan tek kapı aynı: sahip olunan compute → 45K → checkpoint → checkpoint-bağlı eval.** Geri kalan her açık madde (tüm pre-45K launch-anı kararları, F1/F3, RTX-5070 re-run'ının kendisi, tüm post-45K checkpoint-bağlı maddeler) burada bilerek kapatılmadı, çünkü kapatmak ya henüz olmamış gerçek compute ya da açıkça bu pass'in değil Mert'in vermesi gereken bir karar gerektirirdi.

### İkinci gerçek-donanım teyidi: RTX 5070 36M + 171M pilot ıraksaması, z-loss çift-çarpım düzeltmesi, divergence-guard kör-noktası belgelendi (2026-07-12)

Mert, RTX 5070 laptop'ta 2026-07-08 pass'inin aday olarak indirdiği (o anki varsayılan) 3e-4 LR rejimine karşı iki koşu daha çalıştırdı: `scripts/preflight_run.py` (36M) ve `scripts/preflight_run_pilot171m.py` (171M pilot, T1.5 Go/No-Go kapısı), ikisi de commit `8e8978f`'teki kendi kendine yeten `git archive`-inşalı launch bundle'larından. **İkisi de ıraksadı** — 2026-07-02'de ilk görülen ıraksama sınıfının ikinci teyidi. Tam kanıt: `evidence/2026-07-12-laptop-preflight-v2/` (README.md, metrics_summary.json, iki temizlenmiş run config, iki tam temizlenmiş console log — Windows kullanıcı adı `Yiğit` `<WIN_HOME>`'a temizlendi, tüm-klasör leak-grep ile temiz doğrulandı).

- **Teşhis edildi, iki farklı yakın-tetikleyici, bir ortak muhtemel kök neden.** 36M iki kez ıraksadı, ikisi de bir Liquid-unfreeze olayıyla ilişkiliydi: `preflight_run.py`'nin ad-hoc `liquid_warmup_steps=200`'ü Liquid'i LR-warmup-bitişinden (274) VE divergence guard'ın arm noktasından (yine 274) ÖNCE çözüyor — bu script'e özgü bir sıralama bug'ı, pilot yaml'ın kendi dikkatle türetilmiş `liquid_warmup_steps=667 > 450`'inde zaten kaçınılmış (yaml'ın kendi yorumu zaten ad-hoc 200'ü kopyalamamayı uyarıyor). Onun Liquid EMA-göreli spike guard'ı (2026-07-08 pass'inden) adım 220'de doğru tetiklendi (3-strike kuralı) ve adım 414'te temiz şekilde yeniden çözüldü — bu güvenlik mekanizması tasarlandığı gibi çalıştığı doğrulandı. 171M bir kez ıraksadı, **Liquid hiç çözülmeden** (elle adım 563'te durduruldu, 667'nin gerisinde) — bu, 171M'nin tetikleyicisi olarak Liquid-zamanlamasını eler; başlangıç bunun yerine warmup sırasında LR'nin ~2.0e-4–2.5e-4'ü aşmasıyla, MoE `Overflow`/`Balance(std)` bozulmasıyla (0.03→0.15-0.19) lockstep şekilde ilişkili. İki koşuda da `clip=2.0`, `GradNorm` trilyonlara ulaşırken bile loss sinyalini kısmen korudu, ve router z-loss regularizer'ının efektif ağırlığı, iki koşunun kendi `run_config.json`'undan doğrudan doğrulandığı üzere, **2e-6** idi (`z_loss_coef=1e-4` × `router_aux_loss_coef=0.02`, bir çift-çarpım: `layers/moe.py` z-loss'u `aux_loss`'a bir kez ölçekliyor, `train/train.py` tüm `aux_loss`'u tekrar ölçekliyor) — ~1e-3'lük Switch-Transformer/ST-MoE konvansiyonunun ~500 katı altında (2026-07-25'te "~50 katı" aritmetik hatasından düzeltildi: `1e-3 / 2e-6 = 500`; İngilizce `DECISIONS.md`'nin ~136. satırındaki, farklı ve kendi içinde doğru olan bir başka "~50x" ifadesiyle karıştırılmasın — o, çift-çarpımsız *görünen* `1e-4` config değerine karşı ölçüyor, farklı bir payda; bu dosyada o ikinci kıyasın çevirisi yok). İki farklı ölçekte ve iki farklı tetikleyicide ortak imza (GradNorm patlaması + MoE routing bozulması + clip ile korunan loss), bu zayıf router regularizer'ını ortak kök neden olarak işaret ediyor.
- **DÜZELTİLDİ, aday — özel bir GPU re-run ile doğrulanmadı:** `config/config.py:329`, `z_loss_coef` `1e-4` → `0.05`. Bu, mevcut çift-çarpımı, `layers/moe.py`/`train/train.py`'nin çift-çarpım bağlantısını yeniden yapılandırmadan, efektif ağırlığı literatür-standardı `1e-3`'e (`0.05 × 0.02`) indirecek şekilde telafi ediyor (yapısal bir düzeltme ayrı, daha büyük bir değişikliktir, bu pass'te denenmedi). 2026-07-08'in tam bu katsayı üzerindeki freeze kararını bilerek yeniden açıyor. Yeni bir regresyon testiyle kilitlendi, `tests/test_z_loss_effective_weight.py` (efektif çarpımın hem `> 2e-5` — yani düzeltme-öncesi 2e-6'ya geri dönmediğini — hem de `1e-3` hedefinin `1e-6` içinde olduğunu doğrular). Mevcut F1/F3 adaylarıyla aynı disiplin: bir sonraki gerçek GPU koşusu doğrulama olayıdır, bu pass değil.
- **Belgelendi, bilerek DÜZELTİLMEDİ — yeni güvenlik-ilgili bulgu.** *(⚠ SÜPERSEDE 2026-07-25 — bkz. BACKLOG.md "C1 — divergence guard'ın grad_norm kör noktası": Mert bu oturumda bir tasarımı açıkça gözden geçirip onayladı, ve aday-düzeltme olarak uygulandı. Bu madde bulgunun tarihsel kaydı olarak birebir korunuyor; C1'den itibaren artık açık bir boşluk değil.)* `utils/divergence_guard.py` tam olarak okundu (`update_divergence_guard_state()` ve kendi docstring'i) ve saf `loss_ema`-tabanlı olduğu doğrulandı — hiçbir yerde `grad_norm` parametresi yok. Yukarıdaki iki koşuda da guard, zaten hafif yükselmiş bir referansta (erken-başlangıç kararsızlığıyla kirlenmiş) arm oldu ve sonra **hiç tetiklenmedi**, çünkü `loss_ema`, `GradNorm` trilyonlardayken bile o referansın `1.5×`'ini hiç aşmadı — `clip=2.0` loss sinyalini yapay olarak sınırlı tutarken GradNorm-yalnız patlamalarına karşı gerçek bir kör nokta. Yamalanmadı: bu pass'te değişikliği doğrulayacak başka bir gerçek-donanım koşusu olmadan güvenlik-kritik bir devre kesiciyi değiştirmek fazla riskli görüldü; boşluk bunun yerine `BACKLOG.md`'de yeni, açık bir madde olarak belgelendi, sessizce kapatılmadı.
  > ⚠ SONRAKİ GÜNCELLEME (2026-07-25, aynı günün ilerleyen saatleri): C1 gerçek donanımda doğrulandı — bkz. BACKLOG.md "Üçüncü gerçek-donanım teyidi (2026-07-25, Kaggle T4×2)". Hem 36M hem 171M yeniden-doğrulama job'ları tekrar diverge etti (LR/z-loss adayları tek başına yetersiz — bu diverjans sınıfında dördüncü negatif veri noktası), ama C1'in grad-norm eş-tetikleyicisi her iki seferde de doğru tetiklendi (36M step ~416'da, 171M step ~530'da), her ikisi de kontrolsüz bir patlama yerine temiz bir checkpoint kaydı üretti. C1'in kendisi "aday, uygulandı, doğrulanmadı"dan "aday, uygulandı, iki farklı ölçekte gerçek donanımda doğru çalıştığı doğrulandı"ya geçti. Asıl diverjans hâlâ çözülmedi — sadece ona verilen güvenlik tepkisi artık doğrulandı. Tam kanıt: `evidence/2026-07-25-lm-36m-reverify/`, `evidence/2026-07-25-lm-171m-reverify/`.
- **Test sayısı:** `505 passed, 5 skipped` (+2, yalnız yeni z-loss regresyon test dosyası — başka hiçbir test yüzeyine dokunulmadı). `bash scripts/verify_all.sh`: uçtan uca yeşil. **Kalan tek kapı aynı: sahip olunan compute → 45K → checkpoint → checkpoint-bağlı eval.** Mert'in bu pass'teki açık talimatı gereği ("bir daha direkt 45k öncesi koşu yapamayacaz" — artık başka bir doğrudan pre-45K koşusu yok), başka bir preflight/pilot re-run planlanmıyor; bir sonraki gerçek sinyal 45K launch'ının kendisi, ki bu artık yukarıdaki z-loss aday düzeltmesinin de doğrulama olayı.

### Sekiz launch-anı kararı kilitlendi + ölü dataset değiştirildi + 10 compute-gerektirmeyen kapanış maddesi (2026-07-19)

Mert, açık çoktan-seçmeli sorularla (varsayılmadan) sekiz gerçek launch-anı kararı verdi; her biri gerçekte ne değiştiğiyle burada kayıtlı. **Lane: `online_teacher`** (korundu — daha ucuz ama precompute'a bağımlı `offline_clean` yerine seçildi; `scripts/launch_ocean_45k.sh`'in mevcut default'uyla eşleşiyor, script değişikliği gerekmedi). **Liquid/CfC: Keep** (korundu, kod değişmedi). **Model boyutu: 3.67B ölçülen, kanonik** (korundu, kod değişmedi — 2.64B tasarım-hedefi gerilimi bu kararla çözülmedi, `economics/flops_estimator.py`'a dokunulmadı, ayrı bir planlama sabiti). **`top_k`: 256 değil 32** (gerçek değişiklik: `scripts/precompute_logits_topk.py`'nin `DEFAULT_TOP_K`'ı 256→32, `scripts/launch_8xb300.sh`'in bilgilendirici disk-bütçe echo bloğu eşleşecek şekilde güncellendi; hiçbir test eski default'u hardcode etmiyordu). **2 ölü Stage-5 tool-use dataset'i: düşürüldü + doğrulanmış canlı bir alternatifle değiştirildi** (Mert'in açık isteği: sadece düşürme, kategoriyi ve 23.59B token hedefini koru) — `gorilla-llm/gorilla-openfunctions-v2` (ratio 0.25) + `NousResearch/FC-1k` (ratio 0.15), ikisi de 2026-07-18'de canlı HTTP 401 doğrulandı, `scripts/data_pipeline.py`'nin `STAGE5_SOURCES`'ından çıkarıldı; yerine 2026-07-19'da HF Hub API'sinden canlı doğrulanmış (`gated: False`, `license: apache-2.0` — çıkarılan ikisinden daha temiz lisans durumu) tek bir kaynak eklendi: `NousResearch/hermes-function-calling-v1` (config `func_calling`, ratio 0.40 = çıkarılan ikisinin toplam payı). `scripts/titan_preflight.py`'nin `required_datasets` listesi de (hâlâ ölü `gorilla-llm` ID'sini taşıyordu, bilerek-düşürülmüş bir kaynak yüzünden preflight'ı fail ettirirdi) güncellendi. `datasets/SOURCES.md`/`_TR`, `datasets/LICENSES.md`/`_TR`, `datasets/inventory.{md,json}`/`_TR` (yeniden üretildi), ve `reports/dataset_health.md`/`_TR` güncellendi — `_TR` ikizi 2026-01-25'te donmuş bayat bir snapshot'tı (7/16), EN ise 2026-07-18'de tam tazelenmişti; bu pass'te tam parite sağlandı, yalnız 2 ölü kayıt yamanmadı. 23.59B token hedefi bu kaynak listesinden bağımsız (`TITAN_MAX_STEPS × batch × seq_len`'den hesaplanıyor), ve `ratio` alanları runtime'da kendi toplamlarına göre normalize ediliyor — yani ne token bütçesi ne Stage 5'in iç karışım aritmetiği ayrıca uzlaştırma gerektirdi. **3 lisans-TBD dataset (OpenHermes-2.5, github-code, guanaco-llama2-1k): tut, belgeli** (korundu, kod değişmedi — zaten 2026-07-13'ün durumuyla eşleşiyor, artık bilinçli bir risk-kabulü olarak formalize edildi). **Stage-3 TR/sentetik oranı: kodda zaten kararlaştırılmış bulundu, placeholder değil — icat edilmedi, ratifiye edildi.** Mert "şimdi karar ver" dedi, taze bir sayı bekleniyordu; ama inceleme `scripts/data_pipeline.py`'nin `STAGE3_SOURCES`'ının zaten somut bir oran taşıdığını gösterdi: wikipedia-tr (0.015) + CulturaX-tr (0.040) = 0.055 ham TR payı, cosmopedia-stories (İngilizce-sentetik, 0.015)'e karşı — kendi toplamlarına göre runtime-normalize edildiğinde bu **Stage 3 içinde ~%78.6 Türkçe / ~%21.4 sentetik** demek. Bu, kodda belgesiz duran, zaten verilmiş gerçek bir seçimdi, doldurulacak boş bir alan değildi; çalışan bir oranın üstüne icat edilmiş bir sayı yazmak yerine, bu pass mevcut oranı donmuş karar olarak resmen ratifiye etti ve ilk kez burada belgeledi — BACKLOG'un asıl işaret ettiği boşluk dokümantasyondu, verilmemiş bir karar değildi; kod değişmedi. **INT-KERNEL iddiası: şimdi relabel edildi** (korundu-yönü seçildi, gerçek kernel inşa edilmedi) — `TECHNICAL_REPORT.md`/`_TR`'nin "%93.75 bellek tasarrufu" satırı zaten "Tahmini"/"teorik" etiketliydi ama mevcut runtime'ın bundan HİÇBİRİNİ sağlamadığını (forward pass `layers/bitlinear.py`'nin STE'siyle düz bir fp-simülasyonu, gerçek ternary-matematik kernel'i bu repoda henüz yok) net söylemiyordu — bunu açıkça söyleyecek ve gerçekten ÖLÇÜLEN tek sayıya (checkpoint disk-paketleme, 17.99MB→1.06MB, ~17.0x — bir depolama-formatı sonucu, hız sonucu değil) işaret edecek şekilde yeniden yazıldı. **Watermarking: ilke olarak benimsendi, uygulama post-checkpoint'e ertelendi** (bu pass'in kendi araştırma/kararı, 8 sorudan biri değil) — Kirchenbauer-tarzı green-list logit-bias şeması en pratik uyum (model yeniden-eğitimi gerektirmiyor, yalnız generation-time wrapper); kod yazılmadı, gerçek checkpoint olmadan detection-accuracy doğrulanamaz, `reports/responsible_ai_checklist.md`'nin Watermarking satırı güncellendi. **`repo:012` (zararlı-otonomi/gözetim reddi): zaten çözülmüş olduğu doğrulandı, yeni bir düzeltme değil** — bu oturumun kendi önceki sentezi bunu açık sanıyordu; inceleme `MISSION.md`/`USE_POLICY.md`/`SECURITY.md`'nin zaten bunu açıkça söylediğini gösterdi; yalnız `reports/closure_risk_register.md`'nin çözüm-notu eksikti, o eklendi — yeni politika metni yazılmadı, gerekmedi.

**Ayrıca 10 compute-gerektirmeyen, her biri gerçek (kozmetik değil) kapanış maddesi:** (1) `ocean_2xh200_1024_first_launch_profile.md`'nin kopyala-yapıştır bloğunda eksik `TITAN_STRICT_TOKEN_BUDGET=1` + hâlâ duran ölü `TITAN_OCEAN_45K_LAUNCH=1` — ikisi de düzeltildi. (2) `runbooks/checkpoint_offsite_backup.md`/`_TR` (yeni): 14 Mayıs'ın checkpoint'inin kalıcı kaybına yol açan boşluk için somut bir runbook. (3) `scripts/secret_scan.py`'ye `anthropic_key`/`anthropic_service_key`/`google_api_key`/`google_oauth_client_secret` pattern'leri (13 Tem'in ev-dizini taramasının bulduğu gerçek boşluk) + 6 yeni test. (4)+(5) `mertformer_sdk/api.py`'ye `_validate_generate_inputs()` (tip/uzunluk/kontrol-karakteri/parametre-aralığı kontrolü, LLM-seviyeli prompt-injection'ın KENDİSİNE karşı değil) + `responsible_ai_checklist.md` satırı güncellendi + 10 yeni test. (6) `change_control_sop.md`'ye "Commit/Push Authorization" bölümü (önceden yalnız repo-dışı Master Protokol'de vardı) — generator fonksiyonuna gömüldü ki ladder regen'i silmesin. (7)+(8) `repo_directory_contract.md` ↔ `folder_structure_policy.md` çapraz-referansı — yine generator fonksiyonlarına gömüldü. (9) `docs_inventory.py`'nin duplicate-markdown taraması artık `.lint-venv`/`.pytest_cache` gürültüsünü hariç tutuyor. (10) dataset-health `_TR` parite düzeltmesi yukarıdaki ölü-dataset maddesine dahil. **Test sayısı:** bu pass'e giren taban zaten `511 passed, 5 skipped`'ti (DECISIONS.md'de kendi girdisi olmayan `20eb14c`/`484ec86`/`e7c04d3`/`c4ee588` commit'leriyle oluştu); bu pass `tests/test_secret_scan.py`'ye 4, `tests/test_sdk_api.py`'ye 9 yeni test fonksiyonu ekliyor (sonuncunun 3'ü parametrize, yani tek tek daha çok vaka olarak çalışıyor) — tüm düzenlemelerden hemen sonra, ladder'dan önce yapılan lokal tam-suite koşusu `532 passed, 5 skipped` gösterdi; nihai/otoriter sayı ladder'ın kendi koşusunda, bu girdiyi indiren commit'te. Teacher/tokenizer/prompt politikası, MoE 8e2, BitNet çekirdeği, Liquid matematiği dokunulmadı. Dataset politikası dokunuldu (Stage-5 kaynak-listesi değişikliği) — bilerek, dar kapsamlı, ve `AGENTS.md`'nin "dataset politikasını sessizce değiştirme" kuralının tam tersi: canlı-doğrulandı, yeniden-üretilmiş inventory ile test edildi, inmeden önce burada ve `BACKLOG.md`/`dataset_health.md`'de belgelendi. **Kalan tek kapı aynı: sahip olunan compute → 45K → checkpoint → checkpoint-bağlı eval.**

### Param-sayısı tahmincisi düzeltmesi + tam kök-doküman çapraz-tutarlılık pass'i (2026-07-27)

**Karar: her iki analitik param tahmincisindeki shared-expert/moe_intermediate eksik-sayımını düzelt, sonra tam bir kök-`*.md` denetiminin ortaya çıkardığı her gerçek çapraz-dosya çelişkisini kapat — hepsi yalnız doküman, eğitim-matematiğine dokunulmadı.**

- **Bug (düzeltildi, commit `198e06c`):** `scripts/scaling_audit_math.py::estimate_params()` ve `config/config.py::_estimate_total_params()` ikisi de (a) MoE expert'lerini gerçek, daha büyük `moe_intermediate` yerine dense `intermediate_size`'a göre boyutlandırıyordu, (b) `layers/moe.py`'nin her zaman aktif shared expert'ini tamamen atlıyordu. Aktif parametreler ~%44, toplam parametreler ~%8 eksik sayılıyordu. İkisi de düzeltildi; `scaling_audit_math.py` artık `ARCHITECTURE.md`'nin bağımsız kaynaklı ~1.86B aktif-param rakamıyla %1'in altında bir farkla eşleşiyor. 4 yeni test (`622 → 626 passed, 5 skipped`).
- **Kendi-teşhis edilen governance kaçırması:** o düzeltme commit'i sıfır `BACKLOG.md`/`DECISIONS.md`/`CHANGELOG.md` girdisiyle indi — tam da bir önceki commit'in (`f8890af`) bu tam kaçırma sınıfını önlemek için CHANGELOG-drift-önleme notunu eklediği andan hemen sonra. Bu girdi + eşleşen aynı-günlü `BACKLOG.md`/`CHANGELOG.md`/`CHANGELOG_TR.md` girdileriyle kapatıldı.
- **Düzeltmeden önce her biri canlı doğrulanmış altı doküman-bulgusu daha (doğrulanmamış hiçbir varsayım taşınmadı):**
  1. `CHANGELOG.md`/`CHANGELOG_TR.md` — "Pass 7 (2026-06-13)", 2026-03-13/2026-02-08 etiketli girdilerden sonraya yanlış dosyalanmıştı, dosyanın kendi ters-kronolojik sırasını bozuyordu. Doğru yerine taşındı, iki dilde.
  2. `ENV_VARS.md` — tam bir ay bayat (son içerik güncellemesi 2026-06-28), 2026-07-08→07-25 kararlılık-pass'inin tüm env-var yüzeyi eksikti — eksik değişkenlerden birkaçı `BACKLOG.md`, `TRAINING_PLAN.md` ve `START_HERE.md`'nin kendisinde açıkça anılıyor olsa bile. Repodaki her `os.environ.get`/`os.getenv` çağrısına karşı canlı `grep` ile senkronlandı (kısmi/varsayılan bir liste değil).
  3. `V2_BACKLOG_SEED.md` Track F — 3 madde (`liquid_warmup_steps` env override, z-loss çift-çarpım, `mark_weights_updated()` sorusu) bu dosyanın kendi 2026-07-12 girdilerine göre zaten çözülmüş/incelenmiş olduğu halde hâlâ açık gösteriliyordu. Çapraz-referanslarla çözülmüş işaretlendi.
  4. `CHESS_5080_POC_INTERNAL.md` (EN) — kendi TR ikizi zaten EN dosyayı Share Export davranışında bayat diye işaretleyen açık bir satır-içi not taşıyordu. Dokümanlarda oturan, kendi-kendine-itiraf-edilmiş-ama-düzeltilmemiş bir boşluk bırakmak yerine paritye getirildi.
  5. `TECHNICAL_REPORT.md`/`_TR` — başlık tarihi 2026-06-18'de donmuştu, gövde zaten 2026-07-19 tarihli bir revizyon taşıyordu. Sessiz geri-tarihleme yerine açık bir son-revizyon notu eklendi (bu reponun sürecinde başka yerlerde git-history korumasında ve Documents-dışı Master Protokol addenda'larında kullanılan append-only düzeltme deseniyle aynı disiplin).
  6. Doküman-navigasyon parçalanması — `AGENTS.md` (kendi source-of-truth sırasında kendini 1. sıraya koyan dosya) hiçbir başka kök dokümandan gelen link almıyordu (`grep` ile doğrulandı); `README.md`/`README_TR.md` de hiçbir zaman `START_HERE.md`/`README_SUMMARY.md`'ye (dış-reviewer onboarding dokümanları) link vermiyordu. İkisi de `README.md`'nin/`README_TR.md`'nin Canonical surfaces listesine eklenerek düzeltildi — yeni bir hiyerarşi icat ederek değil, zaten var olanı linkleyerek.
- **İşaretlenip incelenen ve doğru şekilde değiştirilmeyen bir madde:** `TROUBLESHOOTING.md`'nin accelerate-config uyumsuzluğu için `run.sh`'i (zero_touch_start.sh değil) tekrar çalıştırma tavsiyesinin bayat olabileceğinden şüphelenildi, ama `grep` `TITAN_FORCE_ACCELERATE_RECONF`'un yalnız `run.sh` tarafından okunduğunu doğruladı — mevcut tavsiye doğru.
- **Sınıflandırma:** Master Protokol'ün kendi değişiklik-sınıfı taksonomisine göre Sınıf A (doküman/truth-sync); gereken minimum `bash scripts/verify_all.sh`'tır ve yeniden koşulup yeşil alındı. Bu pass hiçbir readiness, claim-boundary veya eğitim-matematiği yüzeyine dokunmadı.

## Bağımsız denetim kapanış pass'i (2026-07-29/30) — iki ölü script silindi, üç önceki erteleme tersine çevrildi

Repo'ya sıfır erişimle yapılan bağımsız statik denetim (rapor:
`~/Documents/mertformer_titan_bagimsiz_denetim_2026-07-27.md`, 6.5/10, 63 bulgu) canlı koda
karşı yeniden doğrulanıp dalga dalga kapatıldı. Kayda değer kararlar:

- **`scripts/md_build30_sweep.py` SİLİNDİ.** `REPLACEMENTS` tablosundaki her çift `old == new`
  olduğu için her `.replace()` çağrısı no-op'tu — script hangi argümanla çağrılsa hiçbir dosyayı
  değiştiremezdi ve kendi docstring'i bunu söylüyordu ("fiilen NO-OP'tur"). Hiçbir launcher,
  ladder veya test referans vermiyordu. `scripts/repo_hygiene_guard.py`'nin
  `_FOSSIL_ALLOW_SUBSTR` listesindeki girdisi de birlikte kaldırıldı (o izin listesi dosyayı
  yalnız build-fossil regex'inden muaf tutuyordu, başka hiçbir şey ona bağlı değildi).
  `docs/PROJECT_STRUCTURE.md`'den çıkarıldı; `reports/release_manifest.json` ladder içindeki
  `scripts/sync_manifest.py` ile yeniden üretiliyor.
- **`scripts/titan_onnx_stress_test.py` SİLİNDİ.** Ölü *ve* semantik olarak yanlış:
  `tiktoken.get_encoding("cl100k_base")` ile tokenize ediyordu — yani OpenAI GPT-4
  tokenizer'ıyla, projenin Llama-3 tokenizer'ıyla değil; ürettiği id'ler modelin export
  edildiği sözlükle eşleşmezdi. `tiktoken` ayrıca `requirements.txt`'te yok (bilinçli: ihtiyaç
  düştü ve yeniden EKLENMİYOR), yani script import bile olamazdı. Üstelik cwd'ye göreli
  `titan_mobile.onnx` yolunu sabit gömüyordu ve `scripts/` altındaki bir dosyayı test gibi
  gösteren bir `pytest.skip` shim'i taşıyordu. `docs/PROJECT_STRUCTURE.md`,
  `scripts/README.md` ve `scripts/README_TR.md`'den çıkarıldı.
- **Neden silmek, korumak değil:** ikisi de git history'de duruyor, hiçbir şey kaybolmadı. Altı
  ay sonra "bu script nereye gitti" sorusuna anında cevap veren bir kaydın maliyeti sıfıra
  yakın; public bir repo ise no-op bir sweeper ile yanlış tokenizer'a bağlı bir stress tester
  taşımamalı. `archive/**` snapshot'ları ve `reports/scoped_external_intake_matrix.*` hâlâ iki
  yolu anıyor ve bilinçli olarak DÜZENLENMEDİ — ilki tarihsel snapshot, ikincisi bu repo
  dışındaki harici kopyaların denetim kaydı.

- **Bu pass'te üç önceki erteleme tersine çevrildi — bu bilinçli, drift değil:**
  - 2026-06-13 Pass 7 tasfiyeleri *"17 MoE capacity-loop vektörizasyon — yalnız GPU-perf
    (eğitim kutusunda)"* maddesini şimdi-değil diye kapatmıştı. Artık YAPILDI
    (`layers/moe.py`), çünkü değişiklik GPU dışında kanıtlanabilir çıktı:
    `scripts/cfc_moe_tolerance_check.py` `max_diff_moe=0.000000` veriyor, yani bit-birebir aynı
    loss'lar; ayrıca yeni `tests/test_moe_capacity_drop_order.py` drop-sırası semantiğini
    doğrudan bir `nonzero()` referansına karşı kilitliyor. *Throughput* kazancı ise
    **ÖLÇÜLMEDİ** — burada CUDA aygıtı yok, dolayısıyla sync-sayısı argümanı statik analizdir,
    ölçüm değildir.
  - Aynı Pass 7 girdisinin "capacity formülü inline kalabilir" gerekçesi geçersiz kıldı: formül
    artık modül seviyesindeki `layers.moe.moe_capacity()` helper'ı, çünkü
    `tests/test_moe_capacity.py` ve `tests/test_property_moe_capacity.py` formülü kendi
    içlerinde kopyalıyordu ve tüm bu yeniden yazım boyunca yeşil kalırlardı. İki docstring de
    bu çıkarımı açıkça davet etmişti.
  - Pass 7'nin *"24/25/31/38 test backlog (… packing resume-seam) — additive saf-CPU kapsam,
    risksiz, ertelendi"* maddesi kısmen kapandı: packing resume dikişi artık kapsamda ve orada
    yalnız kapsam eksik değildi — gerçek bir bug saklıydı (Y-5, bkz. BACKLOG).
- **Hâlâ ertelenmiş, dokunulmamış ve bu pass ile kapanmış SAYILMAMASI gerekenler:** Pass 7
  madde 14 (Liquid impl default `TITAN_LIQUID_TRAIN_IMPL`), madde 20 (teacher pad-mask
  GPU-perf), madde 34 (`train/train.py` split), madde 68 (repo-geneli mypy). Özellikle madde 34
  bu pass'te bilinçli dokunulmaz olarak yeniden teyit edildi: 1100 satırlık `train()`'in ücretli
  bir 45K koşusu öncesinde neden yapısal olarak bozulmadan bırakıldığı BACKLOG'da.
- **`train/train.py::train()` REFACTOR EDİLMEDİ — açık sahip kararı (2026-07-29).** Kazanç
  kozmetik; kayıp senaryosu ise buradaki hiçbir CPU-only testin yakalayamayacağı ve ancak
  binlerce dolarlık 45K compute harcandıktan sonra yüzeye çıkacak bir yan-etki-sırası kayması.
  Yalnız içindeki izole bulgulara dokunuldu; fonksiyonun yapısı ve ifade sırası değişmedi. Bu
  pass'in kod-kalitesi tavanının 10/10 değil ~8.5-9/10 olmasının birinci sebebi budur; bir
  eksiklik değil, bilinçli bir risk kararıdır. Tam ayrıntı: `DECISIONS.md`.

## Lisans: Proprietary & Confidential → Apache 2.0 (2026-07-30)

**Yukarıdaki 2026-06-17 tarihli "Lisans çelişkisi → Proprietary çözüldü" kararını GEÇERSİZ
KILAR.**

O karar bir çelişkiyi (README footer'ları `Apache-2.0` derken `LICENSE` Proprietary
diyordu) her şeyi Proprietary'ye çekerek çözmüş ve şöyle bitmişti: *"hukuki tavsiye
değildir; gerçekten açık-kaynak yapma kararı ayrı, bilinçli bir karardır."* İşte bu, o ayrı
ve bilinçli karardır; sahibi tarafından 2026-07-29'da, depoyu public yayına hazırlama
kapsamında verilmiştir.

**Değişenler:**
- `LICENSE` — tam Apache License 2.0 metni, `Copyright 2026 Mert Yunlu`.
- `LICENSE_TR` — bilinçli olarak çeviri DEĞİL. ASF resmî bir Türkçe metin yayımlamıyor ve
  Apache 2.0 aynen çoğaltılmak zorunda; bu yüzden dosya artık tek yetkili metnin İngilizce
  `LICENSE` olduğunu belirten ve açıkça bağlayıcı olmayan bir özet sunan Türkçe
  bilgilendirme belgesidir.
- `README.md` / `README_TR.md` — Lisans bölümü ve sahibinin istediği iki yeni bölüm: "İş
  Birliği ve Ticari Fırsatlar" ile "Katkılar ve PR Kuralları". İletişim bilgisi yalnızca
  GitHub profilidir; hiçbir e-posta veya telefon uydurulmamıştır.
- `CITATION.cff` — `license: "Proprietary"` → `Apache-2.0`.
- `CONTRIBUTING.md` / `CONTRIBUTING_TR.md` — "dış katkılara açık değildir" → Apache 2.0
  5. madde uyarınca açık; PR kapısı olarak `bash scripts/verify_all.sh` adlandırıldı.
- **50 Python kaynak başlığı** — `Proprietary - All Rights Reserved.` →
  `Licensed under the Apache License, Version 2.0 (see LICENSE).` Bu kozmetik değildi: her
  modülde dosya-başı proprietary bildirimi, depo public olduğu anda Apache 2.0 `LICENSE`
  ile doğrudan çelişir ve okuyucu baktığı dosyanın başlığına güvenir.
- `pyproject.toml` değişmedi: zaten `license = {file = "LICENSE"}` beyan ediyordu, dosyayı
  değiştirmek yeterliydi.

**Bilinçli olarak DEĞİŞMEYENLER:**
- `NOTICE`, `MODEL_LICENSE.md`, `MODEL_LICENSE_TR.md` — Meta'nın Llama 3.3 yükümlülükleri
  bu kararın dokunmadığı AYRI ve bağımsız bir katmandır. "Built with Llama" atfı aynen
  kalır. Apache 2.0 bu deponun KODUNU kapsar; model lisansı katmanı etkilenmez ve model
  ağırlıkları duruşu proprietary kalır (zaten henüz eğitilmiş ağırlık yok).
- `datasets/INTERNAL_POLICY*.md`, `datasets/LICENSES*.md` — dahili veri seti politikası
  üçüncü bir bağımsız katmandır ve kod lisansıyla ilgisizdir.
- Her tarihli tarihsel kayıt (`CHANGELOG`, önceki `DECISIONS` girdileri) özgün ifadesini
  korur. Bu girdi geçersiz kılar, yeniden yazmaz.

**Hukuki tavsiye değildir.** `NOTICE`'ta EXTERNAL-PENDING legal olarak işaretli Llama
isimlendirme sorusu açık kalır ve bu değişiklikten etkilenmez. Tam ayrıntı: `DECISIONS.md`.

## Satranç onefile yeniden yazımı (`ChessFormerAI/chessformer`) ayrı paket olarak kalıyor, alt-modül ya da geri-taşıma değil (2026-08-06)

**Bağlam.** `BACKLOG.md`'nin "Chess onefile — independent review found 3 real drift/bugs
(2026-08-02)" maddesi, satranç hattının ayrı, izole bir yeniden yazımının
(`ChessFormerAI/chessformer`, bu reponun `layers/`'ının salt-okunur bir kopyasına karşı
geliştirildi) `scripts/chess_5080_onefile.py`'de dört gerçek bug'ı düzelttiğini belgeledi
(is_causal yanlış-etiketleme, Liquid clamp eğitim/değerlendirme uyuşmazlığı, MoE dispatch
kapasite drift'i, ve bu reponun kendi `layers/mertformer_block.py`'sinde de yaşayan bir
Liquid-state-atma bug'ı), ama geri-taşınma-ya-da-deprecated-işaretleme kararını yeniden
yazımın gerçek donanım sonuçları elde etmesine kadar erteledi. Artık elde etti —
`run_20260802_194441`, gerçek RTX 5070 eğitimi + retroaktif değerlendirme, gerçek ölçülmüş
Elo (1509) ve puzzle accuracy (%45.78). Tam run detayı için `BACKLOG_TR.md`'nin eşleşen
2026-08-06 maddesine, ham sayılar için `evidence/2026-08-02-chess-searchless-5070/`'e bakın.

**Değerlendirilen alternatifler.**
- **D1-D4 düzeltmelerini `scripts/chess_5080_onefile.py`'ye yerinde geri taşı.** Reddedildi:
  o dosyanın kendi governance apparatus'u (`docs/CHESS_ONEFILE_MASTER_TRUTH.md`,
  `chess_evidence_contract.md`, `chess_release_contract.md`, `chess_onefile_glossary.md` —
  `reports/`, `runbooks/`, `checklists/`, `releases/`, `evidence/` genelinde kabaca 80
  tracked stub/contract/rapor dosya adı) tam olarak o dosyanın kimliği, öğretme-corpus'u ve
  curated-position-suite genişletmesi, ve Windows tek-tık delivery hattı
  (`CHESS_5080_POC_INTERNAL.md`) etrafında kurulu. Bunların hiçbiri `chessformer`'da yok ve
  portlamak, zaten kendi ayaklarının üzerinde çalışan bir paket için o apparatus'un çoğunu
  yeniden inşa etmek demek olurdu — ikisini ayrı tutmaya kıyasla ölçülen bir fayda olmadan
  yüksek efor.
- **`chessformer`'ı bu repoya tracked bir alt-dizin ya da git submodule olarak birleştir.**
  Şimdilik reddedildi: `chessformer`'ın kendi bağımlılık yüzeyi, kendi 123-testlik suite'i,
  kendi GUI'si (`chessformer/gui/`), ve bilerek bu reponun chess-onefile stub apparatus'undan
  çok daha yalın kendi rapor şeması var. Birleştirmek ya onu o apparatus'u benimsemeye
  zorlardı (gerçek iş, net fayda yok) ya da tek bir repo içinde ikinci, tutarsız bir
  chess-evidence konvansiyonu yaratırdı. `ChessFormerAI` bağımsız bir proje olarak kalıyor;
  bu repo onun sonuçlarını kanıt olarak referans alıyor (`evidence/2026-08-02-chess-
  searchless-5070/`), Nutrition5k yan-deneyinde zaten kullanılan aynı örüntü.
- **Hiçbir şeyi birleştirmeden `scripts/chess_5080_onefile.py`'yi "superseded" işaretle.**
  Reddedildi: `chessformer`, `chess_5080_onefile.py`'nin yaptığı her şeyin katı bir üst-kümesi
  olarak doğrulanmadı (Türkçe öğretme corpus'u, curated opening/tactical/endgame/blunder-
  correction suite'i, Windows tek-tık EXE delivery'si — bunların hiçbiri `chessformer`'da
  yok). Henüz yapabildiği her şeyi yapamayan bir paket tarafından bir dosyayı "superseded"
  ilan etmek, tam olarak bu reponun kendi disiplininin önlemeye çalıştığı türden bir
  claim-sınırı ihlali olurdu.

**Karar.** `ChessFormerAI/chessformer` ayrı, bağımsız bir paket olarak kalıyor.
`scripts/chess_5080_onefile.py` değişmedi, deprecated değil, superseded işaretlenmedi — D1-D4
bulguları hiç koşulmamış bir kod yolunda belgeli, bilinen, canlı bug'lar olarak kalıyor (kendisi
de hâlâ hiç çalıştırılmadı — bkz. `CHESS_5080_POC_INTERNAL.md`). 123 `chessformer` testi kendi
paketinde kalıyor. Bu işin bu repodaki tek izi evidence klasörü.

**Ödünleşimler.** `chess_5080_onefile.py`'deki bilinen D1-D4 bug'ları o dosyada özellikle
düzeltilmemiş kalıyor; onu doğrudan (chessformer yerine) koşturan biri hâlâ bunlara çarpar.
Kabul edildi çünkü dosya hiç gerçekten koşulmadı ve gerçek bir onefile-hattı koşusu
istenirse yeniden yazım artık doğrulanmış yol.

**Geri-alma etkisi.** Yok — bu karar sonucunda bu repoda hiçbir kod değişmedi, yalnızca
dokümantasyon ve evidence klasörü.

**Uyumluluk etkisi.** Yok.

## `chessformer` checkpoint lisansı: proprietary model-ağırlığı duruşuna Apache 2.0 istisnası (2026-08-06)

**Bağlam.** `MODEL_LICENSE.md` (yukarıdaki 2026-07-30 relicensing girişine bakın) repo kodu
Apache 2.0'a geçerken model-ağırlığı duruşunu bilinçli olarak proprietary bıraktı — o an
"zaten hiç eğitilmiş ağırlık yoktu." Bu bugün değişti: `chessformer` (yukarıdaki bağımsız
yan-proje) Hugging Face'te gerçek, yayınlanmış bir checkpoint'e sahip
(`Mert21779033/mertformer-chess-searchless`), ilk yüklemede yanlışlıkla `MIT` etiketiyle
(aynı gün fark edilip düzeltildi — bkz. `BACKLOG.md`'nin eşleşen girişi) — bu da bu girişin
çözdüğü asıl soruyu ortaya çıkardı: reponun proprietary model-ağırlığı politikası
`chessformer`'ı da mı kapsıyor, yoksa yalnızca kanonik Titan modelini mi?

**Karar.** Her iki projenin de tek sahibi olan Mert Yünlü, `chessformer` checkpoint'lerini
proprietary duruştan açıkça istisna tutuyor: bu reponun kodunun kullandığı aynı Apache
License 2.0 ile lisanslanıyorlar. `MODEL_LICENSE.md`/`MODEL_LICENSE_TR.md`, açık ve sınırlı
bir istisna bölümüyle güncellendi. Kanonik MertFormer Titan modelinin proprietary duruşu
değişmedi — bu, 2026-07-30 relicensing kararını yeniden açmıyor, o kararın henüz chess
checkpoint'i yokken açık bıraktığı bir soruyu netleştiriyor.

**Neden Apache 2.0, ayrı bir model lisansı değil.** Basitlik: `chessformer` zaten her yerde
(README, evidence paketi, HF model card) bu reponun mimari ailesini ve geliştirme soyunu
paylaştığı belgelenmiş durumda; ikinci, özel bir model lisansı, birlikte gönderildiği kod
lisansına zaten uymanın ötesinde gerçek bir fayda getirmeden ekstra bir hukuki yüzey ekler.

**Sonuç olarak değişen:** Hugging Face reposu (bu kararı beklerken aynı gün private yapılmıştı)
tekrar public; model card'ının `license` alanı ve Lisans bölümü artık ilk yüklemedeki yanlış
`MIT` etiketi yerine, `MODEL_LICENSE.md`'nin istisna maddesine link veren Apache 2.0 diyor.

**Geri-alma etkisi.** Düşük — bu, zaten yayınlanmış, bağımsız bir yan-proje checkpoint'i
üzerinde bir lisans bahşi. Geri almak, zaten indirilmiş kopyaları yayından kaldırmaz.

**Uyumluluk etkisi.** Yok.
