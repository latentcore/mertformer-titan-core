# Mimari Kararlar

- **Config disiplini**: `config.py` çalışma zamanı varsayılanı; isteğe bağlı YAML overlay desteği.
- **Swarm mimarisi**: Hedef mimari olarak dokümante; core training'e zorunlu değil.
- **Multimodal**: Metin çekirdeği kanıtlanana kadar ertelendi.
- **Kanonik KD hattı**: Dış hat adı `offline_clean` korunur; 45K closure koşusu için katı precompute-KD yolu olarak ele alınır.
- **Öğretmen yüzeyi**: Teacher kanonik yolda `meta-llama/Llama-3.3-70B-Instruct`'e pinlenir; sessizce takas edilmez.
- **Fallback sınırı**: Kanonik `offline_clean` eğitim yolundan teacher'sız fallback kaldırıldı. Logit'ler eksikse, sessizce düşürmek yerine tam blocker ile fail edilir.
- **Prompt yüzeyi**: `prompts/system_v1.txt` post-training davranış kanıtı genişletmeyi haklı çıkarana kadar tek kanonik sistem-prompt yüzeyi kalır.
- **Artefakt stratejisi**: Mevcut release/repo zip'leri korunur; gerçek koşu erişimi için ayrı bir training-outputs bundle zip eklenir (`artifacts/mertformer_training_outputs_bundle.zip` + SHA256 + manifestler).
- **Denetlenmiş kararlar (2026-06-12 sıfır-çelişki kapanışı) — belgelenir, "yeniden düzeltilmez"**: Çok-ajanlı denetim bunları canlı kodla doğruladı; kasıtlıdır, bug değildir. P5/P8 README perf tablosu (~1.2s/step, ~107 tok/s, 11.000 tok/s toplam) açıkça "Projected/Est./Not Measured/architecture simulation" etiketli — claim-disiplinine uyumlu projeksiyonlar. P13 `utils/safety.py kill_if_non_finite` `utils/__init__.py __all__` ile export edilen public utility (orphan değil). P18 `config.auto_configure_batch_size` nvidia-smi: naif LOCAL_RANK guard'ı eklenmez (rank-0-only guard per-rank micro-batch'i desync ederdi); Method-A `torch.cuda.get_device_properties` gerçek GPU node'larında nvidia-smi'den zaten kaçınır, print'ler env-gated (`TITAN_CONFIG_VERBOSE`). P11 gated Meta `Llama-3.3-70B-Instruct` teacher yüklemesinde `trust_remote_code=True` kasıtlı (güvenilir birinci-taraf repo); production dataset yüklemeleri bunu etkinleştirmez. **Ölçülen param sayısı `3,672,982,022` (~3.67B)** kanonik ölçülen runtime toplamıdır; explicit `moe_intermediate=8192` (config/config.py) bunu tutar; `economics/flops_estimator.py DEFAULT_PARAMS=2.64e9` ayrı design-target olarak korunur (bkz. reports/FACTS.json). D3 Stage-1 kod kaynağı `bigcode/the-stack-v2`→`bigcode/the-stack-dedup` (v2 satırlarında `content` boş, `blob_id`/`src_encoding` taşır → ~%28 kod corpus'unu sessizce açlığa düşürüyordu; the-stack-dedup gerçek `content` sunar; preflight smoke-probe artık boş-alan kaynakta `SOURCE_FIELD_EMPTY` ile gürültülü fail eder; `datasets/hashes.json` revision/sha256 eğitim makinesinde re-pin edilmeli). Llama isimlendirme: tam uyum EXTERNAL-PENDING legal (bkz. NOTICE + reports/teacher_output_license_assessment.md). Tam ayrıntı: `DECISIONS.md`.
- **Mac'te yapılabilir backlog tasfiyeleri (2026-06-13, Pass 7) — gerekçeli "şimdi-değiştirilmez" olarak kapatıldı**: 33 topk bf16 reddedildi (bf16 top-k routing hassasiyetini düşürür + `scripts/cfc_moe_tolerance_check.py` gate'ini kırar; layers/moe.py fp32 cast kasıtlı). 14 Liquid impl default `TITAN_LIQUID_TRAIN_IMPL` ile zaten seçilebilir; DEFAULT'u çevirmek #1 MFU kaldıracı, pilot timing verisine bağlı. 17 MoE capacity-loop vektörizasyon yalnız GPU-perf (eğitim kutusunda). 20 teacher pad-mask GPU-perf (teacher-precompute makinesinde). 36 legacy logit-realign yolu kasıtlı env-gated escape hatch (`TITAN_ALLOW_LEGACY_LOGIT_REALIGN`, default off). 48 sharded checkpoint (accelerator.save_state) gerçek multi-GPU ister (eğitim kutusu). **23 MLA→GQA rename DONE (2026-06-16)** kanonik yolda; importer/test/grep/doc senklandı; kasıtlı korunan: `layers/mla.py` dosya adı + `TITAN_MLA_KV_PACK` env knob + 5 vendored onefile delivery kopyasındaki iç `MLA` sembolü (frozen bundle'lar). 34 train.py split / 68 whole-repo mypy kalıcı bırakıldı (yüksek blast radius, ~sıfır fonksiyonel kazanç). 40 drift telemetry deferred (`lifelong_safety.safety_metrics() last_drift` zaten var, katman default off; salt-additive). 24/25/31/38 test backlog (RoPE position, block-order schema, SIGTERM handler, packing resume-seam) additive pure-CPU coverage, risksiz deferred. Tam ayrıntı: `DECISIONS.md`.
- **MLA → GQA yeniden adlandırma (2026-06-16, DONE)**: Attention sınıfı `layers/mla.py` içinde artık `GQA` (her zaman grouped-query'di; latent-MLA bottleneck değildi). Tam ayrıntı için `DECISIONS.md`.
- **Doğrulanmış bulgular (2026-06-16) — belgelendi, davranış değişikliği 45K sonrasına ertelendi**: z-loss efektif ağırlığı `2e-6` (çift çarpım: `1e-4 * 0.02`), Liquid `dt=1.0` sabit (gated RNN), `epoch_mode` varsayılan `False`, sequential MoE dispatch + `.item()` (yalnız GPU-perf), `mark_weights_updated()` uncalled (cache-invalidation kancası — silinmez). Hiçbiri bu turda değiştirilmedi (45K koşusunu confound etmemek için). Mekanizma + post-45K aksiyon: `DECISIONS.md`.
- **Repo küratörlüğü — ticari/strateji katmanı (2026-06-16)**: Ana repo teknik/kanıt yüzeyidir (PRIVATE kalır; tek public yüzey README türevli Gist). İş materyali reviewer-facing ağaçtan çıkarıldı, **hiçbir dosya kalıcı silinmedi** (hepsi `/private/` altına taşındı ya da untrack edilip yerelde yeniden üretiliyor). Taşınanlar: anthropic iç-strateji notları → `private/anthropic_internal/`; `PITCH(±TR)` + el yazımı `sales_funnel_90d(±TR)` / `ip_licensing_split(±TR)` / `founders_hub_application(±TR)` → `private/commercial/` (README/README_TR index link'leri kaldırıldı). Untrack: `reports/investor_deck.pptx(±TR)` (`build_investor_deck.py` yerelde üretir). Yerinde bırakılan (closure-ladder tarafından **üretilen**, sıkı bağlı — belgelenir, rewire EDİLMEZ): `gtm_master_plan.md`, `commercial_handover_pack.md`, `legal_ip_pack.md`, `reports/commercial_handover/` — bunları çıkarmak yeşil ladder builder'larını rewire etmek (davranış değişikliği) gerektirir; safe-fix/belgele politikası gereği internal closure-governance artefaktı olarak kalır. Aktif ticari işin kanonik yeri ayrı private **dealroom** reposudur. Tam ayrıntı: `DECISIONS.md`.
- **Repo küratörlüğü — ikinci geçiş (2026-06-17): tek-persona + kanonik scaffold**: Kök kanonik scaffold eklendi (`STATUS/TRUTH_MATRIX/BACKLOG/GOVERNANCE/REPRODUCE.md` + `_TR`) → reviewer için tek giriş. `archive/` kuruldu; `docs/tr/` bilinçli kurulmadı (EN↔TR parite gereği TR ikizleri yan yana kalır). README 178KB→~4KB kesildi (tam hâli `archive/`'a; `sync_manifest.py` artık tam ağaç yerine `docs/PROJECT_STRUCTURE.md`'ye pointer basıyor). Anthropic başvuru paketi → `private/anthropic_internal/`; kalan tüm ticaret/GTM/outreach → `private/commercial/` (el yazımı) ya da gitignore (generated). Closure cluster: redundant/superseded generated raporlar gitignore (yerelde üretilir), el-yazımı superseded olanlar → `archive/closure/`; kalan generated closure detayı kök scaffold'un işaret ettiği **ikincil generated detay** olarak korundu (tümünü gitignore'lamak yeşil ladder'ı churn ederdi). Boş şablonlar → `archive/templates/`; ASCII-Türkçe `codex_deep_audit_DE` → `archive/audits/`. Çelişkiler kaynakta/generator'da düzeltildi (test-sayısı 370, 3.70B→3.67B, the-stack-dedup, closure_57 satır 53 out-of-scope, `/Users/` path sızıntıları + guard kör-noktası). Tam ayrıntı: `DECISIONS.md`.
- **Eğitim ölçeği — ara koşu yok, doğrudan 45K (2026-06-17)**: Kanonik koşudan önce ya da sonra **ara-ölçek model koşusu (250M / 500M vb.) YOK** — ne TÜBİTAK ne başka bir milestone için. Sıradaki ve tek eğitim işi **kanonik 3.67B 45K koşusudur (K1)**. **Neden:** daha küçük bir *model* kendi eğitim dinamiklerine sahiptir (MoE 8-top2 ve Liquid/CfC mixer küçük ölçekte farklı davranır — toy ablation Liquid etkisinin gürültüde kaybolduğunu zaten gösterdi), 45K davranışını öngörmez, "kanıt" yüzeyi olarak compute/zaman değmez. **Bu karara DAHİL DEĞİL (korunur):** 45K öncesi ücretsiz smoke bir alt-ölçek model DEĞİLDİR — `TITAN_MAX_STEPS=2` ile *kanonik 3.67B mimarinin kendisinin* birkaç adım koşulması (davranış-param değişikliği yok) + K4 checkpoint zinciri (save→restore→resume). Bu yalnız "kod import olup adım atıyor mu + artefaktlar geri dönüyor mu" doğrulamasıdır — pahalı 45K compute'unu kırık bir import/checkpoint zincirine yakmamak için ucuz sigorta (son H200 koşusu artifact retrieval'da ölmüştü). "250M deneme" değil. Tam ayrıntı: `DECISIONS.md`.
- **45K-öncesi de-risk pass'i — operasyonel sertleştirme uygulandı, eğitim-davranışı yine ertelendi (2026-06-17)**: İki bağımsız kıdemli review (doküman + 6-turlu 45K kod denetimi) canlı doğrulandı; kod denetiminin hükmü: kanonik tek-geçiş yolunda garantili çökme yok, asıl de-risk 2-GPU smoke + koşunun kendisi. **Uygulandı (eğitim matematiğini değiştirmez → confound etmez):** grad_clip tek-yönlü ratchet kaldırıldı (transient; latent bug), atomik checkpoint (`os.replace`), resume `weights_only=False`, MoE runtime buffer'ları `persistent=False`, `MoE.forward` içi `all_reduce` kaldırıldı (DDP/checkpoint-recompute NCCL-deadlock riski + per-step host sync), telemetri throttle (log_interval 1→10 + host snapshot `TITAN_TELEMETRY_INTERVAL=100`). **Token bütçesi:** 23.6B kanonik + 1024 explicit opt-in (~188B, 8×; LR/curriculum step-based, ölçeklenmez) + `TITAN_STRICT_TOKEN_BUDGET` hard-fail guard. **Yine document-only (kod değişmedi):** z-loss 2e-6, dt=1.0, dropout 0.1 stack (undertrained rejim), early-stop patience=5 — koşu-config kararı. **Doküman (frontier-lab cila):** `TECHNICAL_REPORT(±TR)` klinikleştirildi (synaptic/wisdom/fluid-intelligence/Onyx-Storm/Emotional-Weighting register düştü; §10 "spekülatif, uygulanmadı, kapsam-dışı"), `ARCHITECTURE` Liquid 12-seed verdict'e senklandı, inert `orchestrator/` AGI runtime + flag-off cognitive katmanlar out-of-scope belgelendi (silinmedi), MoE-ara-boyut 8192 düzeltildi, closure_57 item 20 "kv-head sharing" olarak dedup. **Öğretmensiz smoke knob'u (davranış-nötr):** `config.py` `distill_alpha` artık env-override edilebilir (`TITAN_DISTILL_ALPHA`, default 0.8 — varsayılan davranış değişmedi); `TITAN_DISTILL_ALPHA=0` kanonik eğitim girişini 70B teacher'ı yüklemeden/indirmeden smoke koşmaya izin verir (train.py teacher'ı yalnız `distill_alpha>0` iken yükler; loss saf cross-entropy'ye düşer). REPRODUCE(.md/_TR)'de tek-komut öğretmensiz pre-45K smoke. **Lisans çelişkisi → Proprietary çözüldü (2026-06-17):** README/README_TR footer'ı `Apache-2.0` diyordu ama otoriter `LICENSE` dosyası **Proprietary & Confidential, tüm haklar saklı** (CONTRIBUTING dış katkıyı kapatıyor, CITATION.cff zaten `Proprietary`, pyproject `license={file="LICENSE"}`, repo private + ticari dealroom). README footer'ları tek hataydı → "Proprietary & Confidential — tüm haklar saklı; bkz. LICENSE" olarak düzeltildi. Bilinçle proprietary yönüne çekildi (diğer tüm metadata yüzeyiyle uyumlu + güvenli yön; başıboş `Apache-2.0` kazara açık-kaynak grant'i = geri-alınamaz). Llama yükümlülükleri değişmedi, `NOTICE`+`MODEL_LICENSE.md` ile açık. Hukuki tavsiye değil; gerçekten açık-kaynak yapma kararı ayrı, bilinçli bir karardır. **Dış review pass — 5 madde, canlı doğrulandı (2026-06-17):** ✅ DÜZELTİLDİ `pyproject.toml`'a eksik `mertformer_sdk.kernels.cpp` eklendi (`cpp/__init__.py` var). ✅ DÜZELTİLDİ (claim) `TECHNICAL_REPORT(±TR)` §3.1 "12x" → Hedef/tahmin, §7 SHA256 → "tasarım" (henüz 45K zinciri yok). ✅ Self-doc: `apply_github_policy.sh` artık rapora `note` yazıyor → `return_code:1` beklenen best-effort 403 (private/non-Pro, §0.8). ❌ BUG DEĞİL: `ci.yml` `checkout@v6`/`setup-python@v6` — bunlar VAR (2026 ortası released) ve reponun kendi CI'ı bugün yeşil `success`; v4/v5'e düşürmek çalışan pipeline'ı regress eder → dokunulmadı. ✅ Zaten yapıldı: README Apache→Proprietary önceki pass'te. **Llama naming (EXTERNAL-PENDING legal):** Llama-3.3 yalnız KD teacher + tokenizer olarak kullanılıyor (Llama weights yok); öğrenci adının "Llama" taşıması gerekip gerekmediği hukuki-yargı sorusu, net rename değil — NOTICE/MODEL_LICENSE'da açık, public model release öncesi IP/legal review'a işaretli. **Bulgu-listesi temizliği + whitepaper fast-forward senk (2026-06-17, Pass 4.5):** app/web Claude WHITE_PAPER'a (EN+TR) arXiv Section 8 ekleyip push etti (`eed0a4d`/`0e8f040`); local temiz fast-forward geride kaldığı için `git merge --ff-only` ile senklendi (divergence/çakışma yok). Ayrı "nihai bulgular listesi" canlı doğrulandı, **yalnız gerçek+güvenli** olanlar düzeltildi: **Doc (EN+TR)** WHITE_PAPER_TR §2 "Statik"→"Durumsuz"; CHANGELOG 2026-06-17 bölümü; IMPLEMENTATION_PLAN Founders-Hub (2026-05-31 yapıldı); TASK private/commercial repoint; README_CHECKLIST tazelendi; ablations/results+no_liquid 12-seed verdict'e senk (+0.50 pilot superseded); AGENTS/START_HERE gitignored `reports/final_*` annotasyonu. **Config/security** ruff→v0.15.5, base.yaml log_interval 1→10, Dockerfile constraints.txt, registry→Build30 V2, secret_scan+policy gho_/ghu_/ghs_/ghr_+PAT, pip_freeze chess. **BUG DEĞİL (bırakıldı):** moe_dispatch parallel/sequential çelişki yok; secret_scan↔policy regex aynıydı (tek gap gho_); CI @v6 yeşil. **Intentional (bırakıldı):** env.lock/cuda.lock template, warmup_steps self-documented dead field, ADR-0001 historical (immutable), dataset TBD disabled. **Pre-45K freeze:** post-45K backlog kod maddeleri + mimari gözlemler dokunulmadı. **Kullanıcı aksiyonu:** `gho_` token'ını GitHub'dan revoke et. **NİHAİ EKSİKLİKLER temizliği — doğrulanmış alt-küme (2026-06-18, Pass 4.6):** Paralel app-Claude'un tüm-gün derlediği ~207 maddelik "nihai eksiklik listesi" 3 Explore ajanıyla canlı doğrulandı; **yalnız gerçek+güvenli** olanlar düzeltildi (training-math'a dokunulmadı, pytest 370 değişmedi). ✅ Stale `BUILD 27`/`V27.0` prefix'leri 14 yorum/print/docstring'den kaldırıldı (kod-logic'i değişmedi). ✅ Reviewer-facing doc-truth: `CITATION.cff` (false `date-released` kaldırıldı, "Onyx Storm" düştü, affiliation eklendi); `LICENSE`(+`_TR`) "MertFormer AI Team"→"Mert Yünlü" + partner/investor overclaim yumuşatıldı + çelişen "confidential Swarm/LiquidRouter" isimlendirmesi çıkarıldı; `SECURITY.md`(+`_TR`) internal jargon temizlendi + gerçek vuln-reporting (GitHub Security Advisories); `CONTRIBUTING.md`(+`_TR`) `codex/`→`feature/`/`fix/` + dürüst AI-asistan disclosure (git-history `Co-Authored-By: Claude` endişesini history-rewrite olmadan kapatır); `requirements.txt` header → Build 30 V2. ✅ Observability: `distillation_manager.py` resume-state `except: pass` artık log basıyor (fallback aynı). ✅ Parite: TROUBLESHOOTING(±TR) NCCL/corruption/tokenizer senaryoları + 2 tiny TR-orphan'a EN twin. ❌ **BUG DEĞİL/FALSE:** "git tag push edilmedi" YANLIŞ (13 tag remote'da); moe_dispatch/CI@v6/secret↔policy önceden teyitli. **LEFT (quarantine/freeze/external):** inert orchestrator (core/self_audit), tüm pre-45K freeze maddeleri, post-45K/infra, deprecated/peripheral TR-orphan, EN-only kasıtlı docs. **Skip:** README badge (PRIVATE repo → canlı badge 404 + gist churn). Tam ayrıntı: `DECISIONS.md`.
- **Repo-geneli nihai denetim kapanışı (2026-06-27, Pass 5) — belgelenir, "yeniden düzeltilmez"**: Devasa bir çok-ajanlı denetim her tracked dosyayı okudu, bulguları yüzeye çıkardı, gerçek olanları canlı kodla doğruladı ve iki cerrahi, davranış-koruyan dalgada düzeltti (Wave A = T1/T2 yapısal+label+bug, 263 uygulandı / 131 dosya; Wave B = T0 mekanik). pytest boyunca 370 passed / 4 skipped; verify_all yeşil. Kararlar kasıtlıdır, geri-alınmaz: **`layers/mla.py` korundu (rename YOK), yalnız-label fix** — sınıf grouped-query attention uygular (GQA: `num_kv_heads`=8 < `num_heads`=16), latent-MLA değil; label/docstring'ler "GQA"ya düzeltildi (`scripts/sync_manifest.py` role-override → "grouped-query attention (GQA) implementation"); dosya adı/modül/state-dict key'leri kasıtlı rename EDİLMEDİ (rename `release_manifest.json` / `immutable_evidence_register.json` / onefile hardcoded SHA referansları + mirror-parite yüzeyini path+SHA sürekliliğinde kırardı; dosya adı bir identifier'dır, claim değil — düzeltilmesi gereken tek şey yanlış "latent attention" claim'iydi). **Liquid/CfC 45K yolunda kalır** — 12-seed ablation inconclusive (Δ küçük, p=0.305); kaldırmak da eşit-kanıtsız, mühürlü kararla korundu; ayrıca `layers/moe.py`'deki `Liquid`/`Fluid Dynamics` label'ları netleştirildi (causal depthwise Conv1d + BitLinear gate, continuous-time CfC cell DEĞİL; gerçek CfC `liquid.py`'de); isim/param path'leri mühürlü checkpoint/state_dict kontratına bağlı olduğu için korundu. **`orchestrator/` + spekülatif katmanlar (`qinn.py`, `world_model_head.py`) = inert / kapsam-dışı** — 45K yolunda feature-flag OFF (hiçbir training loss'a bağlı değil) olarak dürüstçe işaretlendi; içlerindeki gerçek bug'lar düzeltildi ama promote/remove EDİLMEDİ; `qinn.py.__version__` artık `mertformer_sdk.__version__`'dan beslenir (tek doğru kaynak); `world_model_head *_logits` alanları tanh-sıkıştırılmış state olarak belgelendi (logit DEĞİL), isimler test/mirror-parite geriye-uyum için korundu. **`mertformer_sdk/kernels/{cpp,metal,vulkan,npu}` = dürüst stub'lar** — `bitnet_cpu.cpp` düz `torch::matmul` olarak (ternary kernel değil), `metal/vulkan/npu/engine.py` `F.linear` fallback stub olarak (henüz custom shader yok, roadmap) etiketlendi; korundu, silinmedi — kanonik low-bit yollar `bitlinear.py` (PyTorch STE, training) ve `triton_fused_bitlinear.py` (GPU). **Generated raporlar generator'da düzeltildi, elle değil** — `scripts/sync_manifest.py matrix_payload` artık GERÇEK karşılaştırma (`missing_in_structure`/`missing_in_manifest` set-diff; `ok` hesaplanır), hardcoded-green değil; `docs/PROJECT_STRUCTURE.md` ve tüm `reports/*` kaynaktan regenerate edilir, elle edit EDİLMEZ. **2.64B design-target vs 3.67B ölçülen param çelişki DEĞİLDİR** (biri design-target, diğeri ölçülen instantiate sayısı; ikisi de measured/target disiplini ile raporlanır; hangi boyutun gerçekten eğitileceği gerçek bir pre-45K görevi, belgelenir, gizlenmez). **`tokenizer/tokenizer.json` `interfaces/tokenizer_spec.json`'un frozen byte-mirror'ı** (`check_tokenizer_sync.py` ile zorlanır); Wave-A `note` honesty edit'i geri alındı (frozen mirror byte-aynı kalmalı). **S25 ternary CPU-kernel microbenchmark** kapsam-sınırlı ölçülen kanıt olarak eklendi (`reports/benchmarks/ternary_kernel_microbench_s25/`: README + results.json + .cpp); tek-op CPU/NEON microbench (FMA tier bit-exact 3.01×; SDOT turbo ~8.3×, approximate int8); açıkça tam-model t/s DEĞİL, NPU DEĞİL, kanonik kernel dispatcher'a entegre DEĞİL — yalnız ilgili kernel kanıtı. **`config/` vs `configs/` ve çoklu entrypoint (`run.sh`, `zero_touch_start.sh`, `launch_*.command`) olduğu gibi korundu** (yeniden yapılandırma çok sayıda path referansını etkiler, frozen koşuyu kozmetik düzen için kırma riski); kanonik training girişi `run.sh`, runtime default yüzeyi `config/config.py`. **Sonsuz-cila döngüsü yok** — bu son kozmetik/hijyen/truth-sync pass'i; kalan gerçek gate sahip-compute → 45K koşusu → checkpoint → checkpoint-bağlı eval; daha fazla "cila" açıkça kapsam-dışı. Tam ayrıntı: `DECISIONS.md`.
- **Wave C eki (2026-06-27) — versiyon-string tekdüzeliği (yeniden BÖLME)**: Modül `__version__` 35 modül dosyasının tamamında tekdüze `"1.0-BUILD30-V2"` (kanonik proje versiyonu; `CITATION.cff v1.0-TITAN-BUILD30-V2`, `registry/` ve release-zip `...B30...` adlandırmasıyla uyumlu); önceki bir dalga 8 tanesini `"1.0"`a indirip 3-yönlü split yaratmıştı, bu tekdüzelik için geri alındı (bu sabitler dead/unreferenced; hiçbir test/manifest/schema okumaz). `-BUILD30-V2` suffix'i yeniden soyulMAZ (güncel build kimliği, stale fossil değil; yorumlardaki `V21.0 FIX` / `V23.0:` gibi gerçek fossiller kaldırıldı). `__author__` tekdüze `"Mert Yünlü"` (birkaç dosyada çıplak `"Mert"` ile split'ti). `sync_manifest.py` mla role-override, legacy `mla` dosya adının SHA/manifest sürekliliği için korunduğunu ama implementasyonun GQA olduğunu açıkça not eder. Tam ayrıntı: `DECISIONS.md`.
- **Wave D eki (2026-06-28) — post-audit doğruluk/stabilite düzeltmeleri (geri ALMA)**: Tüm `RMSNorm` variance'ı FP32'ye upcast eder — `layers/mertformer_block.py RMSNorm.forward` (block norm1/norm2 VE final_norm tarafından kullanılır) artık `x.float().pow(2).mean(...).rsqrt()` hesaplayıp geri cast eder (`layers/mla.py _QKRMSNorm`'u aynalar); hız için bf16-variance'a geri DÖNME (precision/consistency kazancı 3.67B ölçeğinde önemli; değişiklikten sonra pytest 370/4 değişmedi, hiçbir exact-value test bf16 variance'a bağlı değildi). `config.weight_quantization` etiketi `"absmax_per_tensor"` → `"rms_per_channel"` olarak gerçek koda uyduruldu (`bitlinear.py weight_quant` = per-row RMS); yalnız-label, alan hiçbir yerde okunmaz (doğrulandı) → truth-fix, davranış değişikliği değil. `bitlinear.py weight_quant`'a quant parite notu eklendi: `liquid.py jit_quant` ile lockstep kalmalı (ikisi de per-row RMS); birini absmean'e çevirmek diğerini çevirmeden train(weight_quant)/eval(jit_quant) desync ederdi. Tam ayrıntı: `DECISIONS.md`.

### Laptop preflight koşu geri-bildirimi (2026-07-02) — 45K öncesi kararlılık sinyali (belgelendi, frozen yola UYGULANMADI)
Tek-laptop preflight (`scripts/preflight_run.py`, RTX 5070, commit `5fc5068`; kanıt `evidence/2026-07-02-laptop-preflight/`) `LR=1.5e-3`'te **ıraksadı** (grad_norm → inf; loss rastgelenin üstüne çıktı). Karar: bu gerçek koşu-geri-bildirimidir ve [BACKLOG.md](BACKLOG.md)'deki 45K öncesi kararlılık kalemlerini açar, ama **frozen eğitim yolu `main`'de değişmez** — her fix (LR rejimi, göreli Liquid eşiği, `generate()` Liquid-state parite, held-out ppl harness) kendi compute-koşusunda uygulanıp yeniden doğrulanmadan main'e girmez; böylece kanonik 45K doğrulanmamış bir training-math düzenlemesiyle confound edilmez. Iraksayan koşu checkpoint'leri yalnız altyapı kanıtıdır (SHA-referanslı; ağırlıklar hariç, `.pt` gitignored).
- **45K öncesi kararlılık pass'i (2026-07-08) — aday düzeltmeler eğitim yoluna UYGULANDI, gerçek koşuda yeniden doğrulanmayı bekliyor**: Kaynak brief `CLAUDE CODE — AUTONOMOUS EXECUTION BRIEF · Pre-45K Stabilization + Repo Final-Sweep Pass` (rev v2), macOS'ta yerel çalıştırıldı (CUDA yok — RTX 5070 ayrı makine). Yönetici karar: `DECISIONS.md`'in kendi duran politikası "eğitim-matematiği değişiklikleri **belgeli aday düzeltmeler** olarak iner ve nihai sayılmadan önce kendi compute-koşusunda yeniden doğrulanır" der; bu pass tam olarak o maddeyi işletir — **sıradaki GPU koşusu bu düzeltmelerin doğrulamasıdır**, ıraksama "çözüldü" DEĞİLDİR. **Uygulananlar:** LR rejimi (`1.5e-3`→`3e-4` *tarama başlangıcı*, router ×1.5 farkı kaldırıldı, warmup 0.10→0.15, üçü de `TITAN_*` env ile taranır); ölü config alanları (`warmup_steps`/`min_lr_ratio`) gerçekten okunur hale getirildi (davranış korunarak — `min_lr_ratio` varsayılanı 0.01'e çekildi); Liquid spike eşiği mutlak→EMA-göreli (mutlak 5.0 yalnız ilk 100 gözlemde soğuk-başlangıç tabanı; 2026-07-02'de ~10.4 loss ölçeğinde mutlak eşik her adım vurduğu için Liquid katmanları **hiç eğitilmemişti**); `generate()` Liquid-state paritesi (gerçek bug: her decode adımı CfC yinelemesini `h=0`'dan yeniden başlatıyordu → üretimde Liquid katmanları durumsuz no-op'tu; teacher-forced eğitim bu bug'ı hiç tetiklemiyordu); yeni `eval/held_out_ppl.py` (checkpoint-zorunlu, sabit korpus+seed, deterministik packer; rastgele-başlangıçta `mean_nll 11.7649` ↔ `ln(128000)=11.7599` ile doğrulandı); yeni `config/model/mertformer_pilot_stabilization.yaml` (**ölçülen** 171,617,923 param, tahmin değil; yalnız-kararlılık, kabiliyet/ölçekleme iddiası YOK → mühürlü "ara-ölçek koşu yok" kararıyla çelişmez, STATUS'a uzlaştırma cümlesi eklendi); WSD cosine clamp (resume'da `TITAN_MAX_STEPS` değişirse LR tabanda kalmak yerine tepeye geri tırmanıyordu). **YENİ ve ön-onaylı DEĞİL (bilinçle işaretlendi, gizlenmedi):** genel loss-ıraksama devre kesici (`utils/divergence_guard.py`, `safety_brake_reason="loss_divergence_relative"`) — NaN freni yalnız sonsuz/NaN loss'u, Liquid guard yalnız Liquid parametrelerini yakalıyordu; "sonlu ama tırmanan" loss'u (10.4→15.0, hiç NaN olmadan) hiçbiri durdurmuyordu. Inert `failure_budget.py`'ın slope mantığından port edildi ama duvar-saati yerine loss EMA'sına bağlandı (deterministik + test-edilebilir); orijinal dosya yerinde, kullanılmadan, artık doğru etiketle duruyor (inert kod promote EDİLMEZ). `loss_ema` rank-yerel olduğu için fren kararı **all-reduce** edilir (H1 NaN-fren presedansı) → DDP desync/NCCL hang yok. Varsayılan AÇIK; **Mert bilinçli olarak tutmalı ya da düşürmeli.** **Tasarım sapması (bilinçli):** brief `MertFormer.forward()`'ın liquid state'leri *döndürmesini* istedi; bunun yerine `_present_liquid_states` üzerinde yayınlanıp `get_last_liquid_states()` ile okunuyor — çünkü ~15 çağrı yeri (`train/`, `eval/`, `tests/`, `mertformer_sdk/` ve **`trainer_core.py`'daki ONNX export wrapper'ı**) `forward()`'ı katı 3-tuple açıyor; 4. çıktı traced export kontratını değiştirirdi. Seçilen desen aynı sınıfın `_last_world_model_outputs` deseni. `reset_router_state()` **genişletildi, rename EDİLMEDİ** (mühürlü `mla.py` presedansı). **Ayrıca:** `AGIPaths.DATA_DIR` eksikti (`orchestrator/core.py:186` okuyordu → anında `AttributeError`) — inert kodda gerçek bug, düzeltildi, promote edilmedi; `utils/logger.py` `"source_sha256_status": "pending"` kalıcı bir yalandı → `"not_applicable_live_stream"`; logger↔logbook_build sır-redaksiyon listeleri yeniden senkronlandı (40-hex yakalayıcı canlı logger'da yoktu); `config/base.yaml` INERT işaretlendi; `preflight_run.py` artık 45K'nın gerçek optimizer'ıyla (GaLore+8bit) koşar ve `last_loss > first_loss` olduğunda bloke etmeyen bir ıraksama uyarısı basar. **Sınırlı sweep (Track 2), 4 gerçek bug canlı doğrulanarak düzeltildi:** `cli.py` kpi-report CWD'ye çözüyordu (sessizce "tüm kapılar fail" raporu + exit 0); `kpi.py` göreli interpreter yolu (`FileNotFoundError`); `overfit_gate.py` off-by-one (sınırda **sıfır** dizi); `data_pipeline.py` bağlanmadan önce stage JSONL'ini siliyordu (ağ kesintisi = veri kaybı, üstelik `.exists()` kontrolü stage'i "var" sanıyordu). **pytest 370→388 passed / 4 skipped.** Teacher/tokenizer/dataset/prompt politikası, MoE 8e2, BitNet çekirdeği, Liquid matematiği, readiness semantiği ve iddia sınırı DEĞİŞMEDİ. Yasak iddia eklenmedi. **Kalan tek kapı aynı: sahip olunan compute → 45K → checkpoint → checkpoint-bağlı eval.** Tam ayrıntı: `DECISIONS.md`.
