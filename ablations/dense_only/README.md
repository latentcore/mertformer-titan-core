# Ablation: Dense Only

**Purpose**: Disable both MoE and Liquid for a strict dense baseline.

**Config change**:
- `use_moe: false`
- `use_liquid: false`

**Status**: Not run yet. Execute on training hardware and record results in `ablations/results.md`.
