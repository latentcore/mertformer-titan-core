# BACKLOG — deferred work

Canonical backlog entry point. Turkish: [BACKLOG_TR.md](BACKLOG_TR.md). Seed detail: [V2_BACKLOG_SEED.md](V2_BACKLOG_SEED.md).

## Hard gate (not a code edit)
- **45K GPU run (K1)** — train the canonical 3.67B model on H100/H200; produce the first real loss curve, checkpoint, model card. This is the only thing that converts "target/vision" claims to "measured". **No intermediate 250M/500M run** — straight to 45K (see [DECISIONS.md](DECISIONS.md)). See [STATUS.md](STATUS.md).

**Scope note (2026-07-25):** the small set of genuinely-standalone pre-45K action items is what's enumerated below by name (dataset pinning, the 5 low-priority code-level items in the "safe pass" section, etc.) — not the entire backlog. Most of what's filed under "Post-45K" elsewhere in this doc (checkpoint-bound eval probes, benchmark regression gates, publication/release prep) is **not** separate pending work that needs scheduling before or alongside the run — those items are structurally blocked on a real checkpoint existing (`SKIPPED`/`NO_CHECKPOINT` today, by design) and execute as the natural next step once the 45K run itself produces one. They don't need a decision now; the 45K run is the thing that unblocks them.

## Analytical param-count estimator bug — FIXED (2026-07-27), no training-math change
A cross-check of `ARCHITECTURE.md`'s stated `~1.86B active params (MoE top-2 + shared)` against this repo's two independent analytical estimators — `scripts/scaling_audit_math.py::estimate_params()` and `config/config.py::_estimate_total_params()` (the latter feeds the real `auto_configure_batch_size()` VRAM-budgeting logic, not just a reporting tool) — found both undercounting for two compounding reasons: (1) both reused the dense-layer `intermediate_size` (5632) for MoE-expert sizing instead of the real, larger `moe_intermediate` (8192) that `layers/moe.py`'s `BitSwiGLU` experts actually use; (2) both omitted `layers/moe.py`'s always-instantiated, always-active **shared expert** (`self.shared_expert = BitSwiGLU(hidden_size, moe_intermediate)`, unconditionally added to every token's output via a learnable sigmoid gate) entirely — a 9th, always-active expert-sized block that is not part of `num_experts`. Net effect: active params were undercounted by ~44% (~1.06B vs. the real ~1.886B) and total params by ~8%. **Fixed** in both files to mirror the real architecture; `scaling_audit_math.py` now reports `~3.698B` total / `~1.886B` active, matching `ARCHITECTURE.md`'s independently-sourced figure. 4 new regression tests: `tests/test_scaling_audit_math.py` (3 tests, isolated-stub `cfg` via `monkeypatch` rather than the live global singleton, to avoid the test-order-dependent pollution class already documented elsewhere in this file) and one addition to `tests/test_config_dynamic_param_count.py` (hand-derives the correct GQA-attention + shared-expert total and pins the exact delta introduced by the fix). Pure param-accounting/tooling fix — no training-loop, LR, or architecture change; `auto_configure_batch_size()`'s VRAM math is now more accurate but this repo has never run it against real GPU hardware to begin with, so no prior claim is invalidated. Test count: `622 → 626 passed, 5 skipped` (+4).

## Analytical param-count estimator — SUPERSEDES the 2026-07-27 entry above (2026-07-29), no training-math change
The 2026-07-27 pass above fixed two real undercounts but left a third error in place and, worse, its *result* was wrong in a way that looked right. An independent static audit re-derived the attention term and found `scripts/scaling_audit_math.py` was using the **MHA** formula `4 * hidden * (num_heads * head_dim)`. `layers/mla.py` is **GQA**: `k_proj`/`v_proj` are only `num_kv_heads * head_dim` wide. At the canonical 16q/8kv that overcounted attention by 4,194,304 params per layer = **+75,497,472** across 18 layers. Verified against a live `GQA()` instance: `q_proj + 2*kv_proj + o_proj` equals the summed `numel` of its q/k/v/o projections exactly, `4*h*nh*hd` does not.

Fixing that alone made the script report 3.622B and *fail* its own within-1%-of-measured drift test — which exposed the real story: **two errors had been cancelling**. Both estimators were also omitting, entirely:
- the **Liquid/CfC mixers** — `3 x 16,783,360 = 50,350,080` params (1.37% of the model). Each `LiquidMixer` holds a `LiquidCell` with four `hidden x hidden` BitLinear projections (`input_w`, `hidden_w`, `tau_input_w`, `tau_hidden_w`), a `tau_bias` of shape `(1, hidden)`, and an `nn.LayerNorm(hidden)`. This is the single largest gap and it was in `config/config.py::_estimate_total_params` too — the function that feeds the real `auto_configure_batch_size()` VRAM budget.
- **`LiquidRouter`'s** `fluid_mixer` (depthwise `Conv1d`, `kernel=history_window=4`, `bias=False`) and `fluid_gate` — `6 x 24,576 = 147,456`. The estimators counted only `main_proj`, as if the router were a bare `nn.Linear`.
- **GQA's** `q_norm`/`k_norm` `_QKRMSNorm` weights — `18 x 2 x 128 = 4,608`.

Total omission `50,502,144` almost exactly offset the `+75,497,472` attention overcount, so the net error landed inside the 1% tolerance band and the drift test could see neither bug. Note the 2026-07-27 entry's claimed result, `~3.698B total`: `reports/FACTS.json` already carries that exact figure under `stale_measured_params_historical_only` — that pass had accidentally tuned the script *to the known-stale number*. (The digits are deliberately not repeated here: `scripts/check_facts_consistency.py` forbids a stale measured-param id in any current-truth markdown surface, precisely so a reader cannot mistake a quoted historical value for a live claim. FACTS.json is the place to look it up.)

**Fixed** in `config/config.py::_estimate_total_params` and `scripts/scaling_audit_math.py`. Both now reproduce the canonical measured **3,672,982,022** *exactly, to the parameter*, and the script reports `~1.861B` active — matching `ARCHITECTURE.md`'s independently-sourced `~1.86B` more closely than the previous `1.886B` did. `ARCHITECTURE.md` and `FACTS.json` needed no edit; they were already correct. Added a runtime cross-check in `scaling_audit_math.py` that compares its total against `config._estimate_total_params` and shouts on >0.5% divergence, so these two estimators can never silently drift apart again. Tests: the two stub configs were missing `num_kv_heads`, `use_liquid` and `liquid_layers_idx` — which is *why* both bugs were invisible to them — now added, the hardcoded MHA formula in `tests/test_scaling_audit_math.py` replaced with the GQA one, and the 1%-tolerance assertion replaced with **exact equality** (a tolerance band is precisely what let two offsetting errors pass). Pure param-accounting/tooling fix: no training-loop, LR, or architecture change.

## Feature-flagged drift detectors — scaling bug fixed (2026-07-29), recalibration still open
`layers/lifelong_safety.py::_update_stats` and `train/continual_adapter.py::update` both computed their drift metric **after** the EMA had already absorbed the current observation. Since `running' = d*running + (1-d)*x`, the quantity `|x - running'|` is identically `(1-d) * |x - running|` — so both reported `(1 - decay)` times the real deviation: **1%** of it at `lifelong_ema_decay=0.99`, **2%** at `continual_loss_ema_decay=0.98`. Consequences: `lifelong_drift_threshold=0.35` was unreachable, making the stability-first damping branch in `LifelongSafetyLayer.forward()` dead code; and `continual_drift_threshold=0.2` required a 10-unit single-step loss jump, which `train/train.py`'s NaN brake would catch long before, making the "Continual Drift Alert" branch dead too. Both fixed by sampling drift **before** the EMA update. Both components are feature-flagged **off** by default (`use_lifelong_safety_layer=False`, `use_continual_adapter=False`), so no canonical-run behaviour changes — but a component named "safety layer" whose safety trigger can never fire is worse than no layer. Tests upgraded from shape/`isinstance` smoke checks to real, decay-parametrised drift assertions (the old tests could not have caught either bug, and would not have caught the fix).

**STILL OPEN — candidate, needs a real run to calibrate:** the two thresholds (`0.35`, `0.2`) were chosen against a quantity ~50-100x smaller than what is now reported, so they are almost certainly too loose for the corrected scale. Deliberately **not** retuned in this pass: picking a threshold without a real loss/activation trace to calibrate against would just be substituting one unverified number for another. Retune when the 45K run (or a pilot) produces real drift traces, and only if either flag is actually turned on.

## Hot-path fixes with numeric gates (2026-07-29), no training-math change

Four findings on paths that run inside training. Each was gated by a numeric or differential
check rather than by review alone, because none of them are visible in a loss curve.

- **MoE capacity path de-synced — DONE, throughput gain UNMEASURED.** `layers/moe.py`'s capacity
  block looped over all 8 experts calling `(topk_idx == e).nonzero()`, and also used
  boolean-mask indexing, `.any()`, and per-expert `.item()` in `_dispatch_parallel`. Every one
  of those forces a CUDA device→host synchronisation inside the forward pass (`torch.nonzero`
  must sync to size its output). Replaced with a vectorized stable-`argsort`/`cumsum` form; the
  overflow-row fallback uses `torch.where` and telemetry scatters the mask as a weight.
  `_dispatch_parallel` takes one `counts.tolist()` transfer instead of E `.item()` calls.
  **GATE:** `scripts/cfc_moe_tolerance_check.py` → `max_diff_moe=0.000000`, i.e. losses
  bit-identical to the previous implementation, plus 15/15 MoE guard tests. This reverses the
  2026-06-13 Pass 7 deferral of item 17 (see `DECISIONS.md`).
  **The throughput benefit is NOT measured.** There is no CUDA device on this machine; the
  sync-count reasoning is static analysis. Treat "faster" as a candidate until a real GPU run
  shows it.
- **Capacity formula extracted to `layers.moe.moe_capacity()`.** `tests/test_moe_capacity.py`
  and `tests/test_property_moe_capacity.py` were each re-implementing the formula locally, so
  the entire capacity rewrite above could have broken the real path with both files green.
  Both now import the shipped helper. New `tests/test_moe_capacity_drop_order.py` (14 cases)
  pins *which* assignments survive over capacity — `torch.nonzero` returns row-major, so the
  old code kept each expert's first `capacity` hits in (row, col) order, and the vectorized mask
  is asserted bit-identical to a direct `nonzero()` reference over adversarial patterns.
- **`train/packing.py` resume counter de-synced by oversized rows — DONE.** The `pending` window
  retires a row when `end_cum <= emitted_tokens`, which is only valid while
  `len(buf) == cum - emitted_tokens` holds. The oversized branch did `cum += len(piece)` for a
  row it emitted TRUNCATED and whose tail it discarded, leaking exactly 1 token (the EOS —
  `encode_row` already truncates to `max_seq_len`) into a permanent offset growing by 1 per
  oversized row. **MEASURED** at `max_seq_len=8`, worst gap between a sequence's last
  contributing row and its resume point, sweeping oversized-row count 0/1/5/30/100/400:
  `1, 1, 3, 11, 34, 134` before → `1, 1, 1, 1, 1, 1` after (1 is structural). At 1600 oversized
  rows the old counter never advanced past the oversized block at all, so a resume would
  silently re-read the whole tail; `pending` grew linearly and `_consumed_through()` scans it
  once per sequence, so cost went quadratic (0.6 ms → 32 ms over a 0..6400 sweep). **GATE:** the
  packed token stream is byte-identical — `input_ids`, `identity` and `true_len` compared
  against the pre-fix implementation across four configurations — which is the contract keeping
  teacher and student streams aligned. 3 regression tests confirmed to fail pre-fix.
  This partly closes Pass 7's deferred item 38 (packing resume-seam coverage); the seam was not
  merely uncovered, it was hiding this bug.
- **`train/trainer_data.py` offset index → `array("q")` — DONE.** **MEASURED** 34.8 MiB per 1M
  offsets as `list[int]` vs 7.6 MiB as `array("q")` (4.56x). `CurriculumDataset` is an
  `IterableDataset`, so the object is copied into every DataLoader worker and the multiplier
  compounds with `num_workers`; at ~100M lines that is ~3.4 GiB vs ~0.75 GiB of host RAM per
  worker. `CurriculumDataset` had **no test coverage at all** despite being on the real training
  path — new `tests/test_curriculum_dataset.py` (10 cases) pins the array type plus the
  pre-existing uniform-line-sampling fix, which also had none.
- **Teacher-logit identity sidecar — DONE.** `scripts/validate_logit_alignment.py` called
  `torch.load(shard, weights_only=False)` on every shard purely to read each item's small
  `identity` dict, materialising all the Top-K teacher logits in host RAM to do it. At corpus
  scale that is hundreds of GiB of reads for a few MiB of hashes, making the alignment gate
  unrunnable off the training box. `scripts/precompute_logits_topk.py` now writes
  `<shard>.pt.identities.json` (hooked into `_atomic_torch_save`, the single gate both write
  sites pass through, so the parallel lane gets it free). The sidecar is a **cache, never
  authoritative**: it carries `shard_bytes` and is rejected when that disagrees with the shard
  on disk, because comparing freshly packed sequences against identities from an older shard
  generation would be a silent false PASS on the one gate guaranteeing teacher/student
  alignment. Unknown format, count mismatch and unparseable JSON all fall back to a full load
  with a warning; the fallback path now also frees each chunk so its peak is the largest single
  shard rather than the sum. Named `.pt.identities.json` so it never matches the
  `*_part_*.pt` glob and stays invisible to shard-count gates. 10 cases in
  `tests/test_identity_sidecar.py`.

Test count: `649 → 691 passed, 5 skipped` (+42).

## Dead scripts deleted (2026-07-29)
- **`scripts/md_build30_sweep.py`** — every `REPLACEMENTS` pair was `old == new`, so the script
  was a no-op under any argument. Deleted; rationale in [DECISIONS.md](DECISIONS.md).
- **`scripts/titan_onnx_stress_test.py`** — tokenized with the OpenAI GPT-4 encoding
  (`cl100k_base`) rather than the project's Llama-3 tokenizer, and `tiktoken` is deliberately
  absent from `requirements.txt` so it could not import. Deleted; rationale in
  [DECISIONS.md](DECISIONS.md).

## STILL OPEN — pytest mutates tracked release artifacts (found 2026-07-30)
Running `pytest` rewrites tracked files under `reports/`: `final_orchestrator_status.{json,md}`
and `post_train_autorun_status.{json,md}`. On a machine with no checkpoint this is a
**downgrade** — the committed versions record `mode: demo-only` / `status: completed` with a
resolved checkpoint path, and a local test run overwrites them with `plan-only` / `planned`.
Committing that loses real information, so this pass reverted those files rather than staging
them (`git checkout HEAD -- reports/`). Not fixed here because the fix is a decision, not an
edit: either the affected tests get a `tmp_path` output directory, or those four artifacts stop
being tracked and are regenerated by the ladder only. Both are behavioural changes to the
closure ladder, which is out of scope for an audit-closure pass. **Anyone running the suite
locally must check `git status -- reports/` before committing.**

## Doc-consistency pass (2026-07-27) — cross-file contradictions found via a root-`*.md` X-Ray audit, all fixed
A full-content audit of every root-level `*.md` file (60 files, no subdirectories) surfaced several real, self-contained cross-file contradictions — each verified against live code/git state before fixing, none requiring compute:
- **This file's own discipline gap:** the shared-expert fix above (commit `198e06c`) landed real code+test changes with zero corresponding `BACKLOG.md`/`DECISIONS.md`/`CHANGELOG.md` entry — landing the very next commit after `f8890af` added the CHANGELOG-drift-prevention note this exact gap is about. This section (and the matching `CHANGELOG.md`/`CHANGELOG_TR.md`/`DECISIONS.md` entries) close that gap.
- **`CHANGELOG.md`/`CHANGELOG_TR.md` chronological-order bug:** "Pass 7 (2026-06-13)" was filed after the 2026-03-13 and 2026-02-08 tagged-release entries, breaking the file's own reverse-chronological convention (a 06-13 record cannot correctly sit after 03-13/02-08). Moved to its correct slot, both languages.
- **`ENV_VARS.md` a month stale:** last content-updated 2026-06-28; missing the entire 2026-07-08→07-25 stabilization-pass env-var surface (LR/warmup-sweep family, `TITAN_DIVERGENCE_GUARD`, re-warmup family, off-site-backup family, `TITAN_PREFLIGHT_*` family, `TITAN_DETERMINISTIC`, dataloader flags, `TITAN_PROFILE`/`TITAN_INSTALL`, `MERTFORMER_DDP_SMOKE_SECONDS`/`MERTFORMER_FUSED_BACKWARD`) despite its own scope note claiming to be the single index of training-relevant vars, and despite several of these being named explicitly elsewhere in this same file, `DECISIONS.md`, `TRAINING_PLAN.md`, and `START_HERE.md`. Also had a stale closing note claiming "precompute batch size is a CLI flag, not an env var" — true for direct script invocation, false for `zero_touch_start.sh`'s own `TITAN_PRECOMPUTE_BATCH`/`TITAN_TOP_K`/`TITAN_PRECOMPUTE_GPUS` auto-invocation path. Fully re-synced against a live `grep` of every `os.environ.get`/`os.getenv` call in the repo.
- **`V2_BACKLOG_SEED.md` Track F, 3 stale items:** listed as open backlog three items already resolved elsewhere in this same file/`DECISIONS.md` — the `liquid_warmup_steps` env-override (done 2026-07-12), the z-loss double-multiply (done 2026-07-12), and the `mark_weights_updated()` cache question (investigated and documented 2026-07-12). Annotated resolved with cross-references rather than silently left to keep implying open work.
- **`CHESS_5080_POC_INTERNAL.md` (EN) self-admittedly stale:** its own TR twin carried an inline note that the EN canonical file was out of date on Share Export behavior (Windows build workspace + EXE delivery, Stockfish auto-fetch cache, curated position suite, synthetic teaching corpus — all TR-only). Brought to content parity.
- **`TECHNICAL_REPORT.md`/`_TR` frozen masthead date:** "Date: 2026-06-18" while the body already carried a 2026-07-19 revision (the INT-KERNEL relabel). Added an explicit last-revision note rather than silently back-dating (same discipline as the `MERTFORMER_TITAN_TEK_NIHAI_REFERANS` Documents-external fix earlier the same day).
- **Documentation-navigation fragmentation:** `AGENTS.md` self-declares itself item #1 in the repo's own "Source-of-Truth Order" (§6 of the Master Protocol mirrors this) but had zero inbound links from `README.md`, `README_TR.md`, `START_HERE.md`, `GOVERNANCE.md`, or `STATUS.md` (confirmed via `grep`, zero matches) — a fully orphaned top-priority document. Separately, `README.md`/`README_TR.md` never linked to `START_HERE.md`/`README_SUMMARY.md` (the dedicated external-reviewer onboarding docs), so a reader starting at the GitHub-default `README.md` had no path to discover they exist. Both fixed by adding `AGENTS.md` and the `START_HERE.md`/`README_SUMMARY.md` pair to `README.md`'s/`README_TR.md`'s "Canonical surfaces" list.
- **Checked and confirmed NOT a bug:** `TROUBLESHOOTING.md`'s advice to re-run `run.sh` (not `zero_touch_start.sh`) for an accelerate-config mismatch — `TITAN_FORCE_ACCELERATE_RECONF` is read only by `run.sh` (`grep` confirmed), so the existing advice is accurate; left unchanged.
- **Test count / verification:** all changes in this pass are Sınıf A / Class A (docs-only) per the Master Protocol; `bash scripts/verify_all.sh` re-run green after the edits; `626 passed, 5 skipped` unchanged by this pass (no test surface touched here — the +4 belongs to the shared-expert fix above).

## Pre-45K — data/corpus/launch-gate readiness (2026-07-13)
These items existed only in Mert's personal external planning documents, never tracked in this repo's own backlog — surfaced here so a team with only repo access can see them. None are code bugs; all are real, concrete pre-launch work, verified live against this repo/machine on 2026-07-13.

- **Corpus not materialized:** `datasets/stage*/*.jsonl` is 99 lines total; the canonical 45K run needs ~23.59B tokens (45000 × 128 × 4096). `scripts/data_pipeline.py` exists and can build it, but has never been run at scale.
- **Local disk cannot hold it:** this machine has **3.7 GB free (99% full)** as of 2026-07-13 — corpus materialization and teacher-logit precompute (below) must happen on the rented training machine, not here. Recorded so it isn't silently assumed local.
- **Teacher-logit precompute never run:** `datasets/logits/` is empty (0 shards). `offline_clean` (the strictest, cheapest lane) hard-fails without it. Needs gated Llama-3.3-70B access + `HF_TOKEN` + real GPU time. **Clarification (2026-07-25):** Mert confirms he already obtained gated Llama-3.3-70B access and an `HF_TOKEN` months ago — the credential itself is not the blocker. The only remaining prerequisite is real GPU time on the rented machine.
- **RESOLVED (2026-07-19), `top_k` / disk-budget decision:** Mert decided **32**, not 256 (~4.5TB, not ~36TB). `scripts/precompute_logits_topk.py`'s `DEFAULT_TOP_K` changed `256 → 32`; `scripts/launch_8xb300.sh`'s informational disk-budget echo updated to match. See `DECISIONS.md` "Eight launch-time decisions locked...".
- **RESOLVED (2026-07-19), model size decision:** Mert confirmed **3.67B measured, canonical** (no config change needed — already the working default in `reports/FACTS.json`/`STATUS.md`/the public gist). The 2.64B-vs-3.67B *tension in DECISIONS.md's historical prose* is not erased (it's a real prior discussion, left as-is), but "which size actually trains" now has an explicit, recorded answer. See `DECISIONS.md`.
- **RESOLVED (2026-07-18), `reports/dataset_health.md` refreshed:** ran `scripts/extract_dataset_refs.py --fetch-metadata` (HF Hub API metadata only, no downloads, disk-safe) + one manual check. All 16/16 curriculum datasets now have real status (was 7/16). Full table: `reports/dataset_health.md`.
- **RESOLVED (2026-07-19), the 2 unreachable datasets:** `gorilla-llm/gorilla-openfunctions-v2` and `NousResearch/FC-1k` (both confirmed live HTTP 401 on 2026-07-18) were dropped from `scripts/data_pipeline.py`'s `STAGE5_SOURCES` and replaced with `NousResearch/hermes-function-calling-v1` (config `func_calling`), per Mert's explicit instruction to keep Stage 5's tool-use category intact rather than just drop-and-shrink it. Verified live+ungated+`apache-2.0` via the HF Hub API before landing (cleaner license status than either removed dataset had). `scripts/titan_preflight.py`'s `required_datasets` list (which still referenced the dead `gorilla-llm` ID and would have made preflight FAIL on an intentionally-dropped source) was updated to match. `datasets/SOURCES.md`/`_TR`, `datasets/LICENSES.md`/`_TR`, `datasets/inventory.{md,json}`/`_TR` regenerated. See `DECISIONS.md`.
- **RESOLVED (2026-07-19), the "5 datasets, TBD license" item — kept, documented, per Mert's decision:** `teknium/OpenHermes-2.5` genuinely has no license tag on HF; `codeparrot/github-code` resolves to license tag `other` (inherits per-file licenses from the GitHub repos it scraped — a real mixed-license situation, not a single value to fill in); `mlabonne/guanaco-llama2-1k` has no visible license tag in a manual check. Mert's explicit call: keep all three with this documented caveat rather than drop them (standard practice for open pretraining corpora); formalized as a conscious risk-acceptance in `DECISIONS.md`, not a lingering gap. (The other 2 of the original 5 — `gorilla-llm/gorilla-openfunctions-v2`, `NousResearch/FC-1k` — were the unreachable-repo problem above, resolved separately.)
- **RESOLVED (2026-07-25), `bigcode/the-stack-dedup` revision/sha256 pinned:** the earlier note ("needs network access on the training machine, not here") was overly cautious — `scripts/record_dataset_hashes.py`'s own docstring confirms it only queries the HF Hub file-manifest API (no dataset content download), so it runs fine from any machine with internet, including this Mac. Ran it and hit a real crash (`HFValidationError: Repo id must use alphanumeric chars...: '{DATASET_ID}'`), traced to a genuine bug: `scripts/extract_dataset_refs.py`'s regex scanner matched an f-string *log-message* template in `scripts/preflight_run.py`/`preflight_run_pilot171m.py` (`f'load_dataset("{DATASET_ID}", ...)'`) as if it were a real call, and the unsubstituted `{DATASET_ID}` literal flowed into `datasets/inventory.json`, then crashed the whole pinning pass before any dataset got pinned. Fixed with two changes: (1) `extract_dataset_refs.py` now rejects any captured id containing `{`/`}` (not a valid HF repo id shape); (2) `record_dataset_hashes.py`'s pinning loop (now the testable `_pin_datasets()` helper) wraps each dataset in its own try/except, so one bad/unreachable id can never again take down the rest. `datasets/inventory.json` regenerated (20 real datasets, confirmed clean). `datasets/hashes.json` regenerated (27 sources, 0 errors) — `bigcode/the-stack-dedup` is now `status: "verified"`, `revision: "17cad72c886a2858e08d4c349a00d6466f54df63"`. Covered by `tests/test_dataset_pinning_resilience.py` (2 tests: f-string false-positive rejection + real-call retention; per-dataset failure isolation via a fake API).
- **2-GPU DDP smoke never run** — no multi-GPU hardware has ever exercised this repo's DDP code paths (Liquid freeze/unfreeze, MoE dispatch collectives, Gate 3 rank-sync). Single-GPU/CPU tests cannot substitute. Required before trusting a multi-GPU 45K launch.
  - **B8 (was untracked in this repo until 2026-07-13; found only in Mert's external "Tek Nihai Referans" doc, §I.2.4/§I.3):** Liquid unfreeze relies on its parameters having been pre-registered with the optimizer, with no protective assert — if the registration order ever changes, a layer can "appear unfrozen but not actually update" (optimizer rebuild across the unfreeze boundary is skipped as "tricky under DDP," so the first post-unfreeze steps could be silent no-ops). This is a **distinct mechanism from Gate 3** (Gate 3 asserts DDP gradient all-reduce/rank-sync via `accelerator.gather()`; B8 is about optimizer param-group registration order, never touched by any Gate 3 fix). Status per the external doc: still open, "cannot be verified locally, requires 2-GPU." First step if/when 2-GPU hardware is available: after the step-10000-class unfreeze, `all_gather` a single tau/liquid parameter across ranks and confirm it actually changed value (today's `train.py` has zero `all_gather` calls for this).
  - **DONE (2026-07-25) — `scripts/pre45k_gate.sh` built.** Chains three checks, all before any real training spend: (1) the existing offline preflight (`scripts/titan_preflight.py`'s default profile — disk/data-stage/architecture/mini-forward chain, already the canonical zero-GPU-spend entrypoint per README's verified matrix); (2) the existing dry-run preview (`zero_touch_start.sh --dry-run`, resolves plan + real train command, launches nothing); (3) a **new** short, real 2-GPU DDP smoke test, `scripts/ddp_smoke.py::run_ddp_smoke_test()` — an independent implementation of the same design as the Kaggle-batch orchestrator's `ddp_smoke_test()` (poll real GPU utilization while the subprocess is alive, only confirm DDP if a genuine dual-GPU-active sample was observed, safe fallback on anything ambiguous), deliberately **not** sharing code with `kaggle_batch_runner.py` to avoid regression risk to that file's own 18-test suite this close to launch (same accepted-duplication class as the `RMSNorm`/`_QKRMSNorm` entry above). `scripts/pre45k_gate.py` orchestrates the chain and writes `reports/pre45k_gate_report.json`/`.md` with a combined verdict (`PASS_DDP_NOT_APPLICABLE` / `PASS_DDP_CONFIRMED` / `PASS_DDP_UNCONFIRMED` / `BLOCKED` / `BLOCKED_DDP`); an optional `--strict-ddp` flag turns an unconfirmed-but-present-2-GPU result into a hard block. **Ran for real on this Mac (2026-07-25):** offline preflight PASS (3.47s), dry-run preview PASS, DDP smoke correctly `skipped_not_2_gpu` (0 GPUs here) — verdict `PASS_DDP_NOT_APPLICABLE`, exit 0. **Honest claim boundary — what this does NOT prove:** the DDP smoke test's actual "genuinely detects working 2-GPU DDP" behavior (the `PASS_DDP_CONFIRMED` path) has never been exercised on real 2-GPU hardware from this repo — only its skip-path, its report/verdict logic, and its three failure-mode decision branches are verified (26 new tests: `tests/test_ddp_smoke.py`, `tests/test_pre45k_gate.py`, all mocked at the subprocess/GPU boundary, no real accelerate/CUDA dependency). B8's own "cannot be verified locally, requires 2-GPU" status is unchanged by this — this gate makes the *check* cheap and pre-spend, it does not make the underlying 2-GPU verification possible without 2-GPU hardware. Test count: `596 → 622 passed, 5 skipped` (+26).
- **RESOLVED (2026-07-19), off-site checkpoint backup auto-wiring:** `scripts/offsite_backup_watcher.py` (new) polls `cfg.save_dir`, skips any cycle where the newest file has been touched within a configurable stability window (best-effort partial-write guard -- a partially-synced checkpoint is worse than no checkpoint), and dispatches to `aws s3 sync`/`gsutil rsync`/`rsync` based on the `TITAN_OFFSITE_BACKUP_DEST` scheme, with retry+backoff. Wired into both `scripts/launch_8xb300.sh` (backgrounded before its `exec` into `zero_touch_start.sh`, since `exec` replaces the process and nothing after it would otherwise run) and `scripts/launch_ocean_45k.sh` (mirrors the existing `nvidia-smi dmon`/`trap`-kill pattern exactly, since that script never execs and can clean up after itself). Off by default -- a no-op with a clear log line unless `TITAN_OFFSITE_BACKUP_DEST` is set, so no launch behavior changed for anyone not opting in. Still deliberately NOT choosing a cloud provider (cost/access decision, unchanged from before) -- this closes the "no automation exists at all" gap, not the provider decision. `tests/test_offsite_backup_watcher.py` (14 tests) covers the pure logic (stability detection, sync-command construction per destination scheme, retry/backoff) with injected fakes -- no real subprocess/network/sleep in the test suite. `runbooks/checkpoint_offsite_backup.md`/`_TR` updated to point at the watcher as the now-available automated path, manual procedure kept as the fallback/cross-check.
- **`reports/launch_time_decisions_checklist.md` already covers launch-moment judgment calls** (Liquid keep/drop, lane choice, off-site backup destination, etc.) — cross-referenced here so the two lists don't drift apart.

**Real-world check performed 2026-07-13 (`scripts/secret_scan.py --include-home-dirs`, new capability, see below):** confirms the repo's own A0 security-sweep concern was correct — **93 live secret-pattern hits** (87 `github_token`, 6 `github_fine_grained_pat`) found across files in `~/Documents` and `~/Downloads`, none in the repo itself (repo scan: 0 hits, 896 files). No secret value is reproduced here or anywhere in this pass — the tool redacts before printing. **This is an unresolved, real action item for Mert personally, not a repo/code issue:** revoke the flagged tokens at github.com → Settings → Developer settings → Personal access tokens.
- **`scripts/secret_scan.py` gained an opt-in `--include-home-dirs` flag (2026-07-13):** scans `~/Documents` + `~/Downloads` (cross-platform via `Path.home()`), tolerant of unreadable subdirectories, off by default so `verify_all.sh`'s existing gate is unchanged byte-for-byte.
- **RESOLVED (2026-07-19), the pattern gap above:** `PATTERNS` gained `anthropic_key` (`sk-ant-...`), `anthropic_service_key` (`sk-svcac-...`), `google_api_key` (`AIza...`), and `google_oauth_client_secret` (`GOCSPX-...`). The pre-existing `openai_key` regex's `[A-Za-z0-9]` tail class genuinely could not match the Anthropic prefixes (confirmed — the embedded hyphen breaks the match before 10 chars accumulate); the new patterns are independent entries, not a fix to the old one. 4 new tests in `tests/test_secret_scan.py` (15 total, all passing).

## Pre-45K — RTX-5070 36M + 171M divergence, second confirmation (2026-07-12)
Two real RTX-5070 laptop runs on 2026-07-12 (36M preflight, 171M pilot — both at the then-current default LR regime, `TITAN_LEARNING_RATE=3e-4`) **both diverged**, independently confirming the class of failure first seen on 2026-07-02. Full step-by-step evidence: `evidence/2026-07-12-laptop-preflight-v2/`.
- **36M:** GradNorm exploded from ~30 (step 90) to ~2.3e13 (step 230) coinciding with the first Liquid-unfreeze event (`liquid_warmup_steps=200`, an ad-hoc value the pilot yaml's own comment already warned not to copy); partial recovery, then a second explosion on the Liquid re-unfreeze after cooldown (~step 414+).
- **171M:** GradNorm exploded from ~10K (step 340) to >1e12 (step 560, where the run was manually interrupted) — **without Liquid ever unfreezing** (`liquid_warmup_steps=667`, the run stopped at step ~563, well before that point). This rules out Liquid-unfreeze as the sole/primary trigger for the 171M case; onset instead tracks LR crossing roughly the 2e-4–2.5e-4 range during warmup, in lockstep with MoE `Overflow`/`Balance(std)` degrading (e.g. 171M: `Overflow` 0.03→0.15-0.19 across the explosion window).
- **z-loss effective weight ≈ 2e-6 fix APPLIED (candidate, unverified):** `config/config.py:329` — see the item below, moved from "post-45K, deliberately not changed" to "applied candidate" given this new dual-run evidence. This reopens a 2026-07-08 pre-45K freeze decision; the next real RTX-5070/45K run is the verification event (same discipline as F1/F3).
- **C1 — divergence guard's grad_norm blind spot — APPLIED 2026-07-25, candidate — unverified by a dedicated GPU re-run.** `utils/divergence_guard.py` only tracked `loss_ema`/`ce_ema` relative to a warmup-end reference; it never looked at `grad_norm`. In the 171M run, `clip=2.0` kept the *loss* from exceeding 1.5× the (already-slightly-elevated) reference even while GradNorm reached >1e12 — the guard armed at step 450 (`reference loss_ema=11.9316`, itself already above the healthy ~10.5 baseline) and **never tripped** for the entire remainder of the run, despite unambiguous catastrophic instability. This was deliberately left unfixed for two passes ("the fix design space needs its own consideration, not a rushed patch to a safety-critical component") until Mert explicitly reviewed and approved a design. **Applied fix:** an INDEPENDENT co-trigger on the same safety brake (`train/train.py`), reusing the exact same pure functions the CE guard already uses (`update_loss_ema`, `update_divergence_guard_state` — zero new pure-function code, only new wiring): a cold-start-safe EMA of the pre-clip grad norm (`grad_norm_ema`), armed the same way as the CE guard, tripping when `grad_norm_ema` exceeds `100×` (`grad_norm_guard_multiplier`, config-overridable) the warmup-end reference for `10` consecutive steps (`grad_norm_guard_patience`, shorter than the CE guard's 50 — both recorded explosions were both fast-developing and many orders of magnitude past 100x almost immediately). Either guard tripping alone brakes the run (logical OR, distinct `safety_brake_reason` codes: `grad_norm_divergence_relative` vs `loss_divergence_relative`) — this is a co-trigger, not a replacement. State fully threaded through F3's persistence mechanism (`_current_guard_state()` / resume restoration), so it doesn't reintroduce the exact problem F3 just fixed for the CE guard. Tests in `tests/test_divergence_guard.py`: pure-function replays of the actual recorded 36M (`~30→~2.3e13`) and 171M (`~10K→>1e12`) explosion shapes confirming both trip with enormous margin under the chosen threshold, a healthy-transient-spike non-trip sanity check, and a source-scan regression pinning the wiring (independent signal, OR logic, F3 persistence, config-overridability). `tests/test_resume_policy.py`'s round-trip/backward-compat tests extended to cover the new state fields. **Why still candidate:** the 100×/10-step defaults are a defensible, reasoned choice given the two real data points on record (both explosions clear the threshold by 6+ orders of magnitude) but are not calibrated against a live run — same discipline as every other guard/training-math change in this repo.

### Third real-hardware confirmation (2026-07-25, Kaggle T4×2) — LR/z-loss candidates insufficient, C1 verified working
Both the 36M and 171M jobs were re-run again on Kaggle (2× T4, single-GPU — the batch's own DDP smoke test correctly detected no dual-GPU activity and fell back, `manifest.json`: `"used_ddp": false` for both) as part of a 5-job unattended batch. **Full detail:** `evidence/2026-07-25-lm-36m-reverify/README.md`, `evidence/2026-07-25-lm-171m-reverify/README.md`.
- **Both diverged again** — third real-hardware confirmation of this exact class (2026-07-02, 2026-07-12, now 2026-07-25), this time with the candidate LR=3e-4/z-loss=0.05/EMA-relative-Liquid-threshold fixes actually in effect. **These candidates alone are not sufficient to prevent the divergence** — status moves from "candidate, unverified" to "candidate, tested on real hardware, confirmed insufficient alone." 36M: MoE `MaxLoad` crossed 0.40 from step ~110, Liquid unfroze at step 200, grad norm exploded from thousands to billions within ~200 steps. 171M: same signature, `MaxLoad` 0.40-0.48 from step ~180, a real validation checkpoint was reached at step 500 (val loss 11.8515) before the explosion overtook it at step ~530.
- **C1 (the grad-norm co-trigger above) fired correctly both times** — 36M at step ~416 (`grad_norm_ema=5.9568e9` vs a `6,701,995` reference), 171M at step ~530 (`grad_norm_ema=1.7381e12` vs a `10,581,991,948` reference — a ~1,580× larger absolute reference than 36M's, and the relative/100×-threshold design still worked correctly despite that). Both produced a clean checkpoint save (`infra_preflight_final.pt`, `pilot171m_stabilization_final.pt` — see the evidence READMEs for why these specific files are not retrievable from this repo/laptop, a real orchestrator collection-rule gap, disclosed there) instead of an uncontrolled explosion. **C1 status moves from "candidate, applied, unverified" to "candidate, applied, verified working correctly on real hardware"** — the underlying divergence itself is still not fixed, only the safety response to it is now confirmed at two different scales.
- **Orchestrator bug found, unrelated to training-math (5th job, chess PoC):** the batch's chess job (`05_chess_5080_poc`) failed in 0.365 seconds with `ModuleNotFoundError: No module named 'scripts'` — a pure invocation-path bug in the standalone `kaggle_batch_runner.py` orchestrator (`python3 scripts/chess_5080_onefile.py` run with a `cwd` where `scripts` isn't importable as a package for the script's own `from scripts.chess_onefile_contract import ...`), not a chess-training or PoC result. No chess evidence exists from this run — the job never started training. **Fixed in `scripts/kaggle_batch_runner.py::run_chess()`** in this same pass — `python3 -m scripts.chess_5080_onefile` (module invocation, `cwd=repo_dir`) instead of `python3 scripts/chess_5080_onefile.py`, verified locally against this repo's real `chess_5080_onefile.py` (`scripts/__init__.py` makes `scripts` a real package; the `-m` form puts `repo_dir` on `sys.path`, not the script's own directory) — `python3 -m scripts.chess_5080_onefile --help` now resolves cleanly. The historical chess result from the 2026-07-25 run itself cannot be recovered (nothing to re-run against), but the orchestrator is correct for its next use.

## Post-45K — verified findings, deliberately NOT changed pre-run (so the run is un-confounded)
Each is documented with its mechanism in [DECISIONS.md](DECISIONS.md):
- **FIXED (2026-07-12), candidate — unverified by a dedicated GPU re-run:** z-loss effective weight was ≈2e-6 (1e-4 × 0.02 double-multiply, ~500× below the ~1e-3 Switch-Transformer/ST-MoE convention — corrected 2026-07-25 from a "~50×" arithmetic error that persisted in this file since 2026-07-12; `1e-3 / 2e-6 = 500`, confirmed by the fix itself: `z_loss_coef` moved `1e-4 → 0.05`, a 500× change, not 50×). `config/config.py:329` `z_loss_coef` changed `1e-4` → `0.05` (a single-line compensating change so the *existing* double-multiply structure lands on 1e-3, without restructuring `layers/moe.py`/`train/train.py`) — see `tests/test_z_loss_effective_weight.py`. Reopens the 2026-07-08 pre-45K freeze on this exact item, justified by the two-run divergence evidence above; still needs `cfc_moe_tolerance_check.py` + a real GPU re-run before being treated as settled.
- **Liquid `dt` fixed at 1.0** → canonical CfC is a gated RNN, not continuous-time. Either wire variable `dt` and ablate, or relabel.
- **GPU-perf only:** sequential MoE dispatch skips `capacity_mask` (correct output, wasted FLOPs); per-micro-step `.item()` host-device syncs. Measurable only on real GPU.
- **FIXED (2026-07-12):** `liquid_warmup_steps` now reads `TITAN_LIQUID_WARMUP_STEPS` (mirrors the `TITAN_ROUTER_LR_MULT`/`TITAN_WARMUP_RATIO` pattern in `config/config.py`); default unchanged at `10000`.
- **Investigated, deliberately left uncalled (2026-07-12):** `mark_weights_updated()` — `layers/liquid.py`'s `_ensure_qcache()` already auto-invalidates the quant cache every forward by comparing `_compute_weight_version()` (reads each param's PyTorch `._version` counter) against the cached version, so ordinary in-place optimizer steps (`.add_()`/`.copy_()`) never need this manual hook. The one case it would still matter — a full `.data = new_tensor` rebind, which resets `._version` to 0 and could coincidentally alias a stale cached version — is undetermined without a GaLore/checkpoint-load-path check. Left uncalled and undeleted; full reasoning now documented at the call site.

## Post-45K — holistic-read (EK-4) additions
- **RESOLVED (2026-07-19), LR re-warmup skeleton:** `grep -rli 're-warmup' train/` used to be 0 hits. `train/trainer_core.py::get_rewarmup_schedule()` (new, next to `get_wsd_schedule`) re-anchors to step 0 and ramps from wherever the base run's LR floor landed (`rewarmup_start_lr_ratio`, defaults to the same value as `min_lr_ratio`) back up to peak over `rewarmup_steps`, then repeats the same stable+cosine-decay shape as the base WSD schedule. `config.use_rewarmup_schedule` (default OFF, `TITAN_USE_REWARMUP=1` to opt in) selects it in `train/train.py`'s scheduler construction instead of `get_wsd_schedule` -- the canonical 45K run's own scheduler call is untouched, byte-for-byte, when the flag is off. `tests/test_rewarmup_schedule.py` (7 tests) covers the schedule's shape (cold-start ratio, linear ramp, stable hold, monotone cosine decay, floor-clamp past `num_training_steps` -- mirrors the WSD-clamp regression test's discipline) plus a config-default regression check. Written and tested now (doesn't need a checkpoint to write or unit-test); actually USING it for a real continuation run is still post-45K, since there's no checkpoint yet to resume from.
- **Evidence-gated banner WRITER (design post-run):** build the actual `PRE-TRAINING (UNVERIFIED)` → measured-status writer once a *real* 45K checkpoint + a *real* (non-zero) eval exist, so the gate can be designed/tested against genuine artifacts. A pre-built auto-writer is unsafe (a stray demo checkpoint + a stub `summary.json` "status: ready" with zero counts satisfies a naive gate and stamps a false "TRAINED" claim across ~50 files). The shipped `scripts/flip_status_banner.py` is therefore **report-only** for now; the writer (with explicit human confirm + non-zero-metric gate + frozen-path banners) is this deferred item.
- **Unit-test re-baseline pass — DONE (2026-07-09):** added `decoupled_rope`, top-p, MoE-capacity, quant-parity tests (+ held-out-ppl, repo-hygiene-guard). Count re-baselined `388 → 412 passed, 4 skipped`; every "388 passed" current-truth surface propagated.
- **Liquid training loop perf:** the `for t in range(T)` recurrence (seq=4096, 3 layers) is a real throughput cost; measurable/optimizable only on real GPU (profile-then-optimize, post-run).
- **`ffn_dropout` intent:** not in `config.py` → silently `0.0` in `layers/ffn.py` while `attention_dropout`/`dropout` are `0.1`. Confirm whether FFN-dropout-off is intended before any tuning.

## Cosmetic / housekeeping (safe, non-behavioral)
- **RESOLVED (2026-07-19), atomic-write hygiene — 5 files:** found via a scoped search for the same bug class as the already-fixed `data_pipeline.py` truncate-before-connect issue: a direct (non-atomic) file write to a path that some OTHER piece of code gates a resume/skip/readiness decision on via a bare `.exists()` check (no content validation), so a crash mid-write could leave a corrupt/truncated file silently accepted as "done." Fixed in all 5, all via the same temp-file + `os.replace()` pattern: `scripts/data_pipeline.py::download_stage()` (stage jsonl now written to a `.tmp` path and only replaced atomically once the full stage completes — extends the existing "leave old data untouched on zero-connection failure" guard to "leave old data untouched on ANY failure, including a mid-download crash"); `scripts/precompute_logits_topk.py` (`flush_chunk()` and `_flush_block()`'s `torch.save()` calls, new `_atomic_torch_save()` helper — the shards `precompute_logits_parallel.py::verify_stage_coverage()` and `titan_preflight.py`'s offline-readiness gate both trust via `.exists()` alone); `scripts/chess_5080_onefile.py::save_checkpoint()` (now uses a new `atomic_torch_save()`, matching the pattern its sibling onefile scripts — `kaggle_onefile_demo_build30.py` etc. — already had); `scripts/record_dataset_hashes.py` (new `_atomic_write_json()` helper; `datasets/hashes.json`'s existence alone PASSes `titan_preflight.py`'s offline-readiness gate, no JSON parse); `scripts/build_validation_set.py` (new `_atomic_write_jsonl()` helper; `titan_preflight.py` only counts non-empty lines in `datasets/validation.jsonl`, no `json.loads` per line). 7 new regression tests in `tests/test_atomic_write_hygiene.py` (crash-simulated via monkeypatched `torch.save`/`Path.open`/`Path.write_text` raising mid-write, asserting the pre-existing final file survives untouched; plus an end-to-end `download_stage()` test with a mocked HF source). Zero training-math/architecture change — this is write-hygiene only, on pipeline/tooling code, none of it the frozen 45K training loop itself (which already had atomic checkpoint writes via `os.replace()`, added in an earlier pass).
- **`train/train.py` structural review (2026-07-13):** `train()` bundles setup (signal/accelerator/data/model/optimizer/resume) and the entire main loop in one large function. The "true freeze" idiom (clear `requires_grad` + null out `.grad` for Liquid/tau params) is duplicated ~3x (warmup freeze, cooldown re-freeze-keep, cooldown-over unfreeze) instead of one shared helper — same risk class as the already-fixed decoupled-weight-decay bug, since a future edit to one copy could silently miss the others. Curriculum-stage/validation/saturation-gate logic is deeply nested inline rather than named helpers; `build_optimizer`'s GaLore parameter-group dicts are constructed unconditionally even when GaLore is off. None change behavior — deferred until the 45K run is stable; if only one gets done, extract `_true_freeze_liquid()` first.
- **`layers/liquid.py` `LiquidMixer` training-loop duplication (2026-07-14):** `_train_loop`, `_train_loop_precompute_input`, and `_train_loop_packed_pair` (plus their `torch.compile`-wrapped variants) each re-implement the same CfC recurrence with only the weight-matmul strategy differing. Same risk class as the `train.py` freeze-idiom duplication above — a numerical fix to one variant (e.g. the `time_decay`/`exp` clamp bounds) could silently miss the others. Not touched — this is the live Liquid mixer on the canonical training path; deferred until the 45K run is stable, same discipline as the entry above.
- `mark_weights_updated` dead-method review: **done, see above** (investigated, left uncalled with reasoning documented). `_compute_weight_version` relies on private torch `._version` (guard across versions); RoPE cache lazy-grow comment clarity; `iter_packed_sequences` greedy-buffer note.
- See `V2_BACKLOG_SEED.md` Track A–F for the full list (compile policy, distributed contract, optimizer matrix, etc.).

## Out of scope (documented, not pursued in repo)
- AGI/ASI capability rows in `reports/closure_57_matrix.md` are **out-of-scope pending** (require benchmark / long-horizon evidence) — see [INTERNAL_AGI_GAP.md](INTERNAL_AGI_GAP.md).
- The `orchestrator/` cognitive runtime + flag-off layers (`cognitive_extensions.py`, `world_model_head.py`, `lifelong_safety.py`, `qinn.py`) are **inert on the canonical 45K path** — out-of-scope, not part of the trained model. Documented (not deleted); see [ARCHITECTURE.md](ARCHITECTURE.md) "out-of-scope surfaces".

## Pre-45K — laptop preflight run-feedback (2026-07-02, real run signal)
Source: `evidence/2026-07-02-laptop-preflight/` (RTX 5070, 8 GB, commit `5fc5068`). The pre-flight validated the infrastructure (atomic checkpoint at step 500; guards live; graceful interrupt-save at step 981) AND produced a decisive negative training-dynamics finding: the run **diverged** (loss ~10.4 → ~15.0; grad_norm → `inf`, sustained `1e11`–`1e16`, survived only via `clip=2.0`; MoE load entropy 0.99 → 0.74). These are the pre-45K stabilization items — **documented as run-feedback; the frozen training path is NOT changed until each is applied and re-verified on a real run:**
- **LR regime:** `1.5e-3` is empirically fatal for this arch at this scale → sweep from `3e-4`, drop the router ×1.5 LR multiplier, lengthen warmup; target clip-hit rate `< 5%` (not "survive despite clip").
- **Liquid spike threshold:** absolute `loss>5.0` is scale-blind (never releases at this loss range → the Liquid layer is effectively untrained). Make it **relative** (`EMA×1.5`) + cooldown.
- **`generate()` Liquid state:** thread the Liquid hidden state through decode (as router state already is) + add a full-forward↔incremental-decode **parity test**.
- **Held-out perplexity harness:** fixed corpus + fixed seed + fixed script (today's eval cannot tell whether a run learned anything beyond its own loss curve).
- **100–300M pilot** before the real 45K (gate: clip-hit `<5%`, no persistent Liquid freeze, no MoE collapse, monotone held-out ppl).

## Pre-45K stabilization — APPLIED as candidate fixes (2026-07-08)
Source: `CLAUDE CODE — AUTONOMOUS EXECUTION BRIEF · Pre-45K Stabilization + Repo Final-Sweep Pass` (rev v2, 2026-07-08). The 2026-07-02 run-feedback items above are now **implemented in code**. They are **candidate fixes pending re-verification on a real GPU run** — nothing below is verified, and the divergence problem is **not** "solved" until a clean RTX-5070 re-run says so.
- **LR regime (T1.1):** `learning_rate` `1.5e-3` → `3e-4` (sweep start, NOT known-good); router ×1.5 differential dropped (`router_lr_multiplier` = 1.0); warmup `0.10` → `warmup_ratio` 0.15. All three sweepable without a commit: `TITAN_LEARNING_RATE`, `TITAN_ROUTER_LR_MULT`, `TITAN_WARMUP_RATIO`. Precision note discovered while implementing: the scheduler's real warmup was `int(max_steps*0.1)` = ~183 steps on that 1830-step run (NOT the `warmup_steps: 3000` echoed into the evidence JSON), so grad_norm began exploding at step ~80 — i.e. **during** warmup, at an effective LR of only ~6.6e-4, well under the 1.5e-3 peak. Treat `3e-4` as unverified.
- **Truth-fix (T1.1):** `warmup_steps` and `min_lr_ratio` were dead fields (`# informational only`) while the scheduler used hardcoded literals. Both are now actually read; `min_lr_ratio` default set to `0.01` so runtime behavior is unchanged (mechanism-only change).
- **Liquid spike threshold (T1.2):** absolute `loss > 5.0` → EMA-relative `loss > loss_ema * 1.5`, with the absolute value retained as a cold-start floor for the first `liquid_spike_ema_warmup_steps` (100) observations. Cold-start-safe EMA reuses `train/continual_adapter.py`'s seed-with-first-loss pattern. The cooldown/patience state machine is unchanged.
- **`generate()` Liquid parity (T1.3):** `MertFormerBlock.forward()` now takes `liquid_state` and returns the mixer's final hidden state (4-tuple); `MertFormer.forward()` threads `liquid_states` per layer and publishes them via `get_last_liquid_states()`; `generate()` carries them across decode steps; `reset_router_state()` clears them. New `tests/test_liquid_generate_parity.py` asserts full-forward ↔ incremental-decode parity **and** carries a negative control proving the old (unthreaded) path diverges.
- **Held-out perplexity harness (T1.4):** new `eval/held_out_ppl.py` — `--ckpt`-mandatory checkpoint-identity tokenizer load (no silent fallback), fixed corpus + fixed seed, deterministic packing via `train/packing.iter_packed_sequences`; writes `reports/benchmarks/held_out_ppl_summary.json` with commit + corpus/checkpoint SHA256 + an explicit claim boundary. Sanity-checked locally: a random-init model scores `mean_nll = 11.7649` against `ln(128000) = 11.7599` — i.e. the harness reproduces the uniform baseline.
- **Stabilization pilot (T1.5):** `config/model/mertformer_pilot_stabilization.yaml` — **measured** 172,668,035 params (via `trainer_core.preflight_param_report()`, not estimated; re-measured 2026-07-12 after fixing a `liquid_layers_idx`/MoE-layer collision that had made this config unloadable at all — `validate_layer_config()` raised on load; `[2, 5]` collided with MoE's `[2, 5, 8]`, fixed to the non-colliding `[1, 4, 7]`, which is 3 layers not 2, hence the new count); BitNet + 8e2 MoE + Liquid/CfC + GQA all present; `liquid_warmup_steps` = 667 (= 3000 × 10000/45000, documented arithmetic); runs under GaLore + 8-bit Adam for optimizer parity with the real 45K. **Stabilization-only; carries no capability or scaling claim** — see the STATUS.md reconciliation note.
- **Divergence circuit breaker (T1.6, NEW — not from this backlog):** finite-but-climbing loss now trips `safety_brake_reason = "loss_divergence_relative"` after `divergence_guard_patience` (50) consecutive steps where the loss EMA exceeds `1.5×` the EMA snapshotted at warmup end. Rank-local by construction, so the brake decision is **all-reduced** (mirrors the NaN-brake collective) to avoid DDP desync. `use_divergence_guard` defaults ON; `TITAN_DIVERGENCE_GUARD=0` disables. Logic ported from the inert `orchestrator/failure_budget.py` slope tracker into `utils/divergence_guard.py`, deliberately re-keyed off the loss EMA instead of wall-clock so it is deterministic and testable.
- **WSD scheduler clamp (T1.7):** the decay-phase cosine never clamped `progress` to `[0,1]`. If `TITAN_MAX_STEPS` changes between a checkpoint-saving run and a resume, `progress` can exceed 1 and the LR **rises back toward peak** instead of staying floored at `min_lr_ratio`. One-line clamp + `tests/test_wsd_schedule_clamp.py`.
- **Preflight loss-quality signal (Track 7):** `scripts/preflight_run.py evaluate_verdict()` was infra-only, so the 2026-07-02 divergence produced a clean PASS and a human had to eyeball the curve. It now emits a **non-blocking** warning into `REPORT.md` when `last_loss > first_loss`. PASS/FAIL semantics unchanged (still infra-only, by design).
- **Preflight optimizer parity (Track 7):** `preflight_run.py` no longer forces `use_galore=False, use_8bit_adam=False`. The 45K run uses `GaLoreAdamW8bit`, whose low-rank projection changes the effective step size in ways that do not map 1:1 to plain AdamW — so an LR "verified" under plain AdamW was verifying the wrong optimizer. `build_optimizer()` still degrades gracefully and logs the ACTIVE class.
- **Documentation-only (no behavior change):** `config/base.yaml` marked INERT (only read when `MERTFORMER_CONFIG` is set — nothing in the repo's tooling sets it); `dataloader_num_workers` annotated as force-overridden to 0 on both real training paths; `orchestrator/__init__.py` eager-import coupling documented (all 24 submodules execute on any `from orchestrator...` import — no live bug today, but any new unguarded dependency there would break `train.py`'s startup).
- **Real bugs fixed in inert / peripheral code (fixed, NOT promoted):** `AGIPaths.DATA_DIR` was referenced by `orchestrator/core.py:186` but never defined (instantiating `MertFormerOrchestrator()` raised `AttributeError`); `utils/logger.py` `source_sha256_status` relabelled `"pending"` → `"not_applicable_live_stream"` (a live-appending hash-chained JSONL structurally cannot embed its own final SHA); `utils/logger.py` secret-redaction list re-synced with `scripts/logbook_build.py` (the 40-hex WandB-key catch-all had drifted out of the live logger); `eval/golden_score.py` wrapper added so the real scorer is discoverable from `eval/` like its siblings.
- **Bounded sweep findings (Track 2), all verified live before fixing:** `mertformer_sdk/cli.py` `kpi-report` resolved `project_root=Path(".")` → any non-repo-root CWD silently produced an all-gates-failed KPI report while exiting 0; `mertformer_sdk/kpi.py` `_run_onnx_smoke` launched a hardcoded relative `.titan-venv/bin/python` (Python does not resolve a relative program path against `cwd=`) → `FileNotFoundError` that `check=False` cannot suppress; `scripts/overfit_gate.py build_sequences` off-by-one dropped the final window and produced **zero** sequences at exactly `len(tokens) == seq_len + 1`; `scripts/data_pipeline.py download_stage` truncated `stageN_data.jsonl` **before** attempting any connection, so a network outage destroyed existing stage data and then returned early (and `pilot.collect_risk_flags` only checks `.exists()`, so it reported the stage as present).
- **Test count:** `370 passed, 4 skipped` → **`388 passed, 4 skipped`** (+18: 4 Liquid-decode parity, 11 guard/EMA, 3 WSD clamp). Current-truth claim surfaces propagated; dated historical records (CHANGELOG, DECISIONS addenda) intentionally left at their as-of counts.

**Still the only real gate: owned compute → 45K run → checkpoint → checkpoint-bound eval.** Next non-documentary action is the RTX-5070 re-run with `TITAN_LEARNING_RATE=3e-4 TITAN_WARMUP_RATIO=0.15 TITAN_ROUTER_LR_MULT=1.0`.


## Pre-45K safe pass — DONE (2026-07-09)
GPU-free follow-up (tests + operational hardening; no training-math change). Full record in [DECISIONS.md](DECISIONS.md).
- **Done:** 6 test files (`decoupled_rope`, `top_p`, `moe_capacity`, `quant_parity`, `held_out_ppl`, `repo_hygiene_guard`); `check_disk_space` fail-open→fail-closed; SIGTERM→graceful-checkpoint in `train()`; removed the `TITAN_OCEAN_45K_LAUNCH` no-op; `datasets/validation_provenance.json` (+ `scripts/gen_validation_provenance.py`). Restored a gutted `interfaces/run_manifest_v1.schema.json` to sealed `bdee57a`.
- **Test count:** `388 → 412 passed, 4 skipped` (+24).
- **Deferred (with reason):** ADR-0005 naming-mode assert (needs a careful precompute-path detector; a wrong assert is riskier than the gap); `p100_safe max_steps` (demo-profile judgment call, low value); D5 auto-batch / D7 resume-order (frozen-path → wait for the RTX-5070 re-run to verify `bdee57a` first); `report_builder` baseline (already honestly labelled `hardcoded_target_threshold_not_measured`, not a bug).
  > ⚠ **CORRECTION (2026-07-25):** this "Deferred" line was stale — 3 of the 4 items were actually fixed two days after this section's 2026-07-09 date, in commit `8e8978f` (2026-07-11, `fix(core): correct 5 fake/misapplied fixes...`), and the note was never updated to say so. **ADR-0005:** the assert was a tautology (`getattr(cfg, "adr_0005_single_naming_mode", True) == True`, an attribute never set anywhere, so it could never fail) — replaced with real `detect_shard_naming_mode()`/`assert_single_naming_mode()` in `scripts/precompute_logits_topk.py`, wired into both the read path (`validate_logit_alignment.validate_stage`) and the write path (`precompute_logits_parallel.finalize_stage`); `tests/test_adr0005_naming_mode_guard.py` (9 tests, all passing) confirms it genuinely raises on deliberately-mixed shard-name fixtures. **`p100_safe max_steps`:** was literally `45000` — an exact numeric collision with the canonical 45K run despite a totally different batch_size/seq_len — fixed to `20000` with an explicit in-code disclaimer; `tests/test_kaggle_onefile_closure_build30.py::test_p100_safe_max_steps_does_not_collide_with_canonical_45k` pins it. **D5 auto-batch:** `dynamic_param_count` was read via `getattr` with a fallback constant that nothing ever actually set, so `auto_configure_batch_size()` only *appeared* to vary with model size — `_estimate_total_params()` now computes a real analytical estimate; verified end-to-end (the 171M pilot config vs. the canonical 3.67B config now produce different `total_params` AND different `auto_configure_batch_size()` output). **D7 resume-order:** audited in the same pass and confirmed already correct (the per-epoch `worker_info.seed`-based reseed at `train/trainer_data.py:135-136`) — left untouched on purpose, an independent verification that the existing fix holds, not a new change. **`report_builder` baseline is unchanged** — still correctly labelled `hardcoded_target_threshold_not_measured`, was never a bug. All 4 verified live in this pass (2026-07-25): functions exist, are wired in, and their tests pass.

- **FIXED + bisected (2026-07-12):** the ladder-overwrite bug above was root-caused. `tests/test_final_orchestrator_cli.py::test_final_orchestrator_check_only_releases_lock_and_stays_fast` monkeypatched `build_contract_outputs` with a fake writing `"{}"` to `root / "interfaces" / "run_manifest_v1.schema.json"`, and its `sys.argv` never passed `--project-root` — so `root` defaulted to `final_orchestrator.py`'s own `ROOT` (the real repo tree), and every `pytest -q` run gutted the tracked schema file for real. Fixed by adding `--project-root <tmp_path>` to both this test and its sibling `test_final_orchestrator_plan_only_writes_contracts`. Surfaced a related latent bug in `run_post_plan()`: `run_command()` runs with `cwd=root`, so its relative `"scripts/post_train_autorun.py"` only resolved when `root` happened to equal the orchestrator's own directory — any real `--project-root` override would have hit the same `FileNotFoundError`. Fixed by referencing the sibling script via an absolute path (`ROOT / "scripts" / "post_train_autorun.py"`) and forwarding `--project-root` through to it. Verified: `interfaces/run_manifest_v1.schema.json` byte-count (1907) unchanged across repeated full-suite runs post-fix; all 15 orchestrator/post-train-autorun tests pass.

- **FIXED (2026-07-12):** the staged-vs-tracked class bug above is closed. `repo_hygiene_guard.tracked_files()` now runs `git ls-files --cached --others --exclude-standard` instead of bare `git ls-files`, so untracked-but-not-gitignored new files are scanned even before `git add`. No `git add -A`-before-verify workflow discipline required anymore; verified clean on the current tree (`bare_except=0, build_fossils=0`) and the guard's own 4-test suite still passes.


## External review triage — hardening (2026-07-09, Fable-5 code review)
A deep external read (each finding independently re-verified against the code, adversarially) surfaced three run-time robustness issues in the newest, least-battle-tested code — the safety guards + the config loader. None is a correctness bug, a claim inflation, or a sealed-list issue. Split by what can be verified WITHOUT compute:

- **F2 — config overlay fail-open → fail-closed (DONE, applied 2026-07-09).** `config/config.py` `_load_yaml`/`_load_config_overlays`/`_apply_overrides` silently returned `{}` on a missing/misspelled/malformed overlay and silently dropped unknown keys — so a typo'd `MERTFORMER_MODEL_CONFIG` (or a mistyped key) started the run on canonical ~3.67B defaults with zero warning (same silent-wrong-result class as the Track 2 fixes). Now: an explicitly-requested overlay that is missing/malformed/non-mapping, or an unknown key, RAISES; non-required loads keep the old tolerant behavior byte-for-byte. Verified safe — all shipped overlays use only real fields (guarded by a test), and only `run.sh` (max_arch, file present) ever sets the env var; no test/CI path does. `tests/test_config_overlay_strict.py` (+9). Dormant on the canonical 45K path (TITAN_* env, no yaml overlay) and the pilot (`preflight_run.py` hardcodes dims), so this is standalone hygiene, not a training-path change.

- **F1 — divergence guard tracks the BLENDED loss whose composition shifts (APPLIED 2026-07-25, candidate — unverified by a dedicated GPU re-run).** The relative brake compared `loss_ema` (EMA of the blended `total_loss = (1-α)·CE + α·KD + aux`, snapshotted once at warmup-end) against `1.5×` the reference for `patience=50`. But `α` slides `0.8→0.15` over steps ~13500–36000, so the CE weight rises `0.2→0.85`; if CE > KD (typical late-training, as KD→0 while CE floors at the data entropy) the blended EMA drifts UP with zero divergence and — a sustained plateau, not a spike — trivially survives `patience=50`. **Applied fix:** the guard is now fed a SEPARATE CE-only EMA (`ce_ema`/`ce_ema_observations`/`warmup_end_ce_ema` in `train/train.py`, derived from `loss_ce.item()`); `loss_ema` was NOT repointed — it still feeds the Liquid spike guard unchanged, exactly as the apply-ready design specified. New tests in `tests/test_divergence_guard.py`: a pure-function demonstration that a blended EMA can drift purely from the alpha schedule while a CE-only EMA (with CE truly flat) never breaches, plus a source-scan regression pinning the `update_divergence_guard_state(loss_ema=ce_ema, reference_ema=warmup_end_ce_ema, ...)` call site. **Why still candidate:** its threshold behavior against real CE dynamics (and the residual curriculum-stage CE jumps at 18900/32400/36000/39600, which CE does NOT fix) is verifiable only on a run that approaches divergence — same discipline as every other training-math fix in this repo's history.

- **F3 — new guard/spike state not persisted across resume (APPLIED 2026-07-25, candidate — unverified by a dedicated GPU re-run).** The checkpoint `state` dict omitted all guard/spike locals (`loss_ema`, `loss_ema_observations`, `warmup_end_loss_ema`, `divergence_breaches`, `liquid_spike_counter`, `liquid_frozen_until`, plus F1's new `ce_ema`/`ce_ema_observations`/`warmup_end_ce_ema`), so every resume cold-started the safety machinery. (i) A resume at step ≥10000 with loss still >5.0 → the Liquid spike guard's absolute floor is active for ~100 fresh observations → at most one ~200-step Liquid-only freeze (~0.45%, self-healing) — minor. (ii) **The real one:** the divergence guard re-took its reference from the CURRENT loss on resume, so a crash+resume WHILE diverging (and `TITAN_AUTO_RESUME` defaults ON in `launch_ocean_45k.sh`) re-baselined the brake to the already-broken level. **Applied fix:** `save_checkpoint_smart` now accepts an optional `guard_state` dict (all 6 call sites in `train/train.py` pass it via a `_current_guard_state()` closure); on resume, the init block restores each field via `resume_payload.get("state", {}).get("guard_state") or {}` then `.get(name, <current-default>)` — additive and backward-compatible: checkpoints saved before this fix simply lack the key, and every field falls through to today's fresh-start default. New tests in `tests/test_resume_policy.py`: persistence, backward-compat omission when not provided, full save→load round-trip, graceful handling of a pre-F3 checkpoint, and a source-scan regression on the restoration wiring itself (6/6 call sites confirmed wired). **Why still candidate:** it changes resume-time control behavior, verifiable end-to-end only on a real resume; F1 and F3 co-touch the same persisted set (`ce_ema` et al.), which is why they landed in the same commit.

**Net:** the review reinforced "one gate = compute" — everything real here only bites once a run is burning. F1+F3 are two faces of one soft spot (the divergence guard's reference); both are now applied code (2026-07-25), same "candidate, unverified" discipline as the LR/z-loss fixes before them — the 45K/RTX-5070 re-run remains the verification event for all of them together.


## External review triage — hardening, second pass (2026-07-23, Fable-5 single-file consolidation audit)
A second, independent Fable-5 pass consolidated the training core (`layers/`, `mertformer_sdk/kernels/`, `config/build_label.py`, `utils/divergence_guard.py`) into a standalone `mertformer_titan_core.py` as a portability/correctness audit exercise — explicitly NOT a replacement for the real multi-file DDP/GaLore/distillation training pipeline (the consolidated file itself documents that it drops distillation, curriculum, checkpoint-resume, and DDP; it stayed outside this repo, in a scratch location, and was never intended to run the 45K). Its findings were independently re-verified against the ACTUAL repo code — not just Fable-5's own reimplementation — before anything landed:

- **DONE (2026-07-23), MoE dispatch-parallel MPS bincount → scatter_add_.** `layers/moe.py::MoE._dispatch_parallel` used `torch.bincount(expert_sorted, ...)`, inconsistent with the file's own telemetry path a few lines above (`MoE.forward`, already on `scatter_add_` with an explicit "MPS Safe Bincount" comment). Verified: `torch.bincount` actually WORKS on MPS on both the ad-hoc test environment (torch 2.10.0) and this repo's own `.titan-venv` (torch 2.13.0) — so this was not an active local crash, it is an internal-consistency + `requirements.txt`'s open-ended `torch>=2.0` portability fix (older torch releases have historically lacked MPS bincount coverage). Replaced with the identical scatter_add_ pattern; behavior byte-for-byte preserved. `tests/test_moe_dispatch_parallel_counts.py` (4 new tests): scatter_add_ vs bincount numeric equivalence, zero-count-expert handling, a real forward+backward through the actual dispatch path on MPS, and a parallel-vs-sequential routing-parity check (logits must match within float tolerance). All pre-existing MoE tests re-run green alongside.

- **INVESTIGATED, NOT REPRODUCIBLE (2026-07-23) — MoE dispatch bf16/fp32 dtype-mismatch claim.** Fable-5's own consolidated reimplementation flagged a potential `index_add_`/masked-`+=` dtype crash under bf16 autocast in `_dispatch_parallel`/`_dispatch_sequential` (gate weights stay fp32 off an explicit `.float()` router path; expert output autocasts to bf16). An isolated unit check confirms `index_add_` genuinely DOES raise on a real bf16/fp32 mismatch in this torch version, so the risk class is real in principle. But a full-model reproduction against the ACTUAL repo code (CPU `torch.autocast(dtype=bfloat16)`, `moe_dispatch_mode="parallel"`) did not crash: the residual-stream accumulator stays fp32 throughout, because every `x = x + <bf16 BitLinear output> * scale` residual add auto-promotes back to fp32 under normal type-promotion rules — so `x_flat`/`out_flat` inside `MoE.forward` never actually become bf16 in this repo's real dtype flow. Left UNCHANGED — no fix applied for an unreproduced bug. Cannot be fully ruled out on real CUDA autocast (untested here, no GPU available); if a future real-GPU bf16 run ever raises an `index_add_`/`+=` dtype error in MoE dispatch, this note is the pointer to the fix (`.to(dtype=out_flat.dtype)` on the gate weights before use, mirroring the existing `gate_scale = gate_val.to(dtype=shared_out.dtype, ...)` pattern a few lines below in the same file).

- **CANDIDATE, GPU-only, not applied (2026-07-23) — Triton fused BitLinear backward activation memory.** `mertformer_sdk/kernels/triton_fused_bitlinear.py::_TritonFusedBitLinearSTE.forward` currently `ctx.save_for_backward`s both the raw fp `x`/`w` tensors AND their quantized copies+scales; the fused Triton backward kernels only read the quantized copies+scales (the raw tensors exist only to serve the env-gated eager-backward fallback, `MERTFORMER_FUSED_BACKWARD=0`). Saving only the quantized tensors would roughly halve per-BitLinear-layer backward activation memory on the fused CUDA path. Not applied — this is CUDA/Triton-only code and cannot be executed or verified on any machine available for this pass (no CUDA GPU). Apply+verify on the next real GPU session, alongside confirming the `MERTFORMER_FUSED_BACKWARD=0` eager-fallback branch still has what it needs (it would need to re-derive or separately retain the raw tensors if this lands).

- **Cosmetic, deferred (2026-07-23) — `RMSNorm`/`_QKRMSNorm` duplication.** `layers/mla.py::_QKRMSNorm` and `layers/mertformer_block.py::RMSNorm` are byte-identical forward bodies (both apply the same fp32-stable RMS + weight scale). Same risk class as the already-logged `train.py` freeze-idiom and `layers/liquid.py` training-loop duplication entries above — a future numeric tweak to one could silently miss the other. Not merged — purely structural, zero numeric bug, and per the same standing pre-45K discipline as those two entries, refactoring shared code near the frozen path isn't worth the risk for a readability-only gain this close to launch. Logged for a post-45K pass.

**Net:** one real, low-risk, tested, applied fix (bincount portability); one claimed bug investigated and found NOT reproducible in the real repo (documented rather than silently fixed or silently dropped); two items correctly deferred pending real GPU hardware, matching the existing F1/F3 precedent exactly. The consolidated single-file audit tool itself stays outside this repo (scratch location) — it is a diagnostic aid, not a training-path artifact.


## Nutrition5k publication readiness (2026-07-25)
A separate claude.ai conversation (not this repo, no repo access) reviewed the Nutrition5k side-experiment's evidence docs (`evidence/2026-07-18-nutrition5k-rtx5070/REPORT.md` and `README.md`) for publication-readiness (i.e. before either is put in front of an external reviewer or the public gist) and flagged concrete gaps. Those findings existed only in that external chat transcript — `grep -rn nutrition BACKLOG.md DECISIONS.md` returned zero hits before this entry, meaning a future session with only repo access would never see them. Recorded here so that stops being true.

- **N1 — missing-image comparability caveat: ALREADY CLOSED, no action needed.** `evidence/2026-07-18-nutrition5k-rtx5070/README.md`'s "🟡 Known, real, documented limitation — partial dataset" section already states the real numbers (2,755/4,059 train, 507/709 test staged) more explicitly than what was being asked for. The raw script-generated `REPORT.md` (Section 2's bare table) doesn't carry the same explicit caveat, but `README.md` is the curated, publication-facing document; verified 2026-07-25 that it already covers this.
- **N2 — FIXED (2026-07-25).** The "much larger" backbone framing (`REPORT.md` Section 1, `README.md` in two places) implied a param-count gap without ever citing a number, and an earlier, informal pass on this exact document (in the same external conversation) had guessed "InceptionV2 ≈11M, this model is bigger" without verification. That guess was independently checked here via web search before writing anything: commonly-cited parameter counts for the InceptionV2/BN-Inception family run **~17.2M–23.2M** depending on exact variant (BN-Inception / Inception-v3 naming is itself inconsistent across sources) — i.e. the same order of magnitude as this model's measured 14,074,631 params, not "much larger" by count, and not confidently smaller either as the earlier guess claimed. Correction appended (not a silent rewrite of the dated 2026-07-18 snapshot) to both `REPORT.md` and `README.md`: the real, defensible gap is JFT-300M pretraining + full floating-point precision, not raw parameter count.
- **N3 — DONE (2026-07-25, Kaggle T4×2).** Liquid-OFF ablation on Nutrition5k, real training run (46/60 epochs, early-stopped). Result: calorie MAE 73.4 kcal / 28.7% (vs the 2026-07-18 Liquid-ON baseline's 75.5 kcal / 29.6%) — a slight *improvement* with Liquid removed, all other 4 targets within ~0.5pp of baseline. Consistent with, and adds real-task evidence to, the existing 12-seed toy-scale finding (`ABLATION.md`: Δ−1.63pp, p=0.305, "does not earn its cost") — does **not** reopen the sealed Liquid-keep decision for the 45K canonical run (that decision is 45K-architecture-scoped; this is a separate small vision side-experiment). Full detail: `evidence/2026-07-25-nutrition5k-liquid-off/README.md`.
- **N4 — DONE (2026-07-25, Kaggle T4×2).** MoE-OFF ablation on Nutrition5k, real training run (47/60 epochs, early-stopped). Result: calorie MAE 79.0 kcal / 30.9% (vs baseline 75.5 kcal / 29.6%) — *worse* on every one of the 5 targets, the opposite direction from N3. Read together, N3+N4 suggest MoE contributes real measurable value to this task while Liquid does not — a genuinely different comparative signal than the toy ablations (which never tested MoE). Does not reopen the sealed MoE-8e2 architecture decision (separate, non-canonical side-task). Full detail: `evidence/2026-07-25-nutrition5k-moe-off/README.md`.
- **N5 — ADDED (2026-07-25).** One interpretive sentence in `README.md`/`REPORT.md`'s results section: calorie/mass MAE% (29.6%/24.9%) land closer to the paper's own pretrained baseline than fat/carb/protein MAE% (42.5–45.1%) do — plausible reading: calories/mass correlate with visible volume/size, a signal a from-scratch trunk can pick up; macros require ingredient-level recognition, which is closer to what JFT-300M pretraining specifically provides and this trunk lacks.
- **N6 — ADDED (2026-07-25).** A short diagnostic-value paragraph: the same BitLinear/MoE/LiquidMixer layers trained stably for 58/60 epochs on this task (plain AdamW, no GaLore, no 8-bit Adam, no distillation, no curriculum, and predating the z-loss double-multiply fix entirely since this task has no MoE z-loss term at all) with zero divergence — narrowing evidence that the two RTX-5070 LM-side divergences (2026-07-02, 2026-07-12) trace to the *training regime* (LR/optimizer/z-loss/curriculum interaction), not a defect in the shared layers themselves.
- **N7 — verified closed (2026-07-25), no action needed.** The `<WIN_HOME>` placeholder in `REPORT.md`'s checkpoint paths is a genuine redaction, not a leaked username: `scripts/train_nutrition5k.py`'s `CKPT_DIR`/`WORK_DIR` build a real absolute Windows path with `str(best_path)`, so the raw script output would contain an actual username; the single commit that added this file (`20eb14c`) already carries the `<WIN_HOME>` placeholder, confirming it was manually redacted before landing in the repo.
- **GI1–GI5 (gist reorg) — DONE 2026-07-25 for GI1/GI2/GI4 (partial)/GI5, N/A for GI3.** Mert explicitly gave the gist URL and asked for it directly, closing the earlier "no write access to the secret URL" blocker. Via `gh api gists/<id> --method PATCH` (one atomic multi-file call, matching the established pattern from the 2026-07-23 pass), then a full `§24.7` leak-grep of all 19 resulting files (clean: no home-path leaks, no secret patterns, no dead internal path refs):
  - **GI1 — done.** New `0_NUTRITION5K_VISION_SIDE_EXPERIMENT.md`, named to sort first, with an explicit "why this file is first" framing note (this Gist's other files document *readiness*; this one is the single completed, independently-re-verified result).
  - **GI2 — done.** `9_LAPTOP_PREFLIGHT_2026-07-12_V2_INFRA_EVIDENCE.md`'s "~50x below the standard MoE-literature convention" corrected to "~500x", with the arithmetic (`1e-3/2e-6=500`) inline. This same arithmetic error was also found live in the *canonical repo docs* (`BACKLOG.md` line 38 — this file — and `DECISIONS.md`, both fixed in this same pass; `DECISIONS.md`'s *other* "~50x" instance, comparing against the apparent un-double-multiplied `1e-4` rather than the `1e-3` convention, is independently correct and was left alone).
  - **Test-count sync (not originally a lettered GI item, but the same class of live-drift the GI list exists to catch):** `3_verified_matrix.md` and `4_README.md` updated `564→572 passed, 5 skipped`, matching this repo's current count. `6_EXECUTIVE_BRIEF_2026-05-22.md`'s `505 passed` intentionally left untouched (dated historical record, per the existing precedent noted elsewhere in this file).
  - **GI3 — N/A, not a bug.** The "test-count inconsistency between two gist files" is exactly the `6_EXECUTIVE_BRIEF` vs. everything-else pattern just described — the executive brief is a frozen dated snapshot by design, not a live-truth surface. Nothing to fix.
  - **GI4 — partial.** The original recommendation ("drop distillation from the initial ask") was NOT applied as-is: it would contradict this repo's own locked launch decision (`online_teacher` lane, not `offline_clean` — see DECISIONS.md "Eight launch-time decisions locked..."), so applying it would have silently reopened a sealed decision. Applied instead: a scope-clarifying note on `compute_sponsorship_request.md`'s 8-10 hour ask, making explicit that the window sizes the student-training step only, assuming corpus + teacher-logit precompute are prepared separately beforehand — closes the "ask doesn't match what's achievable in that window" problem without touching the lane decision.
  - **GI5 — done 2026-07-25.** Mert explicitly authorized proceeding ("yap bunu da") rather than waiting further. `1_one_pager.md`'s "Business Model" section (was: "Licensing for enterprise on-device deployments and private inference stacks.") replaced with "Project Status" — states plainly this is a solo, self-funded research/engineering project, not a company, with no commercial offering, and that large-scale trained-checkpoint evidence doesn't exist yet. The "Ask" section (was: "Pilot partners for edge coding workflows and strategic deployment design.") replaced with three research-framed asks: technical review/critique, compute sponsorship (pointing to `compute_sponsorship_request.md`), and research/engineering roles or collaboration. Chose research framing over pitch framing to stay consistent with the rest of the gist's evidence-first tone and the file's own existing reviewer-routing disclaimers, which the old Business Model/Ask language was in tension with. Pushed via the same atomic `gh api ... PATCH` pattern; re-cloned fresh and re-ran the `§24.7` leak-grep — clean.


## Desk hardening pass — CI gates + A2 proof (2026-07-09)
The safe, no-compute subset of the review-triage recommendation, each gate added under the zero-false-positive rule (every new gate is clean on the current tree). No training-math change.
- **I.2.5 A2 — router batch-isolation PROVEN (no `layers/moe.py` change needed).** `MertFormer.reset_router_state()` already lays a fresh per-sample zero state at each `generate()` and the router buffer rolls per batch-row, so the isolation the roadmap worried about already holds — the real gap was the missing PROOF. Added `tests/test_moe_router_batch_isolation.py`: one sample's tokens never change another's router logits (batched row 0 == row 0 run alone), and identical rows stay bit-identical. Closes A2 as "guarantee present, test was missing".
- **I.7.4 CI hardening (all checkpoint-free, wired into `.github/workflows/ci.yml`):** `bandit` security scan at HIGH severity + HIGH confidence (`-lll -iii`, venvs/tests excluded) — a regression backstop, not a style linter; the one HIGH/HIGH finding (B324 SHA1 in `build_validation_set._fingerprint`) was a dedup fingerprint, marked `usedforsecurity=False`, so the gate is 0-clean. `tests/test_no_circular_imports.py` imports the eager `orchestrator` package + every submodule in a subprocess so a new cycle fails here, not at `train.py` startup. `tests/test_property_moe_capacity.py` — `hypothesis` property tests pinning the capacity-formula invariants (≥1, monotonic in factor, covers the balanced share). `interrogate` docstring-coverage floor (`fail-under=15`, current ~16.7%) as a regression guard. `hypothesis`/`bandit`/`interrogate` added to `[project.optional-dependencies].dev`.
- **Test count:** `421 → 428 passed, 4 skipped` (+7). Current-truth surfaces + gist propagated. Still the only real gate: owned compute → 45K.

## Final closure pass — 91-item pre/post-45K backlog sweep (2026-07-12)
A full disposition pass over every item raised in the pre-45K/post-45K planning conversation, classified per item as done / prepared-but-honest / decision-brief / hardware-gated / human-only / already-closed (never silently skipped). Full detail in `reports/` (see file list below); this entry is the index.

- **Version-string single-source migration (done):** 42 files hardcoding `__version__ = "1.0-BUILD30-V2"` (not 38 — the real count) migrated to `from config.build_label import BUILD_LABEL as __version__` (new `config/build_label.py`, zero-dependency leaf module). `layers/qinn.py`/`scripts/chat.py` deliberately left untouched — they intentionally use the separate `mertformer_sdk.__version__` SDK-semver scheme, not a bug.
- **4 new closure gates wired into `verify_all.sh`:** `check_dead_attribute_disease.py` (getattr/hasattr dead-attribute scanner, soft-fail by default), `check_overlay_validity.py` (loads and validates all 4 `config/model/*.yaml` overlays in fresh subprocesses — this is exactly the automated check that would have caught the pilot yaml's Liquid/MoE layer collision automatically), `check_facts_drift.py` (live-recomputes small-overlay param counts + canonical architecture estimate against `reports/FACTS.json`, distinct from the pre-existing `check_facts_consistency.py` string-consistency check), `check_circular_imports.py` (package-level static graph scan, soft-fail — complementary to the pre-existing `tests/test_no_circular_imports.py`, which only covers `orchestrator`'s own eager-import chain). `scripts/linkify_doc_paths.py` (manual maintenance tool, not CI-wired — read-link validity is already covered by `scripts/md_integrity_check.py`).
- **bandit was already CI-wired (found, not missed) — added a LOCAL mirror.** `.github/workflows/ci.yml` already ran `bandit -lll -iii` (HIGH severity + HIGH confidence); the gap was that `verify_all.sh` (the local pre-push gate) never called it, so there was no local signal before push. `scripts/check_bandit_security_scan.py` mirrors the exact existing CI policy and exclude list rather than inventing a stricter one. Hit and fixed 3 compounding bugs building this one script: (1) `-x` needs `./`-relative paths, not bare names or absolute paths — bandit's own exclude matching is walk-relative; (2) `sys.executable` isn't reliable — bandit lives in `.titan-venv`, not necessarily on the caller's own interpreter; (3) a failed bandit subprocess left a stale report file silently re-read as if fresh. All three only caught by writing a real end-to-end test that runs bandit for real, not a string-shape unit test.
- **6 new checkpoint-bound eval probes** (`eval/calibration_ece.py`, `adversarial_prompt_robustness.py`, `bias_fairness_probe.py`, `toxicity_probe.py`, `hallucination_rate_probe.py`, `membership_inference_probe.py`), all sharing `eval/_probe_common.py`'s honest pattern: `SKIPPED`/`NO_CHECKPOINT` before a real checkpoint exists, `random_init_smoke` (never `measured`) under `--allow-random-weights`, real small-scale methodology once a checkpoint exists. `scripts/check_benchmark_regression_gate.py` compares them against a stored baseline (checkpoint-bound, currently `SKIPPED`).
- **Real bug found+fixed while building the above, unrelated to its own stated purpose:** `eval/` had no `__init__.py` (a PEP 420 namespace package), which loses to any same-named REGULAR module found later on `sys.path` — `scripts/eval.py` (an unrelated benchmark-suite CLI) shadowed it, breaking `from eval.X import Y` for any code run as `python3 scripts/<anything>.py`. Fixed with `eval/__init__.py`; regression test in `tests/test_eval_package_shadow_regression.py`.
- **Real bug found+fixed in `scripts/build_scoped_external_intake_matrix.py`:** an unreadable external file (OS permission denial on a `Downloads/` entry) crashed the ENTIRE `verify_all.sh` ladder via an uncaught `PermissionError` in `sha256_file()` — one bad file outside the repo's control blocked every downstream gate. Fixed with a `safe_sha256_file()` wrapper that warns and skips instead of crashing; `verify_all.sh` now completes end-to-end again.
- **`scripts/measure_random_init_baseline.py` (new):** measures the REAL random-init score on `eval/generalization_suite.py`/`eval/agentic_suite.py`'s own harnesses, replacing the honest-but-unmeasured `0.80`/`0.50` placeholder constants' basis with an actual number — deliberately does NOT auto-rewrite those constants (whether "baseline" means floor or aspirational gate threshold is a real semantic question, left as a human decision).
- **`TITAN_STRICT_TOKEN_BUDGET=1` enabled** in both `scripts/launch_8xb300.sh` and `scripts/launch_ocean_45k.sh` (previously coded but never turned on for the real launch) — verified safe against the real token math (23.59B planned vs 23.6B target, no false-trigger).
- **Master Protocol document** (`~/Documents/MertFormer_Kapanış_Release_TruthSync_Master_Protokolu_2026-05-12.md`, Documents-external, no repo commit): 9 gaps fixed, including `⚠ DÜZELTME` correction-blockquotes on its own two historical addenda that (like this repo's `1fdd9d6`/`64dd837`) had claimed fixes that turned out to be cosmetic.
- **13 new `reports/*.md` deliverables** for decision-briefs, checklists, risk analyses, and drafts that need a human decision or a future checkpoint but have real, grounded content prepared now: `lane_cost_tradeoff_brief.md`, `liquid_keep_or_drop_brief.md`, `divergence_guard_decision_brief.md`, `launch_time_decisions_checklist.md`, `stage3_mixture_freeze_proposal.md`, `paper_outline_draft.md`, `blog_liquid_ablation_draft.md`, `anthropic_fellows_application_checklist.md`, `publication_readiness_kit.md`, `post_45k_research_agenda.md`, `responsible_ai_checklist.md`, `data_provenance_audit_template.md`, `data_poisoning_risk_analysis.md`, `independent_signoff_template.md`.
- **`scripts/estimate_shard_disk.py`, `scripts/estimate_carbon_footprint.py`:** real arithmetic calculators, no checkpoint needed, ready to use.
- **Audited, no gap found (I.7 #82):** `scripts/chat.py`'s `input()` and `mertformer_sdk/cli.py`'s `subprocess.run()` calls were reviewed for injection risk — both already safe (text-only model input, list-form subprocess args, no `shell=True`).
- **N/A, not silently skipped (I.7 #83):** SDK rate-limiting doesn't apply — `mertformer_sdk` is a local-inference SDK with no server/API surface to rate-limit.
- **Already closed, verified (I.1 #90):** the "reject harmful-autonomy/covert-surveillance framing" item is already documented in `MISSION.md`, `USE_POLICY.md`, and `SECURITY.md`.
- **`scripts/preflight_run_pilot171m.py` added to the repo** (was previously zip-only) — 172.67M param Go/No-Go pilot orchestrator, sibling to `scripts/preflight_run.py`; `REPRODUCE.md`/`REPRODUCE_TR.md` gained a discoverability section for both.
- **Not resolved, flagged honestly:** items #33 ("4 env escape-hatches") and #38 ("versionless skill-proof files") reference wording from an earlier planning brief this pass could not precisely re-locate in the current live docs; the closest verifiable equivalents (launch-script `TITAN_PREFLIGHT_*` safety defaults, no discovered `--no-validate` bypass path) are already in place, but a precise 1:1 match to the original item text was not confirmed. Left open rather than claiming false closure.

## Public-release closure (2026-07-30) — what is done, what stays open

Verification performed for this pass, with results rather than assertions:

- **Closure ladder:** `bash scripts/final_one_shot.sh` alone -> `[final] COMPLETED`, exit 0.
  pytest `726 passed, 5 skipped`; closure-57 matrix 57/57 green with no in-scope pending;
  tokenizer canonical/mirror byte-identical; FACTS single-source consistency OK; doc-claim
  consistency OK; release-zip denylist + secret scan 0 hits.
- **Git-history secret scan: DONE.** gitleaks was absent and was installed (8.30.1) rather
  than reported as unavailable. 546 commits / ~43.9 MB scanned; 6 hits, ALL verified false
  positives before any allowlist was written -- `generic-api-key` matching the JSON field
  NAME `"key"` in reports/kpi_report_v1.json, whose values are KPI slugs
  (`kpi01_verify_all`, entropy 3.51-3.73). `.gitleaks.toml` records this and allowlists it
  narrowly (one rule, one path, one value pattern). Rescan: no leaks found.
- **Working-tree scan:** scripts/secret_scan.py clean over 950 tracked files; no tracked
  file carries an absolute `/Users/...` path.
- **Clean-clone verification: performed, and it caught a real defect.** A fresh clone plus
  full suite found `tests/test_pre45k_gate.py::test_run_offline_preflight_against_real_repo`
  failing, because it asserted the offline preflight passes -- which requires the gitignored
  stage corpus. It passed only on the machine holding local data; every contributor and CI
  runner would have failed it. Fixed in both directions (skip without the corpus, assert the
  honest FAILURE reason when absent) and re-verified: clean clone `722 passed, 9 skipped, 0
  failed`.
- **Gist `dac0aa0c...` synced.** Grepped for `/Users/`, the username and
  `gho_`/`ghp_`/`sk-ant-` patterns BOTH before and after: 0 hits each time. Updated the test
  count, the licence line, the project-structure reference (it still listed the two deleted
  scripts), and added the hiring/contribution sections. The dated
  `6_EXECUTIVE_BRIEF_2026-05-22.md` deliberately keeps its historical `505 passed`, and the
  "private repo" references were deliberately NOT changed -- the repository is still
  private, and pre-emptively rewriting them would be a false claim.

**STILL OPEN, deliberately, and NOT closed by this pass:**
- The 45K run itself, corpus materialization (23.59B tokens), teacher-logit precompute,
  a real 2-GPU DDP smoke, and K-1 (Liquid perf) -- all compute-gated. `pre45k_gate.sh
  --strict-ddp` on this GPU-less machine returns `PASS_DDP_NOT_APPLICABLE`, which is the
  correct verdict here, not a pass for DDP correctness.
- `train/train.py::train()` refactor and O-6 (preflight duplication) -- deliberate, see the
  entries above.
- Repository visibility. The code side is ready; flipping Private -> Public is the owner's
  decision and the owner's action.

## CI-bypass env leak defeated the new offline-preflight negative test (2026-07-31)
The negative test `c2c212e2` added, `tests/test_pre45k_gate.py::test_offline_preflight_reports_the_missing_corpus_rather_than_passing`, asserts `gate.run_offline_preflight(...)` genuinely returns `ok: False` with no corpus on disk — but `run_offline_preflight()` launches its subprocess with `env = dict(os.environ)`, so the test inherited whatever of `scripts/titan_preflight.py::check_stage_jsonl()`'s two deliberate escape hatches (`GITHUB_ACTIONS`/`CI` == `"true"`, added `7689be19`; and `TITAN_PREFLIGHT_ALLOW_MISSING_STAGE_JSONL=1`, exported by `scripts/verify_all.sh` whenever `TITAN_OFFLINE=1`, CI's own default) happened to be ambient in its own environment. Neither was ambient on a bare local `pytest`, so the test passed there and shipped in `28fc986f` — but real GitHub Actions CI runs via `bash scripts/verify_all.sh`, which exports the override before `pytest` even starts, so the test's own subprocess got `ok: True` and the assertion failed in CI. **Fixed** by having the test `monkeypatch.delenv(...)` all three vars for its own call only, immediately before invoking `gate.run_offline_preflight(...)` — the same `monkeypatch.delenv(..., raising=False)` pattern already used in `tests/test_titan_preflight_contract.py` and four other test files. Both escape hatches remain byte-identical in `scripts/titan_preflight.py` and `scripts/verify_all.sh`; only the test's own view of its environment changed. Verified: with `GITHUB_ACTIONS=true`/`CI=true`/`TITAN_PREFLIGHT_ALLOW_MISSING_STAGE_JSONL=1` set ambiently (simulating CI), the test fails pre-fix and passes post-fix; test count unchanged.
